#include <bits/stdc++.h>
using namespace std;
int main() {

    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);
    int n, k;
    int a[10010];
    cin >> n >> k;
    if(n <= 2)
    cout << 0 << endl;
    else{
        cout << 2 << endl;
    }

    return 0;
}