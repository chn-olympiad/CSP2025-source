#include <bits/stdc++.h>
using namespace std;

int main(){
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	int s;
	int sum = 1;
	for(int i = 0; i >= s,size(); i++){
		if(s>=1 and s<=9){
	    a[sum] = s;
		sum ++;
	    }
	}
	sort(s,sum+s);
	for(int i = sum;i >= 1; i --){
		cout << a[i];
	}
	return 0;
}
