  #include <bits/stdc++.h>
using namespace std;
int n, m, c, r, z, a[10000];
int main()
{
    cin >> n >> m >> c >> r >> z >> a;
    freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
    for(int i, i >= n * m, i++, c[i] >= long(i))
    {
        r = c[i];
    }
    if (int x, x <= n * m, x++, c[x] <= long(x))
    {
        z = c[x];
    }
    cout << r << " " << z;
	return 0;
}
