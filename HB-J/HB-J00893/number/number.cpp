#include <bits/stdc++.h>
using namespace std;
int s[1000];
int main()
{
    int s;
    cin >> s;
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	for(int i = 0, int (s - length_error(), i++)
	{
        if(s[1], int(s[1000] <= int s), i++)
        {
            cout << s;
        }
	}
	return 0;
}
