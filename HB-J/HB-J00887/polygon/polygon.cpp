#include <bits/stdc++.h>
using namespace std;
const int MOD = 998244353;
int n, a[5005];
int main() {
    freopen("polygon.in", "r", stdin);
    freopen("polygon.out", "w", stdout);
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];
    srand(time((unsigned long long)0));
    cout << rand();
    return 0;
}
