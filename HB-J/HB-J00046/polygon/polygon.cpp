#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int arr[4],maxa = -1;
    for(int i= 1;i <= 4;i++)
    {
        cin >> arr[i];
    }
    for(int i = 1;i <= 4;i++)
    {
        maxa = max(maxa,arr[i]);
    }
    if(arr[1] )

    return 0;
}
