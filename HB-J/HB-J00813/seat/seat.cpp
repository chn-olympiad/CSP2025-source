#include<bits/stdc++.h>
using namespace std;

int main(){
    freopen("seat.in","r",stdin);
    freopen("seat.put","w",stdout);
    int a , b , p[10010] , q[10010];
    cin >> a >> b;
    for(int i = 1 ; i <= a*b ; i++){
        cin >> a[i];

    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
