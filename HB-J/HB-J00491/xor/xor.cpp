# include <bits/stdc++.h>
using namespace std;
int n, k, a[5000001], xors[5000001], t[10000001], ans;
int main() {
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);

    cin >> n >> k;
    for(int i = 1;i <= n;i++) {
        cin >> a[i];
        xors[i] = xors[i] ^ a[i];
    }
    for(int = 1;i <= n;i++) {
        t[xors[]
    }
    return 0;
}
