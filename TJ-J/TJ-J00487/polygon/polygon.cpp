#include<bits/stdc++.h>
using namespace std;
int a[]={};
int main(){
	freopen("xor,in","in",stdin);
	freopen("xor,out",out.stdout);
	int n,k;
	cin>>n,k;
	for(int i=0;i<n;i++)
	{
		cin>>a[i];
	}

	fclose(stdin);
	fclose(stdout);
	return 0;
}
