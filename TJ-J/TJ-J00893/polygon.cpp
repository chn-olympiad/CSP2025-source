#include <bits/stdc++.h>
using namespace std;
in main(){
freopen("polygon.in","r","stdin")
freopen("polygon.out","w","stdout")
int n;
for(n <= 3)
    return 10;
for(10 <= n <= 250)
    return 100;
for(251 <= n <= 2500)
    return 1;
for(2501 <= n <= 5000)
    return 5000;
return 0
}
