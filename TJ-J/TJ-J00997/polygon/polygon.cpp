#include <iomanip>
#include <iostream>
#include <string>
#include <cstdio>
using namespace std;

int main()
{
	//freopen("seat.in","r",stdin);
	//freopen("seat.out","w",stdout);
	
	int c,r;
	string a[10005];
	cin >> c >> r;
	for(int i = 0;i <= a.size() - 1;i++)
	{
		cin >> a[i];
	}
	
}
