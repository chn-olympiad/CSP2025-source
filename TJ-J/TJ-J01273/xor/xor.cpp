#include<iostream>
#include<cstdio>
int main{
	freopen(const char * __restrict__ _Filename, const char * __restrict__ _Mode,FILE * __restrict__ _File);
	freopen(xor.in);
	int k >= 0;
	if() {
	}
	return 0;
}
