#include <bits/stdc++.h>
int main()
{
	freopen("polygon.in","r",stdin);
	freopen("polygon.out","w",stdout);
	int n;
	cin>>n>>a[n];
	for(int i=0;i<=n;i++){
		int b=a[i];
	}
	if(b+b+b>2*b){
		int ans++;
	}
	fclose(stdin);
	fclose(stdout);
	cout<<ans<<endl;
	return 0;														
}

