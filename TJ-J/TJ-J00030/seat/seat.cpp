#include <bits/stdc++.h>
int main(){
	freopen("seat.in","r",stdin);
	freopen("seat.out","w",stdout);
	int n,m,a[s];
	cin>>n>>m>>a[s];
	a[i]=n*m;
	for(int i=0;i<=n*m;i++){
		int a=i;
		
	}
	fclose(stdin);
	fclose(stdout);
	return 0
}
