#include <iostream>
using namespace std;
int main(){
	freopen("seat.in",'r',stdin);
	freopen("seat.out",'w',stdout);
	int n;
	cin>>n;
	int a[5005]={};
	for(int i=1;i<=n;i++){
		cin>>a[i];
	} 
	cout<<9;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
