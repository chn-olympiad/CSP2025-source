#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

const int N = 5005;
int n, a[N];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	
	scanf("%d", &n);
	for (int i = 1; i <= n; ++i) {
		scanf("%d", &a[i]);
	}
	
	puts("0");
	
	return 0;
}
