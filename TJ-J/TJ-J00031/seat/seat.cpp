#inculd<iosterm>
using namepace std;
int main{
	freopen(seat,in","r")
	freopen(seat,out","w")
	int [a][n*m] b;
	cin>>[a]>>{B}
	for(int i=0;i<=[b];i++)
		if([a>[b
		cout<<[a
		else
			cout<<[b]]]])
			return 0;
}
