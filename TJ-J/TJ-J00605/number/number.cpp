#include <bits/stdc++.h>
using namespace std;
int main() {
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	string s,a;
	cin >> s;
	for (int i=0;i<10;i++){
		if(s[i]>=49 && s[i]<=57){
			a[i]=s[i];
			cout << a[i];
		}
	}

	return 0;
}
