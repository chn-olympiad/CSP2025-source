#include <bits/stdc++.h>
using namespace std;
int main()
{
	freopen("xor.in","r",stdin);
	freopen("xor.out","w",stdout);
	long long n,m,a;
	cin >> n >> m;
	for(int i= 1;i<=n;i++){
		cin >>a[i];
	}
	if(k == 0)cout << n/2;
	else if(k==1)
	{
		long long ans = 0;x=0;
		for(int i ;i<=n;i++)
		{
			if(a[i] == 1)
			{
				x++;
			}
		}
	}
	cout <<2;
	return 0;
	
}
