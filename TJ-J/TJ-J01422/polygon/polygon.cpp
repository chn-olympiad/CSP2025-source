#include<bits/stdc++.h>
using namespace std;
int main(){
	fropen("polygon.in","r",stdin);
	fropen("polygon.out","r",stdout);
	int n;
	cin>>n;
	int m[n];
	for(int i=0;i<n;i++){
		cin>>m[i];
	}
	cout<<9;
	fclose(stdin)
	fclose(stdout)
	return 0;
	}
