#include<bits/stdc++.h>
using namespace std;
int main(){
	fopen("xor.in","r",stdin);
	fopen("xor.out","r",stdout);
	int n,m;
	cin>>n>>m;
	int a[n];
	for(int i=0;i<n;i++){
		cin>>a[i];
	}
	cout<<2;
	fclose(stdin)
	fclose(stdout)
	return 0;
	}
