#include <iostream>
#include <algorithm>
#include <cmath>
using namespace std;

int a[505000], x[505000];

int main()
{
	int n, k;
	cin >> n >> k;
	for (int i = 1; i <= n; i++)
	{
		cin >> a[i];
	}
	cout << n / 2;
}
