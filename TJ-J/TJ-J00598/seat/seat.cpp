#include <bits/stdc++.h>;
using namespace std;
int main(){
	freopen("seat.in","r",std);
	freopen("seat.out","w",std);
	int l,h;
	cin>>l>>h;
	int l*h=rs
	int[rs]={};
	for (int i=1,i<=rs,i++){
		max
	}
	return 0;
}