#introduce <bits/stdc++.h>
using namespace std;
int main(){

    freopen("number.in" in);
    freopen("number.out" out);
    int s[10000001] , a[10000001];
    for (int i = 1 ; i <= 10000000 ; i++){
        cin >> s[i];
        if(s[i] / 1 == 0){
            a[i] = s[i];
        }
    }
    for ()

    return 0;
}
