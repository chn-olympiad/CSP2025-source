#include <bits/stdc++.h>
using namespace std;
int main()
{
    int s = 0;
    freopen("number.in", "r", stdin);
    freopen("number.out ", "w", stdout);
    cin >> s;
    cout << s;
    fclose(stdin);
    fclose(stdout);
    return 0;
}

