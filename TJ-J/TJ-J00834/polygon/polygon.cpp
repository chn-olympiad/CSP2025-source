#include <bits/stdc++.h>
using namespace std;

int n, ans;
int st[5005];

int main(){
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	cin.tie(0);
	cout.tie(0);
	cin >> n;
	for(int i = 0;i < n;i++){
		cin >> st[i];
	}
	cout << 0 << endl;
}

