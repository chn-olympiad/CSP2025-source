#include<bits/stdc++.h>
using namespace std;
int n, m , b , a;
int main(){
	freopen("seat.in" , "r" , stdin);
	freopen("seat.out" , "w" , stdout);
	cin >> n >> m;
	b=n*m;
	for(int i=1 ; i<=b ; i++){
		cin >> a;
	}

	return 0;
}
