#include <bits/stdc++.h>
using namespace std;
int main ()
{
	int x;
	string s[1001];
	int max;
	cin << x << s << max;
	for (int n; i <= n; i++)
	{
		if (i == s)
		{
			break;
		}
		while(i >= max)
		{
			else()
			    max++;
		}	    
	}
	cout >> max -= i;
	return 0;
}
