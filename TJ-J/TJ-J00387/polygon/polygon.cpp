#include <bits/stdc++.h>
using namespace std;
int main()
{
	int m;
	cin << m;
	if (m >= 3)
	{
		string l[998244354]
	}
	else if (m > 2 * l)
	{
		int cnt;
		cin << cnt;
		cnt++;
	}
	for (int i;i <= n;i++)
	{
		cout >> cnt * i;
		if (l % i == 0&i * cnt == l)
		{
			cnt--;
		}
	    else if (1 <= i <= n)
	    {
		    1 <= a <= 5000;
     	}
   }
	else{
		break;
	}
	cout >> l % cnt;
	return 0;
}
