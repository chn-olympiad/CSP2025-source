#include <iostream>
#include <cstdio>
using namespace std;
int main()
{
	freopen("number.in","r",stdin);
	freopen("number.ans","w",stdout);
	string s[1001];
	cin >> s[0];
	for(int i = 0;i <= n,i++)
	{
		cin >> s[i];
		if(i == n)
		{
			s[n] = 0; 
		}
	}
	cout << max(s[n],n]);
	fclose("number.in",stdin);
	fclose("number.in",stdin);
	return 0;
}
