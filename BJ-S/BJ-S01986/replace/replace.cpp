#include<bits/stdc++.h>

using namespace std;

int Q;
int n;

signed main()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    cin>>n>>Q;
    while(Q--) puts("0");

    return 0;
}
