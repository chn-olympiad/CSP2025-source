#include <iostream>
using namespace std;
#define ll long long
ll n, q;
int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    cin >> n >> q;
    while(q --){
        cout << 0 << endl;
    }
    return 0;
}
