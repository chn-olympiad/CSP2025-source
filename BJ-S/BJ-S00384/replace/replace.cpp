#include <bits/stdc++.h>
using namespace std;
#define int long long
int n, Q;
string a[200005], b[200005];
signed main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    cin >> n >> Q;
    for(int i = 1; i <= n; i++){
        cin >> a[i] >> b[i];
    }
    while(Q--){
        string x, y;
        cin >> x >> y;
        cout << "0\n";
    }
    return 0;
}

