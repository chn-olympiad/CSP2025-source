#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    std::ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n,q;
    string s;
    for(int i=1;i<=n;i++){
        cin>>s;
        cin>>s;
    }
    for(int i=1;i<=q;i++){
        cin>>s;
        cin>>s;
        cout<<0<<endl;
    }
}
