#include<bits/stdc++.h>
using namespace std;
long long a,b,c,p;
int main(){
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    std::ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>a>>b>>c;
    for(int i=1;i<=a;i++){
        cin>>p;
        cin>>p;
        cin>>p;
    }
    for(int i=1;i<=c;i++){
        for(int j=1;j<=a;j++){
            cin>>p;
        }

    }
    cout<<0;
}
