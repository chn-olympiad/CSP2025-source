#include <bits/stdc++.h>
using namespace std;
int n, x, m;
int main()
{
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    cin >> n >> m;
    for(int i = 0;i < n * 3 + m;i ++) cin >> x;
    cout << 1346507;
    return 0;
}
