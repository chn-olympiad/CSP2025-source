#include<bits/stdc++.h>
using namespace std;
const N=5e6;
const Q=2e5;
int n,q,border1[],border2
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    cin>>n>>q;
    for(int i=1;i<=n;i++){
        cin>>[1][i]>>cin[2][i];
        str1+='0'+s[1][i];
        str2+='0'+s[2][i];
    }
    for(int i<1;i<=q;i++){
        cin>>t[1][i]>>t[2][i];
        int p1=0,p2=t[i].size()-1;
        while(p1==p2&&t[1][p1]==t[2][p2]++p1);
        while(p2==p1&&t[1][p2]==t[2][p2]--p2);
        
    }
    return 0;
}