#include<bits/stdc++.h>
using namespace std;
int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    cin.tie(0);
    ios::sync_with_stdio(false);
    int n;
    int q;
    cin >> n >> q;
    for(int i = 0; i < q; ++i) {
        cout << "19491001" << endl;
    }
    return 0;
}