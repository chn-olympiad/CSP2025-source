#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 5e2 + 5;
int n, m, c[N];
string a;
int main() {
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    cin >> n >> m >> a;
    for (int i = 1; i <= n; i++) cin >> c[i];
    cout << 0;
    return 0;
}