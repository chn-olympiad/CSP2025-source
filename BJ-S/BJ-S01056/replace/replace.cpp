#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 1e5 + 5;
int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    string a;
    cin >> n >> q;
    while (n--) cin >> a >> a;
    while (q--) {
        cin >> a >> a;
        cout << "0\n";
    }
    return 0;
}