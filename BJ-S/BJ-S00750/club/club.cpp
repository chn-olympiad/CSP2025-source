#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("club.in", "r", stdin);
    freopen("club.out", "W", stdout);
    int t;
    cin >> t;
    while (t--){
        ;
    }
    return 0;
}
