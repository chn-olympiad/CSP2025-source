#include <bits/stdc++.h>
#define ll long long
using namespace std;

const int maxn = 2e5 + 50;



int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    cin >> n >> q;
    while (q--) cout << 0 << endl;
    return 0;
}