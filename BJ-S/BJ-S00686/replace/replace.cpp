#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define DBG

int main()
{
    #ifdef DBG
        freopen("replace.in", "r", stdin);
        freopen("replace.out", "w", stdout);
    #endif

    ll llN = 0;
    ll llQ = 0;

    cin >> llN >> llQ;

    for (ll i = 1; i <= llQ; i++)
    {
        cout << "0" <<endl;
    }

    return 0;
}
