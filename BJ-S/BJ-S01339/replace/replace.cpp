#include <bits/stdc++.h>
using namespace std;
string s1[100005], s2[100005];
int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < n; i++)
        cin >> s1[i] >> s2[i];
    for (int Q = 0; Q < q; Q++){
        string t1, t2;
        cin >> t1 >> t2;
        cout << 0 << endl;
    }
    return 0;
}
