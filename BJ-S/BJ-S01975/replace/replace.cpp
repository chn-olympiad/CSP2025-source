#include<bits/stdc++.h>
using namespace std;
#define int long long
const int N=210;
string s;
int q,n;
signed main()
{
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin>>n;
    cin>>q;
    for(int i=1;i<=n;i++){
        cin>>s;
        cin>>s;
    }
    while(q--)
    {
        cin>>s;
        cin>>s;
        cout<<0<<'\n';
    }
    return 0;
}
