#include<bits/stdc++.h>
using namespace std;

int n, m, k, m1, m2, m3, k1, k2, k3, k4, k5;
int main()
{
    int n, q;
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    cin >> n >> m >> k;
    for(int i = 1; i <= m; i++)
        cin >> m1 >> m2 >> m3;
    for(int i = 1; i <= k; i++)
        cin >> k1 >> k2 >> k3 >> k4 >> k5;
    cout << 13;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
