#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll N=2*1e5+10;
ll a[N][10],b[N][10];
string ax[N],bx[N];
ll n,q;
string s1[N],s2[N];
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    cin>>n>>q;
    for(ll i=1;i<=n;i++){
        ll cnt=0;
        cin>>s1[i]>>s2[i];
        cout<<s1[i]<<" "<<s2[i]<<endl;
        for(ll j=0;j<s1[i].size();i++){
            if(s1[j]!=s2[j]){

            }
        }
    }
    for(ll i=1;i<=q;i++){
        cin>>ax[i]>>bx[i];
        cout<<0<<endl;
    }
    return 0;
}
