#include <bits/stdc++.h>
#include <cstdio>
using namespace std;
freopen(replace.in,r,stdin);
freopen(replace.out,w,stdin);
int main(){
    int n,q;
    cin>>n>>q;
    return 0;
}
fclose(replace.in,r,stdout);
fclose(replace.out,w,stdout);