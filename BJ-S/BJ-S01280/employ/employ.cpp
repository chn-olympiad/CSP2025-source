#include <iostream>
#include <string>

using namespace std;

int interview(string hard, int* pate, int index, int pnum, int lost){
    if (index == pnum-1){
        if (pate[index] > lost){
            
        }
    }
}

int main()
{
    int n, m;
    string s;
    cin >> n >> m >> s;
    int patience[n];
    for (int i = 0;i<n;i++){
        cin >> patience[i];
    }
    return 0;
}
