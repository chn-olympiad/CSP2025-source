#include <bits/stdc++.h>
using namespace std;

int like[1000005];
char s[1000005];

int main(){
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    int m,n;
    cin >> n >> m;
    for(int i = 1; i <= n; i++){
        cin >> like[i];
    }
    for(int i = 1; i <= m; i++){
        cin >> s[i];
    }
    cout << 22;
    return 0;
}