#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m, tmp;
    cin >> n >> m;
    string s;
    cin >> s;
    for(int i = 0; i < n; i++){
        cin >> tmp;
    }
    cout << "2";
    return 0;
}
