#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    int n, m, k;
    cin >> n >> m >> k;
    for (int i = 1; i <= m; i++) {
        int x1, x3, x2;
        cin >> x1 >> x2 >> x3;
    }
    for (int i = 1; i <= k; i++) {
        int x;
        cin >> x;
        for (int i = 1; i <= m; i++) {
            cin >> x;
        }
    }
    cout << 0;
    return 0;
}
