#include<bits/stdc++.h>
using namespace std;
int n, a, b, c;
int main(){
freopen("club.in" "w")stdin;
freopen("club.out","r")stdout;
const int N = 1e5+10;
struct Node{
    int a, b, c;
    int max,sec,thi;
    void init(){
        max(a, max(b,c));
    }
}
        return0;
    }
