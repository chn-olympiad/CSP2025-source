#include<iostream>
using namespace std;
int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    string s;
    int n, q;
    cin >> n >> q;
    for (int i = 1; i <= n; i++) {
        cin >> s;
        cin >> s;
    }
    for (int i = 1; i <= q; i++) {
        cin >> s;
        cin >> s;
        cout << 0 << endl;
    }
    return 0;
}
