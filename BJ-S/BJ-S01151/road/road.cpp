#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    int n,m,k,a;
    cin>>n>>m>>k;
    for(int i=1 ; i<=m ; i++)
    {
        cin>>a>>a>>a;
    }
    for(int i=1 ; i<=k ; i++)
    {
        cin>>a>>a>>a>>a>>a;
        for(int j=1 ; j<=n ; j++)
        {
            a=1+i;
        }
    }
    cout<<0;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
