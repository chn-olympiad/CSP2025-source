#include<bits/stdc++.h>
using namespace std;
int s[510];
int main(){
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, k;
    cin >> n >> k;
    string a;
    cin >> a;
    for(int i = 1; i <= n; i++){
        cin >> s[i];
    }
    cout << 0;
    return 0;
}
