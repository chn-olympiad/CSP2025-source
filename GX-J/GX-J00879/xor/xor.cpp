#include<iostream>
#include<algorithm>
using namespace std;
int main()
{
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    long long n,k;cin >> n >> k;
    long long a[n + 5];
    for (int i = 0;i < n;i++)
        cin >> a[i];
    cout << 2;
    return 0;
}
