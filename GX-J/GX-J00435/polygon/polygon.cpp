#include <iostream>
#include <fstream>

using namespace std;
/*
int n, a[5001];

void dfs (int dep, int sta ,int sum)
{
    for (int i = sta; i < n; i++)
    {

    }
}
*/
int main()
{
    freopen("polygon.in", "r", stdin);
    freopen("polygon.out", "w", stdout);
    /*
    cin >> n;
    for (int i = 0; i < n; i++)
        cin >> a[i];
    dfs(0, 0, 0);
    */
    cout << 0;
    return 0;
}
