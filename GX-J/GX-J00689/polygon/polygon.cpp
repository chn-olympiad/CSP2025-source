#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("polygon.in", "r", stdin);
    freopen("polygon.out", "w", stdout);
    int n;
    cin >> n;
    int num;
    for (int i=0; i<n; i++){
        cin >> num;
    }
    cout << n+8 << endl;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
