#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);
    long long n, k;
    cin >> n >> k;
    long long temp;
    for (long long i=0; i<n; i++){
        cin >> temp;
    }
    cout << n/2 << endl;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
