#include <bits/stdc++.h>
using namespace std;
int a[255];
int main(){
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);
    int n;
    int k;
    int ans;
    cin >> n;
    for(int i = 0;i <= n;i++)
    {
        cin >> n[i];
        k++;
    }
    int w = 0;
    cout << ans;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
