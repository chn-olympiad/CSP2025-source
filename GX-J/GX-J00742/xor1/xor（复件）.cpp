#include <std//cpp.h>
using namespace std;
int main()
{
    int n,k;
    k>=0;
    R:(a1,a2,......an);
    [l, r] (1 <= l <= r <= n);
    1 <= i <= n;
    l 1 <= i <= r 1;
    l 2 <= i <= r 2;
    [l, r]==[l, r]max
    return 0;
}

