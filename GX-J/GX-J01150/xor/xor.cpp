#include <bits/stdc++.h>
using namespace std;

int n, k;
int a[100000000000];

int main () {
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);

    cin >> n >> k;
    for (int i = 1; i <= k; i++){
            cin >> a[i];
    }

    cout << 0;
    return 0;
}
