#include <iostream>
using namespace std;
int main()
{
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
    int n, k, a, r, l;
    cin >> n >> k;
    cin >> a;
    for (int i = 1; i <= n; i++)
    {
        if ((l1 <= i <= r1) && (l2 <= i <= r2))
        {

        }
    }
    cout << n << endl;
    return 0;
}
