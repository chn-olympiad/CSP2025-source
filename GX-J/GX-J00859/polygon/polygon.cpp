#include <iostream>
using namespace std;
int main()
{
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n, a, l, ans, max;
    cin >> n;
    cin >> a[i];
    for (int i = 1; i <= n; i++)
    {
        if ((l >= 3) && (ans > 2*max))
        {

        }
    }
    cout << n;
    return 0;
}
