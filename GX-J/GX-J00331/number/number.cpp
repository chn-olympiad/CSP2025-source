#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout);
    string s,a="";
    cin>>s;
    for(int i=0;i<len(s);i++){
        if(s[i] in "0123456789"){
            a+=s[i];
        }
    }
    a.sort[a]
    return 0;
}