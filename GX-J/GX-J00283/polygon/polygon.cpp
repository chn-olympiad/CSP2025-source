#include<bits/stdc++.h>
using namespace std;

char n[5555];
char a[n];

int main()
{
    freopen("polygon.in", "r", stdin);
    freopen("polygon.out", "r", stdout);
    cin >> n;
    cin >> a;
    if(n < 3)
    {
        return 0;
    }

    return 0;
}
