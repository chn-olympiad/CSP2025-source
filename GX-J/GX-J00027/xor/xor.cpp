#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
    /*int n,k,a[1e7];
    cin >> n >> k;
    for(int i = 1;i <= n;i++){
        cin >> a[i];
    }
    for(int len = 1;len <= n;len++){
        for(int left = 1;left+len-1 <= n;left++){
            int xorsum = x(left,left+len-1);
            if(xorsum == k)
        }
    }*/
    cout << 2;
    return 0;
}
