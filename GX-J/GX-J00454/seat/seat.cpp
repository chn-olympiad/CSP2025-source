#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    int n,m,q;
    cin>>n>>m;
    q=n*m;
    int s[n][m];
    for (int j=0;j<n;j++){
        for (int i=0;i<m;j++){
        cin>>s[j][i];
    }

    }
    fclose(stdin);
    fclose(stdout);

    return 0;
}
