#include <iostream>
using namespace std;
int main(){
    freopen("number.in"."in".stdin);
    freopen("number.out"."out".stdout);
    char s;
    int n;
    cin >> s >> n;
    wrile(s<=s[9]){
        if(s=n) cout << n;
    }
    return 0;
}
