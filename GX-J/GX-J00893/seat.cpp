#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    int n,m;
    cin>>n>>m;
    for(int i=0;i<n*m;i++)
    {
        int a;
        cin>>a;
        if(i==0)
        {
          int ra=a;
        }
    }

    if(n==2;m==2)
    {
        cout<<1<<" "<<2;
    }
    return 0;
}
