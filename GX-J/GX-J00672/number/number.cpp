#
s << cin;
if ("1234567890" in s)
    {
    s.split(" ")
    while True:
    if (s[0] < s[1])
        {
        s[0] = s[1];
        s[1] = s[0];
    else if (s[1] < s[2])
        {
        s[2] = s[1];
        s[1] = s[2];
        }
    else if (s[2] < s[3])
        {
        s[3] = s[2];
        s[2] = s[3];
        }
    else if (s[3] < s[4])
        {
        s[4] = s[3];
        s[3] = s[4];
        }
    else (s[0] > s[1] > s[2] > s[3] > s[4])
        break

else:
        return 0
        break

        }
