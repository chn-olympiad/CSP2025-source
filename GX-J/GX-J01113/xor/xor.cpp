#include<iostream>
using namespace std;

freopen("xor.in", 'r', stdin);
freopen("xor.out", 'w', stdout);

int main()
{
    int n, k, a[500000];
    cin >> n >> k;
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    cout << 2;
    return 0;
}