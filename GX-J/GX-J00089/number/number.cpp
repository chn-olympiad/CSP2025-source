#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout);
    string s;
    cin>>s;
    while(1)
    {
        if(s/10%10>=0||s/10%10<=9)
        {
            sum=
        }
    }
    return 0;
}
