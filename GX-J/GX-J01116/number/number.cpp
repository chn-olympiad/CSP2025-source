#include<bits/stdc++.h>

using namespace std;
freopen("number.in","r",stdin);
freopen("number.out","w",stdout);

int main(){
    cin >> s;
    int s,s[i];
    if ( s[i] <= 9 && s[i] >= 1){
        int n[1] = s[i];
    }
    if (n[x-1] < n[x]){
        n[x-1] = n[x];
        n[x] = n[x-1];
    }
    cout << n;
    
    
    return 0;
}
fclose("number.in",stdin);
fclose("number.out",stdout);

