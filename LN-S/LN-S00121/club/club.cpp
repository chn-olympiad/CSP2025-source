#include<iostream>
using namespace std;
const int N=1e5+10;
int main(){
    freopen("club.in","r",stdin);
    freopen("club.out","w",stdout);
    ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
    int T;
    cin>>t;
    while(T--){
        int n;
        cin>>n;
        int mem[N],enj[5],dp[N][5];
        for(int i=1;i<=n;i++) cin>>mem[i]>>enj[i];
        int Limit=n/2+1;
        dp[1][1]
    }
    return 0;
}
