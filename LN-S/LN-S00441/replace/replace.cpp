#include<bits/stdc++.h>
using namespace std;
signed main()
{
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    int n,q;cin>>n>>q;string s;
    for(int i=1;i<=n;i++)
        cin>>s;
    for(int i=1;i<=q;i++)
        cin>>s;
    cout<<0;
    fclose(stdin);
    fclose(stdout);
}
