#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m, i, d, p;
    int s, a[s];

    d=n-m;
    if(s=1); i+=1;
    if(s=0); i=i;

    cout << p << endl;
    return 0;
    }
