#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("club.in", "r", stdin);
    freopen("club.out", "w", stdout);
    int j, n, a[j][n];
    cin >> a[j][n];
    for(int j=1; j<=3; j++);
    for(int i=1; i<=n; i++);
    int c,i;
    c=fadd(a[j][i], a[j+1][i]); j++;
    max(c, c);
    int d;
    d=fadd(c, a[j+2][i]);
    max(d, d);
    cout << d << endl;
    return 0;
}
