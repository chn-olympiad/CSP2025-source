#include <bits/stdc++.h>
using namespace std;

int main()
{
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	int n,m,c[505];
	string s;
	cin >> n >> m;
	cin >> s;
	for (int i = 1;i <= n;i++) cin >> c[i];
	cout << 0;
	return 0;
}
