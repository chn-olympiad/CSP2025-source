#include <bits/stdc++.h>
using namespace std;

int n, m, k;

int main() {
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    scanf("%d%d%d", &n, &m, &k);
    for (int i = 1; i <= m; i++) {
        int n, v, w;
        scanf("%d%d%d", &n, &v, &w);
    }
    for (int i = 1; i <= k; i++) {
        int o;
        scanf("%d", &o);
        for (int j = 1; j <= n; j++) {
            int u;
            scanf("%d", &u);
        }
    }
    printf("0\n");
    return 0;
}