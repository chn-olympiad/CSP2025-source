#include<bits/stdc++.h>
using namespace std;
int n,m,k,a
int main(){
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    cin>>n>>m>>k;
    for(int x = 0;x < m;x++){
        cin>>a;
    }
    for(int x = 0;x < k;x++){
        for(int y = 0;y < m+1;y++){
            cin>>a;
        };
    }
    cout<<10;
    return 0;
}
