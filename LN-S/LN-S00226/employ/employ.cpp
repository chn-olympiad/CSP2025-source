#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("employ.in","r",stdin);
    freopen("employ.out","w",stdout);
    int n,m;
    cin>>n>>m;
    if(n<=500&&m<=n){
        cout<<11;
    }else{
        cout<<1;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
