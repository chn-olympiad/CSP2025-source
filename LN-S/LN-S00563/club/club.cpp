#include <bits/stdc++.h>
using namespace std;
int n,m, a[100005];
int main(){
    freopen("club.in","r",stdin);
    freopen("club.out","w",stdout);
    cin >> m>>n;
    for(int i = 1; i<= n; i++)
    {
        for (int j = 1;j<=i;j++)
        {
            cin >> a[i];
        }
    }
}
