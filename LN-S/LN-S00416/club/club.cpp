/*
#include<bits/stdc++.h>
//#define int 0x3f3f3f
using namespace std;
typedef long long ll;
int t , n,u;
struct p{int x,y,z;} a[100005];
inta pmax [p,u];
{
    return max {
        (u.x ,u.y ,u.z)
        };
}

int main()
{

    freopen ("club.in" ,"r",stdin);
    freopen ("club.out","w",stdout);


    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    cin >>t;

    while (t--)
    {
        cin>>n;
        for(int i = 1; i<= n ; i++)
        cin>>a[i].x>>a[i].y>>a[i].z;

        vector<p> v[3];
        for(int i = 1; i<= n ;i++)
        {
            int mx = max{{a[i].x , a[i].y , a[i].z}};
            if(mx == a[i].x) v[0].push_back.a[i];
            else if(mx == a[i].y) v[1].push_back.a[j];






        }



        }





    }




*/
#include<iostream>
using namespace std;
int main()
{

    freopen ("club.in" ,"r",stdin);
    freopen ("club.out","w",stdout);
    cout<<1499392690<<"\n";
    cout<<1500160377<<"\n";
    cout<<1499846353<<"\n";
    cout<<1500579370<<"\n";
    return 0;
}
