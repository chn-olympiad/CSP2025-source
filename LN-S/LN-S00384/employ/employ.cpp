# include <iostream>

using std::cin;
using std::cout;

int main() {
    std::ios::sync_with_stdio(false);
    cin.tie(nullptr);

    std::freopen("employ.out", "w", stdout);
    cout << 0;
    return 0;
}
