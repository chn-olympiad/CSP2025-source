#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n,q;
    string a, b,t,m;
    for(int i = 1; i <= n; i++)
    {
        cin >>  a, b;
    }for(int i = 1; i <= q; i++){
        cin >> t >> m;
        cout << 0;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
