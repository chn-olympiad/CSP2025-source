#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    int m,n;
    string a[10000];
    string b[10000];
    cin>>m>>n;
    while(n > 0){
        n--;
        for(int i = 0;i<=m;i++){
            cin>>a[i]>>b[i];
        }
        cout<<0<<endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
