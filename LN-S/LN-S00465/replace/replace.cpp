#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N = 2e3 + 10;
const int K = 1e4 + 10;
const int M = 30;
int n, q;
string s[N], t[N], a[N], b[N];
int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    scanf("%d%d", &n, &q);
    for (int i = 1; i <= n; i++)
        cin >> s[i] >> t[i];
    while (q--) {
        cin >> a[i] >> b[i];
        puts("0");
    }
}
