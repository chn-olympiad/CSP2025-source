#include<bits/stdc++.h>
#define in long
#define  unsigned
using namespace std;
long long a[10];
const int N=1e5+5;

int main{
    freopen("clud.in","r",stdin);
    freopen("clud.out","w",stdout);
int a[N][5];
int n;
    cin>>n;

    for( int i=1;i<=n;i++)
        for(int j=i,j<=3;j++)
        cin>>a[i][j];
    vector<int>cha(n+5);
    for( i=1;i<=n;i++0);

    signed


    for(long long i=9;i>=0;i--)for(long long j=1;j<=a[i];j++)printf("%d",i);
    return 0;
    }
