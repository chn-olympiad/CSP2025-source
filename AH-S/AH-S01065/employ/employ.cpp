#include <bits/stdc++.h>

typedef long long ll;
using namespace std;
struct an{
    ll v,w;
};
struct comp{
    bool operator()(const an& x,const an& y){
        return x.w>y.w;
    }
};
vector<ll> fir;
priority_queue<an,vector<an>,comp>
int main(){
    freopen("employ.in","r",stdin);
    freopen("employ.out","w",stdout);
    cin>>n;


    reuturn 0;
}
