#include<bits/stdc++.h>
using namespace std;
const int mod=998244353;
int main()
{
   freopen("employ.in","r",stdin);
   freopen("employ.out","w",stdout);
   int n,m,wait[505];
   unsigned long long ans=0;
   string hard;
   cin>>n>>m;
   cin>>hard;
   for(int i=0;i<n;i++){
       cin>>wait[i];
   }
   sort(wait,wait+n);
   for(int i=0;i<n;i++){
       if()
   }
   cout<<ans%mod;

return 0;
}

