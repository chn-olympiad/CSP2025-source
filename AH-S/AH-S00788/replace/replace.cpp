#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("replace.in", "r", stdin);
    freopen("repalce.out", "w", stdout);
    int n, q;
    cin >> n >> q;
    for (int i=1;i<=q;i++)
        cout << 0 << endl;
}
