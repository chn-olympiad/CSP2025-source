#include <bits/stdc++.h>
using namespace std;
const int N = 2 * 1e5;
string s[N], t[N];

int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);

    int n, q;
    cin >> n >> q;
    for (int i = 0; i < n; i++) {
        cin >> s[i];
    }

    while (q--) {
        cin >> t[i];
        string x, y, z;

    }

    fclose(stdin);
    fclose(stdout);
    return 0;
}
