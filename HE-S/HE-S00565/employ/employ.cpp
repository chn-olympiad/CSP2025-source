#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int n, m, c[501];

int main()
{
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	string s;
	cin >> s;
	for (int i = 0; i < n; i++)
	{
		cin >> c[i];
	}
	cout << 0;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
