#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int n, q;

int main()
{
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 0; i < n; i++)
	{
		string s1, s2;
		cin >> s1 >> s2;
	}
	for (int i = 0; i < q; i++)
	{
		string s1, s2;
		cin >> s1 >> s2;
	}
	cout << 0;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
