#include <iostream>
#include <cstring>
using namespace std;
int n,m,sum,c[510],q,s;
string s;
int main() {
    freopen("employ.in","r",stdin);
    freopen("employ.ans","w",stdout);
    cin >> n >> m;
    cin >> s;
    for(int i = 1;i <= n;i++) {
        cin >> c[i];
    }
    for(int i = 1;i <= n;i++) {
        t *= i;
    }
    for(int i =1;i <= n;i++){
        for(int j =1;j <= n-i;i++){

        }

    }
    if(s[i] == '1') {
        sum++;
    }
    if(sum >= m) {
        q++;
    }
    return 0;
}
