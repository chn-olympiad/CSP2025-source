#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("employ.in","w",stdin);
	freopen("employ.out","r",stdout);
	int n,m;
	cin >> n >> m;
	int a[n];
	for(int i=0;i<n;i++){
		cin >> a[i];
	}
	int c[n];
	for(int i=0;i<n;i++){
		cin >> c[i];
	}
	cout << m;
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
