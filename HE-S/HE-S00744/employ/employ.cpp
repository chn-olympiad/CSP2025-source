#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
    int n,m,x,a[10002];
    cin>>n>>m;
    cin>>x;
    cin>>a[x];
    if(m=1)
    cout<<1;
    if(n=m)
    cout<<n*n;
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
