#include <bits/stdc++.h>
using namespace std;
int n, m;
int main()
{
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	int arr[n];
	string s;
	cin >> s;
	for(int i = 1; i <= n; i++) {
		cin >> arr[i];
	}
	cout << "1";
	return 0;
}
