#include <bits/stdc++.h>
using namespace std;

struct __pair{
	string x,y;
};
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int n,q;
	cin >> n >> q;
	__pair p[n];
	for(int i=0;i<n;i++) cin >> p[i].x >> p[i].y;
	while(q--){
		string a,b;
		cin >> a >> b;
		cout << 0 << endl;
	}
																																																																																																																																																																										
	return 0;
}
