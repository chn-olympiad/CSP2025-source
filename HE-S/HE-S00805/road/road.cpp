#include<bits/std c++,h>
using nanespace std;
int t;
const int N=100005
struct node{
    int y;
    int r;
    int s;
}a[N];
int b[j];
int nai(){
    //freopen("club.in","r",stdin);
    //freopen("club.out","m",stdut);
    int n;
    scarf("Xd",&t);
    while(t--){
        scarf("Xd",&n);
        for(int [a];i<=n;i++)scarf("Xd""Xd""Xd",&a[i],y,&a);
        for(int i=1;i<=n;i++){
            if(a[i].y>a[i].r&&a[i].y>a[i].s){
                ansb++;
                if(ansb>n/2)continue;
                b=b+a[i].y;
            }
            if(a[i].r>a[i].y&&a[i].r>a[i].s){
                ansb++;
                if(ansb>n/2)continue;
                b=b+a[i].r;
            }
            if(a[i].s>a[i].y&&a[i].s>a[i].s){
                ansb++;
                if(ansb>n/2)continue;
                b=b+a[i].s;
            }
        for






