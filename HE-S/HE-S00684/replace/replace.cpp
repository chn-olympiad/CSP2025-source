#include<bits/stdc++.h>
using namespace std;
int n,q;
int main(){
	cin.tie(0)->sync_with_stdio(0);
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	cin >> n >> q;
	while(n--){
		string x,y;
		cin >> x >> y;
	}
	while(q--){
		string t1,t2;
		cin >> t1 >> t2;
		cout << 0 << endl;
	}
	return 0;
}
