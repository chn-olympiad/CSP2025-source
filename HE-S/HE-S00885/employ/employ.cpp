#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("employ.in","r",stdin);
    freopen("employ.out","w",stdout);
    int n,m,a[510];
    string st;
    cin>>n>>m;
    cin>>st;
    for(int i=0;i<n;i++){
        if(a[i]!=0) cin>>a[i];
    }
    cout<<2;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
