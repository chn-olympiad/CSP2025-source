#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("club.in", "r", std.in);
	freopen("club.out", "w", std.out);
	int a[1000];
	int b[1000];
	int t, n;
	cin >> t >>endl;
	cin >> n >> endl;
	for (int i = 0;i<t;i++){
		cin >> a[i];
		for (int j = 0;i < n;i++){
			cin >> b[j];
			if a[i] > b[j]{
			cout << a[i];
			}
		}
	}


	return 0;
}
