#include<bits/std++.h>
using namespace std;

int c[105];

int main(){
	freeopen ("employ.in","r" stdin);
	freeopen ("employ.out","w" stdout);
	int n, m;
	cin >> n >> m;
	int s;
	cin >> s;
	for (int i = 0; i < n; i++){
		cin >> c[i];
	}
	for(int t = 0)
	return 0;
}
