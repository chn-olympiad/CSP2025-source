#include<bits/stdc++.h>
using namespace std ;
int main(){
    freopen("road.in","r",stdin) ;
    freopen("road.out","w",stdout) ;
    int n , m , k , z , x , i , a , s , d , f , g ;
    for ( int q = 1 ; q <= m ; q++ ) {
        cin >> z >> x >> i ;
    }
    for ( int q = 1 ; q <= k ; q++ ) {
        cin >> a >> s >> d >> f >> g ;
    }
    for



    return 0 ;
}
