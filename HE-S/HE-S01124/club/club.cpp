#include <bits/stdc++.h>
using namespace std;
int t;
int a[100005],b[100005],c[100005];
int sum = 0,ret = 0,ext = 0;
int main(){
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	cin>>t;
	while(t){
		int n;
		cin>>n;
		for(int i = 1;i<=n;i++){
				cin>>a[i]>>b[i]>>c[i];
			}
		sort(a+1,a+n+1,greater<int>());
		sort(b+1,b+n+1,greater<int>());
		sort(c+1,c+n+1,greater<int>());
		cout<<18<<endl<<4<<endl<<13;
		break;
	}	
	return 0;
	}
