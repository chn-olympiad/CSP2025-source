#include <iostream>
#include <vector>
using namespace std;
vector<int> road_(int n, int m, int k)
{
    int u, v, w;
    vector<int> a;
    for(int i = 0 ; i < n; i++)
    {
        a.push_back(u);
        a.push_back(v);
        a.push_back(w);
    }

}
int main()
{
    return 0;
}
