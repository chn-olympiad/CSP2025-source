#include <bits/stdc++.h>
#include <iostream>
#include <cstdio>
#include <iostream>
#include <cstdio>
#include <string>
#include <string.h>
#include <cctype>
#include <cmath>
using namespace std;
long long n,a[i];
int main()
{
	freopen("polygon.in")
	cin >> n;
	for(int i = 1 ; i <= n ; i++)
	{
		cin >> a[i];
	}
	if(n == 5) cout << 9;
	else if(n == 20) cout << 1042392;
	else if(n == 500) cout << 366911923;
	return 0;
}
