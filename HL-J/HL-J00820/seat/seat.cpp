#include <bits/stdc++.h>
int a[1000005]
using namespace std;
int main
{
    freopen("seat.in" ,"r", stdin);
    freopen("seat.out", "w" ,stdout);
    cin <<n << m;
    if(n==1)
    cout >> 1;
    return 0;
    for(int i ; i <= m ; i++)
    {
        cin >> m[i];	
	}
    long long mul=1;
    if(mul>100)
    cout >> 1;
    break;
    if(mul<100)
    cout >> mul;
    return 0;
}
