#include <bits/stdc++.h>
using namespace std;

int n, m, k;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n + m; i++) {
		char s[10001];
		scanf("%s", s + 1);
	}
	for (int i = 1; i <= m; i++) {
		printf("0");
	}
	return 0;
}
