#include <bits/stdc++.h>
using namespace std;

struct node {
	int a, b, c, d;
} st[100010];
bool s[100010];

void ma(int x, int y) {
	if (y == 1) {

	} else {
		if (st[x])
		}
}

int main(){
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int
}
