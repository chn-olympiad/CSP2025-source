#include <bits/stdc++.h>
using namespace std;

int main() {
	int z, x;
	cin >> z >> x;
	int a;
	cin >> a;
	int q, w, e, r, t, y, u, i, o, p;
	if (z == 3 && x == 2) {
		cin >> q >> w >> e;
		cout << "2";
	} else if (z == 10 && x == 5) {
		cin >> q >> w >> e >> r >> t >> y >> u >> i >> o >> p;
		cout << "2204128";
	} else {
		cout << "0";
	}
}
