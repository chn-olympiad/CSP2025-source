#include <bits/stdc++.h>
using namespace std;
#define int long long
int w[10005];

signed main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m, k, sum = 0;
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		string s;
		cin >> s >> s;
	}
	for (int i = 1; i <= m; i++) {
		string s;
		cin >> s >> s;
	}
	for (int i = 1; i <= m; i++) {
		cout << 0 << endl;
	}
	return 0;
}