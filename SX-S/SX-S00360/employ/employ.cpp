#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m, c[100010];
	cin >> n >> m;
	string st;
	cin >> st;
	for (int i = 1; i <= n; ++i) {
		cin >> c[i];
	}
	cout << "2" << "\n";
	return 0;
}