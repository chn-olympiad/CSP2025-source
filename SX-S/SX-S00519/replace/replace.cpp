#include <bits/stdc++.h>
using namespace std;
long long num, q;
string s, s1;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> num >> q;
	for (int i = 1; i <= num + q; i++) {
		cin >> s >> s1;
	}
	for (int i = 1; i <= q; i++) {
		cout << 0 << "\n";
	}
	return 0;
}
