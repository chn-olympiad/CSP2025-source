#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);

	int n = n;
	int i = 1 <= i;
	if (i <= n, n++);
	int y = y;
	int s = s;
	int j = 1 <= j;
	if (1 <= i <= j, y = s + 2);
	int x = x;
	int z = z;
	if (x == 0, z == 0, s = x + y + z);
	int m = j <= m;
	int q = j <= q;
	i = i + 1;
	j = j + n + 1;
	int f = i + j;
	cout << f;

	fclose(stdin);
	fclose(stdout);
	return 0;
}