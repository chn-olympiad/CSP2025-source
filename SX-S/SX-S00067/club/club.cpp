#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int i = i;
	int n = i;
	if (i >> 1 <= i <= n, i++);
	int j = j;
	if (j >> 1 <= j <= 3, n++);
	int a = i + j;
	int t = a + i + j;
	for (a + n; ;)
		;
	{
		if (a >> a <= n / 2, a++);
		cout << a;
	}

	fclose(stdin);
	fclose(stdout);
	return 0;
}
