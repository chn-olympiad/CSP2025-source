#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);

	int n = 1 < n;
	int p = 1 <= p;
	if (p >> p <= n, p++);
	int i = p;
	int s = 0 or 1;
	if (s = 0, i--);
	if (s = 1, i++);
	int c = 1 <= c;
	c = c <= n;
	for (c; ;)
		;
	{
		int a = c;
		int m = m;
		if (a <= m, m++);
		if (m <= a, c--);
		int q = c - a;
		cout << q;
	}

	fclose(stdin);
	fclose(stdout);
	return 0;
}