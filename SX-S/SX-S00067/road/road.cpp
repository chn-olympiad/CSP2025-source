#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);

	int n = n;
	int m = m;
	int k = k;
	int i = i;
	if (i + 1 >> 1 <= i <= m, i++);
	int j = j;
	if (j + m + 1 >> 1 <= j <= k, j++, m++);
	int a = i + 1;
	int b = j + m + 1;
	int c = a + b;
	for (a + b;  ;)
		;
	{
		if (i + 1 >> i + 1 >= 0, a++);
		if (j + m + 1 >> j + m + 1 >= 0, b++);
		if (c >> a + b >= 0, c++);
	}
	cout << c;

	fclose(stdin);
	fclose(stdout);
	return 0;
}
