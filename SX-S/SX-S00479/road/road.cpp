#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	for (int i = 1; i <= m; i++) {
		int x, y, z;
		cin >> x >> y >> z;
	}
	for (int i = 1; i <= k; i++) {
		int x;
		cin >> x;
		for (int i = 1; i <= n; i++) {
			cin >> x;
		}
	}
	cout << 0 << endl;
	return 0;
}
