#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+10;
long long n, m, k;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdin);
	cin >> n >> m;
	cout << m;
	fclose(stdin);
	fclose(stdout);
	return 0;
}