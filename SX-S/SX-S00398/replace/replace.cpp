#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdin);
	cin >> n >> p;
	for (int i = 1; i <= p; i++) {
		cout << 0;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
