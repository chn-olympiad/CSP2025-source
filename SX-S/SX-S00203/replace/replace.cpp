#include <bits/stdc++.h>
using namespace std;
int x, y;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> x >> y;
	for (int i = 1; i <= y; i++) {
		cout << 0 << "\n";
	}
	return 0;
}
