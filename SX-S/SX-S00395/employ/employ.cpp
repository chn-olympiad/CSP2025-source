#include <bits/stdc++.h>
using namespace std;
int n, m, c[505];
char s[505];

int main() {
	freopen("enploy.in", "r", stdin);
	freopen("enploy.out", "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n; i++)
		cin >> s[i];
	for (int i = 1; i <= n; i++)
		cin >> c[i];

	return 0;
}