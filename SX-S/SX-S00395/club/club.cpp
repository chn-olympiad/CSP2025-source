#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+5;
int t, n, a[N][5], o, to, th;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	cin >> t;
	while (t--) {
		cin >> n;
		int max = n / 2;
		for (int i = 1; i <= n; i++) {
			for (int j = 1; j <= 3; j++) {
				cin >> a[i][j];
			}
		}
		int mx = 0;
	}
	return 0;
}