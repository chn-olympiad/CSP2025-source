#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int a, s;
	cin >> a >> s;
	string s1, s2;
	for (int i = 1; i <= a; i++)
		cin >> s1 >> s2;
	for (int i = 1; i <= s; i++) {
		cin >> s1 >> s2;
		cout << 0 << endl;
	}
	return 0;
}
