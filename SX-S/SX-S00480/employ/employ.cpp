#include <bits/stdc++.h>
using namespace std;
int n, m, a[505];
char s;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		cin >> s;
		a[i] = 1;
	}
	for (int i = 1; i <= n; i++) {

	}


	return 0;
}