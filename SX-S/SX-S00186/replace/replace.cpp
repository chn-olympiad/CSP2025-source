#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n, t;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> t;
	for (int i = 1; i <= n; i++) {
		string a, b;
		cin >> a >> b;
	}
	for (int i = 1; i <= t; i++) {
		string a, b;
		cin >> a >> b;
		cout << 0 << endl;
	}
	return 0;
}
