#include <bits/stdc++.h>
using namespace std;
int n, q;
string a, b, x, y;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	while (n--)
		cin >> x >> y;
	while (q--) {
		cin >> a >> b;
		cout << 0 << endl;
	}
	return 0;
}
