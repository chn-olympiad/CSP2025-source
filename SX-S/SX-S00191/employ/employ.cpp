#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int n, m;
string s;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> n >> m >> s;
	for (int i = 1; i <= n; ++i) {
		cin >> m;
	}

	cout << 0 << endl;

	return 0;
}