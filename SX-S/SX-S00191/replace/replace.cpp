#include <bits/stdc++.h>
#define endl "\n"

using namespace std;

int n, q;
string s;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> n >> q;
	for (int i = 1; i <= n + q; ++i) {
		cin >> s >> s;
	}

	for (int i = 1; i <= q; ++i) {
		cout << 0 << endl;
	}

	return 0;
}