#include <bits/stdc++.h>
using namespace std;
int n, m, c;
string s1;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);
	cin >> n >> m;
	cin >> s1;
	for (int i = 1; i <= n; i++) {
		cin >> c;
	}
	cout << 2;
	return 0;
}