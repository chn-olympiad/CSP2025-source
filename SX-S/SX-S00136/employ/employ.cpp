#include <bits/stdc++.h>
using namespace std;
long long n, m, c;
string s;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(), cout.tie();
	cin >> n >> m;
	cin >> s;
	for (int i = 1; i <= n; i++) {
		cin >> c;
	}
	cout << 107;
	return 0;
}
