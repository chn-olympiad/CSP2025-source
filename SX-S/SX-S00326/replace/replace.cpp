#include <bits/stdc++.h>
using namespace std;
long long n, k;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> k;
	while (k--) {
		cout << 0 << endl;
	}
	return 0;
}
