#include <bits/stdc++.h>
#define ll long long
#define st string
using namespace std;
ll n, m;
st a, b;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "r", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n + m; i++) {
		cin >> a >> b;
	}
	for (int i = 1; i <= m; i++) {
		cout << 0 << endl;
	}
	return 0;
}
