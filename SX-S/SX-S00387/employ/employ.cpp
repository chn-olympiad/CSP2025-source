#include <bits/stdc++.h>
using namespace std;
const int N = 505;
int n, m, grade[N];

int main() {
	cin >> n >> m;
	if (m == 1) {
		cin >> n >> m;
		for (int i = 1; i <= n; i++)
			cin >> grade[i];
		for (int i = 1; i <= n; i++) {
			if (!grade[i])
			}
	}
}