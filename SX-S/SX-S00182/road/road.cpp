#include <bits/stdc++.h>
using namespace std;
int n, m, k, u, v, w, l;

int main() {
	cin >> n >> m >> k;
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	for (int i = 1; i <= m; i++) {
		cin >> u >> v >> w;
	}
	for (int i = 1; i <= k; i++) {
		for (int j = 1; j <= n; j++) {
			cin >> l;
		}
	}
	cout << 34;
	return 0;
}

