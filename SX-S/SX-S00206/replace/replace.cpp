#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);

	int n, q;
	cin >> n >> q;

	for (int i = 1; i <= n; i++) {
		string s, s1;
		cin >> s >> s1;
	}
	for (int i = 1; i <= q; i++) {
		string s, s1;
		cin >> s >> s1;
	}
	for (int i = 1; i <= q; i++) {
		cout << "0" << endl;
	}
	return 0;
}
