#include <bits/stdc++.h>
using namespace std;
string s;
long long n, m, a[10000005];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m >> s;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}

	cout << 2;
	return 0;
}
