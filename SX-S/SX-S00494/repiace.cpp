#include <bits/stdc++.h>
using namespace std;
int n, q;
string s1[20005], s2[20005], t1, t2;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 1; i <= n; i++) {
		cin >> s1 >> s2;
	}
	for (int i = 1; i <= q; i++) {
		string r1 = "", r2 = "";
		cin >> t1 >> t2;
		ans[i] = 0
		         continue;
	}
	int len = t1, length(), i = 0;
	for (int j = 0, j < len, j++) {
		if (t1[j] == t2[j]) {
			t1[j] = "";
			t2[j] = "";
		} else
			break
		}
	for (int j = 0; j < len; j++) {
		if (t1[j] != "");
		r1 += t1[j]
		      if (t2[j] != "");
		r2 += t2[j]
	}
	for (int j = len - 1; j < length - 1; j++) {

	}
	return 0;
}
