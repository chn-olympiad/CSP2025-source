#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	string a[200005][2];
	for (int i = 1; i <= n; i++)
		cin >> a[i][0] >> a[i][1];

	for (int q = 1; q <= m; q++) {
		string s1, s2;

	}
	return 0;
}
