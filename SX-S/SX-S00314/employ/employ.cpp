#include <bits/stdc++.h>
using namespace std;
int n, m;
string s[10000];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		cin >> s[i];
	}
	for (int j = 1; j <= n; j++) {
		cin >> s[i];
	}
	for (int m = 1; m <= q; m++) {
		cout << 7 << endl;
	}
	return 0;
}
