#include <bits/stdc++.h>
using namespace std;
int n, q;
string s[10000];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 1; i <= n; i++) {
		cin >> s[1] >> s[2];
	}
	for (int j = 1; j <= q; j++) {
		cin >> s[3] >> s[4];
	}
	for (int m = 1; m <= q; m++) {
		cout << 0 << endl;
	}
	return 0;
}

