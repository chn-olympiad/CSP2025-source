#include <bits/stdc++.h>
using namespace std;
#define ll long long
ll n, m;
char a[501];
ll b[501];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	for (ll i = 1; i <= n; i++) {
		cin >> a[i];
	}
	for (ll i = 1; i <= n; i++) {
		cin >> b[i];
	}
	cout << 0;
	return 0;
}
