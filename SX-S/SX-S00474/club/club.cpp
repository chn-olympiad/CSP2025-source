#include <bits/stdc++.h>
#define ll long long
using namespace std;
const ll N = 1e5+10;
ll t, a[N];

int main() {
	//freopen("club.in","r",stdin);
	//freopen("club.out","w",stdout);
	cin >> t;
	while (t--) {
		ll n;
		cin >> n;
		memset(a, 0, sizeof(a));
	}
	return 0;
}