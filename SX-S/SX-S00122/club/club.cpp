#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int n = n * 2;
	cin >> n;
	cout << n;
	int i, j;
	int a[i][j];
	for (int i = 1; i <= n; i++) {
		if (j = 1) {
			a[i][j] = n + i;
		} else if (j = 2) {
			a[i][j] = n + i;
		} else {
			a[i][j] = n + i;
			cin >> i >> j >> n;
		}
		cout << a[i][j];
	}
	return 0;
}
