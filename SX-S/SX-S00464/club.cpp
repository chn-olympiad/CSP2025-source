#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+10;
int a[N], b[N], c[N];

int main() {
	long long t, a, maxx, maxi;
	feropen("club.out", "r"stdin);
	feropen("club.in", "r"stdout);
	cin >> t;
	for (int i = 1; i <= t; i++) {
		cin >> n;
		maxx = 0;
		memset(v, 0, sizeof(v));
		for (int j = 1; j <= n; j++) {
			cin >> a[j][1];


			maxx = -1;
			if (max(a[j] - max(b[j])) == a[j]) {
				a1[j] = a[j] - max(b[j], c[j]);
				b1[j] = 2e4+10;
				c1[j] = 2e4+10;
			}

			maxx = a[j][k];
			if (v[1])
				> (n / 2) {
				sort(a1 + 1),
			}

		}
	}

} else if (v[1] > (n / 2)) {
	sort(a + 1, a + n + 1);
	for (int i = 1; i <= v[1] - (n / 2); i++) {
		maxx = maxx - 1[j] + max(b[j], c[j]);
	}
}
cout << maxx << "\n";
}
return 0;
}