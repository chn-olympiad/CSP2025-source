#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", "stdin");
	freopen("replace.out", "w", "stdout");
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	int a, b;
	long long xabcx, xadex, ab, cd, bc, de, aa, bb, aaaa, bbbb;
	cin >> a;
	cin >> b;
	cin >> xabcx;
	cin >> xadex;
	cin >> ab;
	cin >> cd;
	cin >> bc;
	cin >> de;
	cin >> aa;
	cin >> bb;
	cin >> xabcx;
	cin >> xadex;
	cin >> aaaa;
	cin >> bbbb;
	if (a = 4) {
		cout << 2 << endl << 0;
	}
	return 0;
}
