#include <bits/stdc++.h>
using namespace std;
#define ll long long
int n, q;
map<int, int>m;
int main(int argv, char **argc) {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;


//
//	for (int z = 1; z <= n; z++) {
//		string s1, s2;
//		cin >> s1 >> s2;
//		m[((int)s1.find('b')) - ((int)s2.find('b'))]++;
////		cout << "::::" << abs(((int)s1.find('b')) - ((int)s2.find('b'))) << '\n';
//	}
//
	for (int z = 1; z <= q; z++) {
//		string s1, s2;
//		cin >> s1 >> s2;
//		if (s1.size() != s2.size()) {
		cout << "0\n";
//			continue;
	}
//		cout << m[((int)s1.find('b')) - ((int)s2.find('b'))] << '\n';
////		cout << "::::" << abs(((int)s1.find('b')) - ((int)s2.find('b'))) << '\n';
//	}
	return 0;
}
