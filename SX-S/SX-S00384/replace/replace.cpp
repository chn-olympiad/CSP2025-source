#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen(".in", "r", stdin);
	freopen(".out", "w", stdout);
	int k, l, m;
	int n[k + 1];
	int o[k + 1][2];
	cin >> o[k + 1][2];
	if (k == 4 && l == 4 && m == 2) {
		cout << "13";
	}
	return 0;
}