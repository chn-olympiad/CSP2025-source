#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	for (int i = 0; 1 <= i <= n; i++) {
		string s1, s2;
		cin >> s1 >> s2;
		for (int j = 0; 1 <= j <= q; j++) {
			string t1, t2;
			cin >> t1 >> t2;
		}
	}
	if (n = q) {
		cout << "ti hao nan,wo bu hui";
	} else {
		cout << "Hello,world";
	}
	return 0;
}
