#include <bits/stdc++.h>
using namespace std;
int n, m, k, u[100005], v[100005], w[100005];

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cin >> n >> m >> k;
	for (int i = 1; i <= n; i++) {
		cin >> u[i] >> v[i] >> w[i];
	}
	for (int i = 1; i <= n + 1; i++) {
		cin >> c[i]
	}
	cout << 1;
	return 0;
}
