#include <bits/stdc++.h>
using namespace std;
const int N = 505;
int n, q;
int s1[N], s2[N];
int t1[N], t2[N];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;

	return 0;
}
