#include <bits/stdc++.h>
using namespace std;

struct node {
	int a, b, c, d;
} st[100005];
bool s[100005];
int b[10], n;
const int max();

void ma(int x, int y) {
	if (y == 1) {
		st[x].d = max(max(st[x].a, st[x].b, st[x].c)) {
			else

				else if (y == 2) {
					st[y].d = max(max(st[y].a, st[x].b, st[x].c)) {
					}
				}
		}

		bool (node x, node y) {

		}

		int main() {
			//freopen("club.in","r",stdio);
			//freopen("club.out","w",stdout);
			int t;
			cin >> t;
			for (int i = 1; i <= t; i++) {
				cin >> st[i].a >> st[i].b >> st[i].c;
				ma(i, a);
			}
			memset(s, false, sizeof s);
			memset(b, sizeof b);
			int n, a = 1, m = 0;
			cin >> n;
			for (int j = 1; j <= n; j++) {
				cin >> st[j].a >> st[j].b >> st[j].c;
				ma(j, a);
			}
			sort(st + 1, st + 1 + n, pi);
			for (int j = 1; j <= n; j++) {

			}
		}
		return 0