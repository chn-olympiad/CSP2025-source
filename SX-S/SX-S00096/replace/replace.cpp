#include <bits/stdc++.h>
using namespace std;
int a[200005], b[200005];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cin >> q;
	for (int i = 1; i <= q; i++) {
		cin >> b[i];
	}
	cout << 52;
	return 0;
}
