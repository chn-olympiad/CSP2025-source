#include <bits/stdc++.h>
using namespace std;
int a[100005];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	string s;
	int n, m;
	cin >> s;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 220604;
	return 0;
}
