#include <bits/stdc++.h>
using namespace std;

int n, q;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	while (n--) {
		string s1, s2;
		cin >> s1 >> s2;
	}
	while (q--) {
		string s1, s2;
		cin >> s1 >> s2;
		cout << 0 << endl;
	}

	return 0;
}
