#include <bits/stdc++.h>
using namespace std;
int n, q;
string s1, s2, t1, t2;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q >> s1 >> s2 >> t1 >> t2;
	for (int i = 0; i < q; i++) {
		cout << 0;
	}
	return 0;
}
