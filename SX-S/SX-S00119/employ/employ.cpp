#include <bits/stdc++.h>
using namespace std;
int n, m;
string a;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int c[100025];
	cin >> n >> m;
	cin >> a;
	for (int i = 1; i <= n; i++)
		cin >> c[i];
	cout << "0";
	return 0;

}
