#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string s;
	for (int i = 0; i < s.size(); i++) {
		cin >> s[i];
	}
	int c[510];
	for (int j = 0; j < n; j++) {
		cin >> c[j];
	}
	cout << 2 << endl;
	return 0;
}