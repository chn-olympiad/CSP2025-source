#include <bits/stdc++.h>
using namespace std;
long long n, q;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	scanf("%lld", &n);
	scanf("%lld", &q);
	for (int i = 1; i <= q; i++) {
		printf("0\n");
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
