#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define cin in
#define cout out
int main(){
	fstream in,out;
	in.open("a.in",ios::in);
	out.open("b.out",ios::out);
	int n,q;
	cin >> n >> q;
	for(int i=0;i<q;i++){
		cout << 0 << endl;
	}
	return 0;
}