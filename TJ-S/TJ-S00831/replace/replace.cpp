#include <bits/stdc++.h>

using namespace std;

int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    cin >> n, q;
    for (int i = 0; i <= t; i++) {
        string si1, si2;
        cin >> si1 >> si2;
        cout << n + q << endl << q << endl;
    }



 return 0;
}

