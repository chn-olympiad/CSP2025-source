#include <bits/stdc++.h>

using namespace std;

int n, q;
string s1[200006], s2[200007], t1, t2;

int main()
{
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 1;i <= n;i++)cin >> s1[i] >> s2[i];
	while (q--)
	{
		cin >> t1 >> t2;
		cout << 0;
	}
	return 0;
}
