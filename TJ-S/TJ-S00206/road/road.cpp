#include <bits/stdc++.h>
#include <cmath>
#include <vector>
#include <string>
using namespace std;
const int MAXN=1000001;
int t,n,a[MAXN],b[MAXN],c[MAXN],y[MAXN],cs=0,aa=0,bb=0,cc=0,an[MAXN][MAXN],bn[MAXN][MAXN],cn[MAXN][MAXN],ccs[MAXN],sum=0;
int main(){
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	cout<<10;
	return 0;
}
