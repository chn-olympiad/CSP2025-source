#include<bits/stdc++.h>
using namespace std;
const int maxn = 1000006;
int n, m, k;
int u[maxn], v[maxn], w[maxn];
int main(){
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
	cin >> n >> m >>k;
	for(int j = 1; i <= m; i++){
		cin >> u[i] >> v[i] >> w[i];
	}
	return 0;
}
