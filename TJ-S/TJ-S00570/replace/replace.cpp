#include <bits/stdc++.h>
using namespace std;

const int N = 2e5

struct strc(){
	string s1,s2;
	int stsize;
}stc[N];

int c,q;

int main(){
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	
	cin >> c >> q;
	for(int i = 1 ; i<= c ; i++){
		cin >> stc[i].s1 >> stc[i].s2;
		stc[i].stsize = s1.length();
	}
	
	fclose(stdin);
	fclose(stdout);
	return 0;
}
