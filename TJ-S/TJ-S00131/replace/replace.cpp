#include <bits/stdc++.h>
using namespace std;
#define endl "\n";
int main() {
	freopem("replace.in", "r", stdin);
	freopem("replace.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	int n, q;
	cin >> n >> q;
	for (int i = 0; i < n; i ++) cout << 0 << "\n";
	return 0;
}
 
