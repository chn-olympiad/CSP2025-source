#include <iostream>
#include <string>

using namespace std;

int c[500]

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string dfclt
	cin >> dfclt;
	for (int i = 0;i < n; ++i) {
		cin >> c[i];
	}
	cout << 0 << endl;
	return 0;
} 
