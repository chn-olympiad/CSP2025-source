#include <iostream>
#include <string>

using namespace std;

int n, q;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout); 
	cin >> n >> q;
	for (int i = 0; i < n; ++i) {
		string str1, str2;
		cin >> str1 >> str2;
	}
	for (int i = 0; i < q; ++i) {
		cout << 0 << endl;	
	}
	
}
