#include<bits/stdc++.h>
using namespace std;


int main()
{
	freopen("replacce.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	for(int i = 1; i <= n; i++)
	{
		int s1, s2;
		cin >> s1 >> s2;
	}for(int i = 1; i <= q; i++)
	{
		int s1, s2;
		cin >> s1 >> s2;
		cout << 0 << endl;
	}
	return 0;
}
