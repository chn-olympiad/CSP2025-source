#include<bits/stdc++.h>
using namespace std;
int n, m;
int day[501], tim[501];
int main(){
	freopen("employ.in", "r", stdin);
	freopen("employ.out". "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n; i++){
		cin >> day[i];
	}
	for (int i = 1; i <= n; i++){
		cin >> tim[i];
	}
	sort(tim + 1, tim + n + 1);
	if (m == 1){
		cout <<< "515058943";
	}else {
		
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
