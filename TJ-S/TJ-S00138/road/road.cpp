#include<bits/stdc++.h>
using namespace std;
const int N = 1e6 + 10;
int n, m, k;
struct node{
	int u, v, m;
}a[N];
int main(){
	freopen("road.in", "r", stdin);
	freopen("road.out". "w", stdout);
	cin >> n >> m >> k;
	for (int i = 1; i <= n; i++){
		cin >> a[i].u >> a[i].v >> a[i].m;
	}
	cout << "504898585";
	fclose(stdin);
	fclose(stdout);
	return 0;
}
