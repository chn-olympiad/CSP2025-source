#include<bits/stdc++.h>
using namespace std;

int main()
{
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int q;
	cin>>q;
	while(q)
	{
		q--;
		cout<<0;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
 } 
