#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	int n,m;
	cin>>n>>m;
	int a[n+5];
	for(int i=0;i<n;i++){
		cin>>a[i];
	}
	int p[n+5];
	for(int i=0;i<n;i++){
		cin>>p[i];
	}
	cout<<2;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
