#include <bits/stdc++.h>
using namespace std;

long long n, m, mod = 998244353, ans = 0, c[510];
string s;

int main()
{
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	
	scanf("%lld", &n, &m);
	getline(cin, s);
	for (int i = 0; i < n; i++)
	{
		scanf("%lld", &c[i]);
	}

	printf("2204128");	
	
	fclose(stdin);
	fclose(stdout);
	return 0;	
}
