#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int n,q;
	cin >> n >> q;
	string s[n],t[n],a[q],b[q];
	for(int i=0;i<n;i++){
		cin >> s[i] >> t[i];
	}
	for(int i=0;i<q;i++){
		cin >> a[i] >> b[i];
	}
	cout << 1;
	return 0;
} 
