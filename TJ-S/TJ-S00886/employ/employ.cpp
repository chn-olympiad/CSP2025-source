#include <bits/stdc++.h>
constexpr int N = 500 + 5;

char pass[N];
unsigned long long a[N][N];
int p[N];

struct {
    ;
} dp[N][N];

int main ()
{
    //freopen("employ.in", "r", stdin);
    //freopen("employ.out", "w", stdout);

    int n, m;
    scanf("%d%d%s", &n, &m, (char *)&pass);
    for (int i=0; i<n; ++i) pass[i] -= '0';
    for (int i=0; i<n; ++i) scanf("%d", &p[i]);
    __asm__(
        "nop aaaa"
    );
}