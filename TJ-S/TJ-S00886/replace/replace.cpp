#include <bits/stdc++.h>
constexpr int N = 2e5 + 5;
constexpr int Q = 2e5 + 5;

std::string tuples[N][2];

int main ()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);

    int n, q;
    std::cin >> n >> q;
    /*
    for (int i=1; i<=n; ++i)
    {
        std::cin >> tuples[i][0] >> tuples[i][1];
    }
    */
    while (q--)
    {
        /*
        std::string t1, t2;
        std::cin >> t1 >> t2;
        */
        std::cout << "0\n";
    }
}