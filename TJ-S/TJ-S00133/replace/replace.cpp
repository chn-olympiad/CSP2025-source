#include<bits/stdc++.h>
using namespace std;
const int MX = 1e4+1;
string a[MX], b[MX], c[MX], d[MX];

int main(){
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n,q;
	cin >> n >> q;
	for (int i = 1; i <= n; i++) cout << 0 << endl;
	return 0;
} 
