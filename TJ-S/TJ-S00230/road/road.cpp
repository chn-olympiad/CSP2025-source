#include <bits/stdc++.h>
using namespace std;
int v[10000];
int main()
{
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	int n,m,y,a[10000],b[10000],c[10000],d[10000];
	cin>>n>>m>>y;
	for(int i=1;i<=n;i++)
	{
		cin>>a[i]>>b[i]>>c[i];
		for(int j=1;j<=n;j++)
		{
			max(a[j],b[j],c[j]);
			if(b[j]=a[j])
			int v=c[j];
			int cv=a[j]+b[j];
		}
		a[i]=d[i];
		if(c[i]==1)
		{
			break;
		}
	}
	for(int i=1;i<=n;i++)
	{
		if(c[i]!=a[i])
		{
			break;
		}
		else
		{
			for(int j=1;j<=n;j++)
			{
				int s;
				a[j]-b[j];
			}
		}
	}
	int brt;
	for(int i=1;i<=b[i]-a[i+1];i/a[i]-b[i+1])
	{
		int gtr;
		for(int p=1;p<=n;p++)
		{
			int k=(a[p]-b[i+1]);
		}
		if(a[i]-b[i+3]!=0)
		{
			break;
		}
	}
	int ans;
	for(int j=1;j<=m;j++)
		{
			ans+=v[j];
		}
	cout<<15;
	return 0;
}
