#include <bits/stdc++.h>
using namespace std;
int mian(){
    freopen("club.in","r",stdin);
    freopen("club.out","w",stdout);
    int n,m,z,h,s;
    cin>>n>>m;
    for(int i=1;i<=n;i++){
        if(s[i]==0){
            cout<<0;
        }else{
            cout<<1;
        }
    }
    cout<<s;
    fclose(stdin);
    fclose(stdout);
}
