#include <bits/stdc++.h>
using namespace std;
int n, m;
char s[20000];
int main()
{
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	scanf("%d %d %s", &n, &m, s);
	int i;
	for(i = 1; i <= n; i ++){
		int c;
		scanf("%d", &c);
	}
	if(n == 3) printf("2");
	else printf("2204128");
	return 0;
}
