#include <bits/stdc++.h>
using namespace std;
int n, m;
string s[200001][2];
int main () {
	freopen ( "replace.in", "r", stdin );
	freopen ( "replace.out", "w", stdout );
	cin >> n >> m;
	for ( int i = 1; i <= n; i++ ) cin >> s[i][1] >> s[i][2];
	for ( int i = 1; i <= m; i++ ) {
		int cnt = 0;
		string str1, str2;
		cin >> str1 >> str2;
		printf ( "%d\n", 0 );
	}
	return 0;
}
