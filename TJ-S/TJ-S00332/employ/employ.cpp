#include<bits/stdc++.h>
using namespace std;
const int MAXN = 1e4 + 5e1;
int n, m, k, kd = 1001, ans = -1;
string s;
int main(){
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m >> s;
	for(int i = 0; i < n; i++){
		int u, v, w;
		cin >> u ;
	}
	cout << 0 << endl;
	return 0;
}
