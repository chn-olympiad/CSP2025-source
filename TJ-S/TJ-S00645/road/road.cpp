#include <bits/stdc++.h>
using namespace std;
const int N = 11000,M = 1100000;
int n,m,k,a[N][N];
struct Point
{
	int v;
	int w;
};
int main()
{
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	cin >> n >> m >> k;
	for (int i = 1;i <= m;i++)
	{
		int u,v,w;
		cin >> u >> v >>w;
	}
	for (int i = 1;i <= k;i++)
	{
		int c;
		cin >> c;
		for (int i = 1;i <= n;i++)
		{
			int x;
			cin >> x;
		}
	}
	cout << 0;
	return 0;
}
