#include<bits/stdc++.h>
using namespace std;
long long n, m, k, v, u, w, a;
int main()
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cin >> n >> m >> k;
	for(int i=0; i<m; i++)
	{
		cin >> u >> v >> w;
	}
	for(int i=0; i<k; i++)
	{
		cin >> a;
	}
	
	cout << "5182974449";
	return 0;
}
