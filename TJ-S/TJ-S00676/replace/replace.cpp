#include<bits/stdc++.h>
using namespace std;
int main( )
{
	freopen( "replace.in", "r", stdin );
	freopen( "replace.ans", "w", stdout );
	int n, m;
	cin >> n >> m;
	for ( int i = 0; i < n; i++ )
	{
		cout << "0";
	}
	fclose( stdout );
	return 0;
}
