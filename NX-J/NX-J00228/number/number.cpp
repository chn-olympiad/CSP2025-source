#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("number.in";"r";stdin);
    freopen("number.out";"r";stdout);
    int s;
    cin>>s;
    for(int i=10;i<=1000000,i*10)
    {
        for(int sum=s,sum/i)
        {
            cout<<s;
        }
    }
	return 0;
}
