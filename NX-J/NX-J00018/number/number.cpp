#include <bits/std c++.h>
using namespace std;
int main(){
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout );
    int s;
    cin << s;
    for{
        if()
    }

    fclose("number.in","r",stdin) ;
    fclose("number.out","w",stdout)   ;
    return 0;
}
