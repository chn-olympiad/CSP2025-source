#include<bits/stdc+.h>
using namespace std;
int main()
{
    freopen("numder.in""r"stdin)
    freopen("numder.out""w"stdout)
    int s;
    for(int i;i<=s;i++)
    {
       cin>>s;
    }
    if(s[i]%1)
    {
       if(s[i]<s[i+1])
       {
          s[i]=s[i+1]
          s[i+1]=a[i]
       }
    }
    cout<<s;
}
