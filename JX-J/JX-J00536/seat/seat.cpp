#include<bits/stdc++.h>
using namespace std;

long long n,m,a[110],c,r;

int main()
{
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);

    cin >> n >> m;
    for(int i=1;i<=n*m;i++)
    {
        cin >> a[i];
    }
    for(int i=0;i<=n*m;i++)
    {
        for(int j=0;j<=n*m;j++)
        {

        }
    }
    cout << c << r << ' ';

    fclose(stdin);
    fclose(stdout);

    return 0;
}
