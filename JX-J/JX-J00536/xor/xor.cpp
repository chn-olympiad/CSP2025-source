#include<bits/stdc++.h>
using namespace std;

long long n,k,a[1000010],maxa;

int main()
{
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);

    cin >> n >> k;
    for(int i=0;i<=n;i++)
    {
        cin >> a[i];
    }
    cout << maxa;

    fclose(stdin);
    fclose(stdout);

    return 0;
}
