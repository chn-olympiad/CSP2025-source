#include<bits/stdc++.h>
using namespace std;
int a[1000];
int main()
{
  freopen("xor.in","r",stdin);
  freopen("xor.ans","w",stdout);
  int n,k,l,r,an=0;
  cin>>n>>k;
  for (int i=0;i<n;i++)
    cin>>a[i];
  for (int i=0;i<n;i++)
    if (l<k && r)
  cout<<an<<endl;
  fclose(stdin);
  fclose(stdout);
  return 0;
}
