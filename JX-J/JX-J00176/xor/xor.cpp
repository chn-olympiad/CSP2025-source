#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
    int n=0,k=0;
    cin>>n>>k;
    long long sz[n];
    for(int i=0;i<n;i++){
        cin>>sz[i];
    }
    cout<<2;
    return 0;
    fclose(stdin);
    fclose(stdout);
}
