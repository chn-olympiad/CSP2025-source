#include<bits/stdc++.h>
using namespace std;
int n,f,e,ans,a[10005];
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    cin>>n;
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    for(int j=1;j<=n;j++){
        f=max(f,a[j]);
        e+=a[j]
    }

    if(n>=3){
        if(e>2*f){
            for(int ii=3;ii<=n;ii++){

            }
        }
        return 0;
    }
    else{
        return 0;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}

