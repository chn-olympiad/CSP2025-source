#include <bits/stdc++.h>
#include <algorithm>
using namespace std;
int mod=998244353;
int main(){
      freopen("polygon.in","r",stdin);
      freopen("polygon.out","w",stdout);
      int n,cnt=0;
      scanf("%d",&n);
      vector<int>timb;
      int _sum;
      for(int i=0;i<n;i++){
            scanf("%d",&timb[i]);
      }
      do{

      }while(next_permutation(k,k+n));
      printf("%d",cnt%mod);
      return 0;
}
