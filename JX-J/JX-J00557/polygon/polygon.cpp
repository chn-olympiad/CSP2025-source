#include<bits/stdc++.h>
using namespace std;
int n,a[1000],z;
int main(){
	freopen("polygon.in","r",stdin);
	freopen("polygon.out","w",stdout);
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>a[i];
	}
	 if(a[5]==5)
	 cout<<"9";
	 else if(a[5]==5)
	 cout<<"6";
	 else
	 cout<<"10";
	fclose(stdin);
	fclose(stdout);
	return 0;
}
