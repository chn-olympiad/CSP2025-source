#include<bits/stdc++.h>
using namespace std;
int a[100005];
int main(){
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
    int n,k;
    cin>>n>>k;ac
    for(int i=1;i<=n;i++)
        cin>>a[i];
    if(n<=2&&k==0)
    cout<<1;
    else if(n<=100&&k==0){
        cout<<n/2;
    }
    fclose(stdin);
    fclose(stdout);
return 0;
}
