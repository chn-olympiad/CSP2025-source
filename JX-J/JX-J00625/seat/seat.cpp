#include<bits/stdc++.h>
using namespace std;

main int()
{
 freopen("seat.in","r",stdin);
 freopen("seat.out","w",stdout);
 
 int m,n;
 if i>=1,<=n*m;
    a>=1,<=100;
 cin>>m,n,a;
 cout<<a=i/a=n*m-i+1<<endl;
 
 fclose(stdin);
 fclose(stdout);
 return 0;
}
