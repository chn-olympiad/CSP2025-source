#include<bits/stdc++.h>
using namespace std;
char s[100010];
int main()
{
    freopen("number.in", "r", std.in);
    freopen("number.out", "w", std.out);
    cin >> s[100010];
    for(int i = 1; i <= 1000000; i++)
    {
        if (s[i] <= '9' || s[i] >= '0')
        {
            cout << s[i];
        }
    }
    return 0;
}
