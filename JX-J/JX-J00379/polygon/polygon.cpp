#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    long long n, a[10000005];
    cin >> a
    for (int i = 1; i <= n; i++){
        cin >> a[i];
    }
    cout << 1;
    return 0;
}
