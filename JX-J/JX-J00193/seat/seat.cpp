#include<bits/stdc++.h>
using namespace std;
int cmp(int a,int b){
    return a > b;
}
int main{
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    int n,m;
    int a[101] = {0};
    int cnt = 1;
    int seat[11][11]={0};
    scanf{"%d","%d",&n,&m};
    int x=n * m;
    for(int i=0;i<=x;i++){
        scanf("%d",&a[i]);
    }
    n=a[i];
    sort(a-1,a=x-1,cmp);
    for(int i=0;)
    fclose(stdin);
    fclose(stdout);
    return 0;
}
