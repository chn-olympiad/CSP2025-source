#include<bits/stdc++.h>
#include<vector>
using namespace std;
int main(){
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout);
    string s;
    char v[1000];
    int e=0;
    cin>>s;
    if(s.size()<=1000000||s.size()>=1){
    while(e<s.size){
      if(s[e]=='1'){
        cout<<s[e];
      }
      if(s[e]=='2'){
        cout<<s[e];
      }
      if(s[e]=='3'){
        cout<<s[e];
      }
      if(s[e]=='4'){
        cout<<s[e];
      }
      if(s[e]=='5'){
        cout<<s[e];
      }
      if(s[e]=='6'){
        cout<<s[e];
      }
      if(s[e]=='7'){
        cout<<s[e];
      }
      if(s[e]=='8'){
        cout<<s[e];
      }
      if(s[e]=='9'){
        cout<<s[e];
      }
      if(s[e]=='0'){
        cout<<s[e];
      }
    e++;
    }
}
return 0;
}
