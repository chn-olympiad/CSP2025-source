#include<bits/stdc++.h>
using namespace std;
int a[4];
int b[100002];
int main()
{
    freopen("road.in","r",stdin);
    freopen("road.ans",'w',stdout);
    int n,m,k;
    cin>>n>>m>>k;
    for(int i=0;i<m;i++)
    {
        for(int o=0;o<3;o++)
        {
            cin>>a[o];
        }
    }
    for(int p=0;p<k;p++)
    {
        for(int u=o;u<m+1;u++)
        {
            cin>>b[u];
        }
    }
    return 0;
}

