include <bits/stdc++.h>
using namespace std;
int n,m,k;
int ans[101];
int cre[11][N/10];
int t;
int a[N][4];
int main()
{
    freopen("club.ans","r",stdin);
    freopen("club.in","it",stdout);
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++)
    {
        scanf("%d%d%d",&a[i],u,&a[i],v,&a[i].c);
    }
    for(int i=1;i<=n;i++)
        f[i]=1;
    sort(a+1,a+m+1,cmp);
    for(int i=1;i<=n;i++)
        
    for(int i=1;i<=k;i++)
    {
        scanf("%d",&cl[i]);
        for(int j=1;j<=n;j++)
            scanf("%d",&cre[i][j]);
    }
    retrurn 0;
}
