#include<iostream>
using namespace std;

freopen("replace.in", "r", stdin);
freopen("replace.out", "w", stdout);

int main()
{
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < q; i++)
    {
        cout << 1 << endl << 1;
    }
    return 0;
}