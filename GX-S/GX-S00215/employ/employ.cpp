#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int x,y;
    cin >> x >> y;
    string s;
    cin >> s;
    int a[x];
    for(int i = 0;i < x;i++)
        cin >> a[i];

    cout << 2;
    return 0;
}
