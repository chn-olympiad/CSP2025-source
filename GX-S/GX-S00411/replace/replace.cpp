#include<bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int t, n;
struct node{
    int x, y, z, c;
}a[N];
bool cmp(node A, node B){
   return A.c < B.c;
}
void solve(){
    int q;
    cin >> n >> q;
    while(n--){
        string s, s1;
        cin >> s >>s1;
    }
    while(q--){
        string s2, s3;
        cin >> s2 >> s3;
        cout << "0\n";
    }
}
int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
        solve();
    return 0;
}
