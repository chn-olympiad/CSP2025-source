#include<bits/stdc++.h>
using namespace std;
string s[100001];
int main()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < n; i++)
        cin >> s[i];
    string c;
    while(q--)
    {
        cin >> c;
        cout << 1 << endl;
    }
    return 0;
}
