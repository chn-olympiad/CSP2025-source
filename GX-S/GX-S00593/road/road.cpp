#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    ios::sync_with_stdio(false), cin.tie(0);
    int n, m, k;
    cin >> n >> m >> k;
    for(int i = 0; i < m; i++)
    {
        int a, b, c;
        cin >> a >> b >> c;
    }
    for(int i = 0; i < k; i++)
    {
        int prize;
        cin >> prize;
        for(int j = 0; j < m; j++)
        {
            int newroad;
            cin >> newroad;
        }
    }
    cout << 0;
    return 0;
}
