#include <bits/stdc++.h>
using namespace std;

int n, k, m, v, u, w, a[3], b[10000010];

int main(){

    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    cin >> n >> k >> m >> u >>v >>w;
    for(int i = 1; i <= m; i++){
        cin >> a[i] >> a[i] >>a[i];
    }
    for(int i = 1; i <= n + 1; i++){
    cin >> b[i];
    }
    for(int i = 1; i <= k;i++)
        cout << 0 <<endl;
    return 0;
}
