#include <bits/stdc++.h>
using namespace std;

int n, k, m, v, u, w, a[3], b[10000010];

int main(){

    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    cin >> n >> k >> m >> u >>v >>w;
    for(int i = 1; i <= m; i++){
        cin >> a[i] >> a[i] >>a[i];
    }
    for(int i = 1; i <= n + 1; i++){
    cin >> b[i];
    }cout << 0;
    return 0;
}
