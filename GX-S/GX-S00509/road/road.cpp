#include<bits/stdc++.h>
using namespace std;
const int MAXN=1005;
int u[MAXN];
int v[MAXN];
int w[MAXN];
int c[MAXN];
int a[MAXN];
int n,m,k;
int road_build,countryside_change;
int cost_n,cost_m,cost_k;
int  minn;

void dfs( int  p , int minn_n , int minn_m , int minn_k ){
	for( int = 1 ; i< = ( n+m+k ) ; i++ )
		dfs( n , m , k )
		return ;
	
	for(  int i = 1 ; i <= k ; i ++  )
		if(k){
			countryside_change++;
			const_k += w[i];
			m--;
		}
		minn=minn_k<(minn_n+minn_m)?minn:(minn_n+minn_m);
		
	
}

int main(){
	
	freopen("road,in","r",stdin);
	freopen("road.out","w",stdout);
	
	
	cin >> n >> m >> k;
	
	for(  int i = 0 ; i <= m ; i ++  ){
		cin >> u[i] >> v[i] >> w[i];
	}
	
	for(  int j = 0 ; j <=( k+m ) ; j ++  ){
		for(  int h = 1 ; h <= ( n=1 ) ; h ++  )
			cin >> c[h] >> a[h];
		
		
	return 0;
}
