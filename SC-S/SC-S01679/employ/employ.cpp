#include<bits/stdc++.h>
using namespace std;

int n , m;
int dif , a[100005];

int main(){
    freopen("employ.in","r",stdin);
    freopen("employ.out","w",stdout);

    cin >> n >> m >> dif;
    for(int i = 1;i <= n;i++) cin >> a[i]

    cout << 2;

    fclose(stdin);
    fclose(stdout);
    return 0;
}