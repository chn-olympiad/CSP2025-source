#include <cstdio>

using namespace std;

int main() {
	freopen("replace.out", "w", stdout);
	freopen("replace.in", "r", stdin);
	int n, q;
	scanf("%d %d", &n, &q);
	for (int i = 1; i <= q; ++i) {
		printf("0\n");
	}
	return 0;
}