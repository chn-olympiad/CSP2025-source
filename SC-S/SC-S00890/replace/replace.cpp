/*
因为太久没上机导致忘记prim导致死磕T2 80分解导致现在只能不可以总司令了
预估 100+56
大失误，一年不上机对bro确实太勉强了
算了反正是来玩的，一等去年就get了，能进NOIP就行
悲惨的高三er还是过于忙碌了 
*/
#include<bits/stdc++.h>
using namespace std;
const int N=5e6+6;
int n,q;
char s[N];
signed main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cin>>n>>q;
	cin>>(s+1);
	for(int i=1;i<=q;i++){
		cout<<0<<'\n';
	}
	return 0;
}
