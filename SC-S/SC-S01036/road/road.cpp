#include <bits/stdc++.h>
#define int long long
#define endl "\n"

using std::cin;
using std::cout;

struct node {
	int v, w, next;
};
signed main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	for (int i = 1; i <= m; i++) {
		int u, v, w;
		cin >> u >> v >> w;
	}
	cout << 0;
	return 0;
}