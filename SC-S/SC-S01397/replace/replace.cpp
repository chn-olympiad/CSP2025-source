#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int n, q;
string s, ss;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	
	cin >> n >> q;
	
	cin >> s >> ss;
	
	for (int i = 1; i <= q; i++) {
		string t, tt;
		cin >> t >> tt;
		cout << 0 << '\n';
	}
	
	return 0;
}