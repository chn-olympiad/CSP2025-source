#include <iostream>
using namespace std;
#define int long long
int n, q;
signed main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	scanf("%lld %lld", &n, &q);
	while (q--) puts("0");
	return 0;
}
