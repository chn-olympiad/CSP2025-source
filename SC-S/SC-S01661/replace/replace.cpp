#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string str;
	for (int i = 1; i <= n; i++)cin >> str >> str;
	while(m--) {
		puts("0");
	}
	return 0;
}