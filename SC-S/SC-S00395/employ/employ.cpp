#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int n, m;
string s;
int c;
int main () {
	freopen ("replace.in", "r", stdin);
	freopen ("replace.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	cin >> n >> m;
	cin >> s;
	for (int i = 1; i <= n; i++) {
		cin >> c;
	}
	cout << 2;
	return 0;
}