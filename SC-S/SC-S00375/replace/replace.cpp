#include <bits/stdc++.h>
using namespace std;
long long n,q;
int main()
{
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	cin >> n >> q; 
	while(n--)
	{
		int a,b;
		cin >> a >> b;
	}
	while(q--)
	{
		int a,b;
		cin >> a >> b;
	}
	if(n == 4 && q == 2)
	{
		cout << "2\n0";
	}
	if(n == 3 && q == 4)
	{
		cout << "0\n0\n0\n0";
	}
	
	return 0;
 } 