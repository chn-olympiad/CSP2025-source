#include<bits/stdc++.h>
using namespace std;
int n , q; 
int main() {
	freopen("replace.in" , "r" , stdin);
	freopen("replace.out" , "w" , stdout);
	cin >> n >> q;
	for(int i = 1 ; i <= n ; i++) {
		string a , b;
		cin >> a >> b;
	}
	for(int i = 1 ; i <= q ; i++) {
		string a , b;
		cin >> a >> b; 
		cout << 0 << "\n";
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}

