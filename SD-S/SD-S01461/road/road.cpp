#include <bits/stdc++.h>
//×î¶ÌÂ·
using namespace std;

int main(){
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	for(int i = 0; i < m; ++ i){
		int u, v, w;
		cin >> u >> v >> w;
	}
	int c, a;
	cin >> c;
	for(int i = 0; i < k; ++ i){
		cin >> a;
	}
	cout << 0;
	return 0;
}
