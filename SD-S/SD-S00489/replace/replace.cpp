#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
vector<string> e1,e2;
vector<ll> net[233];
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	ll n,q;
	cin>>n>>q;
	for(ll i=1;i<=n;i++){
		string x,y;
		cin>>x>>y;
		e1.push_back(x);
		e2.push_back(y);
		ll len=x.length();
		for(ll u=2,v=0;u<=len;u++){
		}
	}
	while(q--){
		for(ll i=1;i<=n;i++){
			cout<<"Good Bye,CSP-S.";
		}
	} 
	return 0;
}
//0pts
