#include <iostream>
#include <iomanip> 
#include <algorithm>
#include <cmath>
#include <string>
#include <cstdio>
using namespace std;
long long A[700],C[700];
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	long long n,m,i;
	string s;
	cin>>n>>m;
	cin>>s;
	for(i = 1 ; i <= n ; i++) cin>>C[i];
	cout<<"2";
	fclose(stdin);
	fclose(stdout);
	return 0;
}
