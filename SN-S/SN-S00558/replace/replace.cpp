#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef double db;
typedef pair<int, int> pii;

const int N = 201000;
int n, q;
string s[N];

int main() {
//	freopen(".in", "r", stdin);
//	freopen(".out", "w", stdin);
	scanf("%d%d", &n, &q);
	for (int i = 1; i <= n; i++)
		cin >> s[i];
	 
	return 0;
}
