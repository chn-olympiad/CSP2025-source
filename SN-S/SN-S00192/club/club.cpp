#include<bits/stdc++.h>
using namespace std;
bool is_even(int n)
{
	if(n % 10 == 2 || n % 10 == 4 || n % 10 == 6 || n % 10 == 8 || n % 10 == 0)
	{
		bool is_even = 1;
	}
}

int i,j;
bool is_into(int saticfid,int a,int i, int j)
{
	if(is_into[i][j])
	{
		saticfid = a[i][j];
	}
}
int main()
{
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	int k;
	cin >> k;
	int n;
	cin >> n;
	int d;
	int number;
	for(int i = 0; i < n; i++)
	{
		cin >> number.d.[i];
	}
	int sum_accept = -1;
	int * a =  new int [10000];
	for(int i = 0; i <= 3; i++)
	{
		cin >> a[i];
	}
	if(a[0] <= n / 2 and  a[1] <= n / 2 and a[2] <= n / 2)
	{
		sum += i;
	}
	 cout << sum << endl;
	
	
	
	
	return 0;
}
