#include<bits/stdc++.h>
using namespace std; 

int main(){
	//freopen("club.in", "r", stdin);
	//freopen("club.out", "w", stdout);
	
	int t;
	int n;
	int x, y, z;  
	cin >> t;
	for(int i = 0; i <= t; ++i){
		cin >> n;
		for(int i = 0; i <= n; ++i){
			cin >> x >> y >> z;
		}
	}
	
	int q = max(x, y, z);
  

	
	for ( x = 1; x < i; ++x ) {
		X++;
	} 
	
	cout << x << endl ;
	
	
    return 0;	
} 
