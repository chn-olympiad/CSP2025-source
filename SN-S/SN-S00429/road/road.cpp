#include <bits/stdc++.h>
using namespace std;

int n, m, k;
int a;

int main() {
freopen("road.in","r",stdin);
freopen("road.out","w",stdout);

ios::sync_with_stdio(0);
cin.tie(0);cout.tie(0);

	cin >> n >> m >> k;
	for(int i = 1;i <= 3 * n + k * (n + 1);i++) {
		cin >> a;
	}
	
	cout << 0;
	return 0;
}

