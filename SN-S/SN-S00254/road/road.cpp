#include <bits/stdc++.h>
using namespace std;
const int N = 1e6+5;
int n,m,k,a[N],b[N],c[N],d[N];
int main(void)
{
	cin >> n >> m >> k;
	for (int i = 1; i<= m; i++)
	{
		cin >> a[i] >> b[i] >> c[i];
	}
	for (int i = 1; i <= k; i++)
	{
		cin >> d[N];
	}
	cout << 24;
	return 0;
}
