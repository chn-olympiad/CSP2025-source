#include <bits/stdc++.h>
using namespace std;
struct r{
	int u,v;
};
int w[1000010];
r p[1000010];
int main(){
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	/*int n,m,k; //输入的第一行包含三个非负整数 n, m, k，分别表示原有的城市数量、道路数量和准
//备进行城市化改造的乡镇数量。
	cin >> n >> m >> k;
	for(int i=1;i<=m;i++){
		cin >> p[i].u >> p[i].v >> w[i];
	}
	int c00;
	cin >> c00;
	*/
	
	cout << 0 << endl;
	return 0;
}
