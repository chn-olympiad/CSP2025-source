// GZ-S00246 贵州大学附属中学 李雨钊
#include <bits/stdc++.h>
using namespace std;
int n , m , k , u[10000005] , v[10000005] , w[10000005] , c[10000000005] , ans , numx;
int main()
{
	freopen("road.in" , "r" , stdin);
	freopen("road.out" , "w" , stdout);
	cin >> n >> m >> k;
	for(int i = 1; i <= m; i++)
	{
		cin >> u[i] >> v[i] >> w[i];
	}
	for(int j = 1; j <= k; j++)
	{
		cin >> c[i];
		for(int i = 1; i <= n; i++)
		{
			int a[j + 5],i ;
			cin >> a[j],i
		}
	}
	for(int j = 1; j <= k; j++)
	{
		int num,j;
		for(int i = 1; i <= n; i++)
		{
			num,j += a[j],i; 
		}
		numx = min(num,)
		
	}
	ans = c[1] + w[4] + w[5] + 3; 
	cout << ans;
	return 0;
}
