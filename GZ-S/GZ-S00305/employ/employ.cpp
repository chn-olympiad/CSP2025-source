// GZ-S00305 贵阳市花溪区湖潮中学 王一帆
#include <bits/stdc++.h>
using namespace std;

// int test(int left, char diff[n], int patience[n]) {

// }

int main()
{
    // freopen("employ.in", "r", stdin);
    // freopen("employ.out", "w", stdout);
    int m, n;
    cin >> m >> n;
    char diff[n];
    cin >> diff;
    int patience[n];
    for (int nn; nn < n; nn++)
    {
        cin >> patience[nn];
    }


    return 0;
}