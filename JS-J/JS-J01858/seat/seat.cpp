#include<bits/stdc++.h>
using namespace std;

int main(){
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    int n,m,b;
    cin >> n >> m;
    int a[n*m]
    sort_heap(_a[n*m]_right);
    for (int i = 0;i = n*m;i++){
        if a1 < ai;
        b++
    }
    cout << int b/m+1 << b%m;
    return 0;
    fclose(stdin);
    fclose(stdout);
}
