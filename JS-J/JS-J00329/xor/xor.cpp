#include <bits/stdc++.h>
using namespace std;
#define int long long

const int N = 5 * 1e5 + 10;
//bu tai hui,pian yi xia fen
int n , k;
int a[N];

signed main () {
    freopen("xor.in" , "r" , stdin);
    freopen("xor.out" , "w" , stdout);
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    cout << "2";
    fclose(stdin);
    fclose(stdout);
    return 0;
}
