#include <bits/stdc++.h>
using namespace std;
long long a[500005];
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    long long n,ans;
    cin>>n;
    if(n==3){
        cout<<1;
        fclose(stdin);
        fclose(stdout);
        return 0;
    }
    cout<<n;
    fclose(stdin);
    fclose(stdout);
    return 0;
}