#include <iostream>
#include <vector>
using namespace std;
int ch[5005];
const int mod=998244353;
unsigned long long tot;
unsigned long long dm;
int main(){ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);freopen("polygon.in","r",stdin);freopen("polygon.out","w",stdout);
    int n;
    cin>>n;
    for(int i=0;i<n;i++){
        cin>>ch[i];
    }
    if(n<3){
        cout<<0;
        return 0;
    }
    return 0;
}
