#include<bits/stdc++.h>
using namespace std;

int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n;
    cin >> n;
    int a[n] = {0};
    for(int i = 0;i < n;i++){
        cin >> a[i];
    }
    int num = 9;
    cout << num;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
