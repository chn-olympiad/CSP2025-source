#include <bits/stdc++.h>
using namespace std;

int n, m;
int x, i;

int main()
{
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);
    cin >> n >> m;
    for (i = 1; i <= n; i++)
        cin >> x;
    cout << 1 << endl;
    return 0;
}
