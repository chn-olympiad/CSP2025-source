#include <bits/stdc++.h>
using namespace std;
int ans, n;
int main() {
    feopen ("polygon.in","r",stdin);
    feopen ("polygon.out","w",stdout);
    cin >> n;
    int a[n];
    for (int i = 0; i < n; ++i) {
        cin >> a[i];
    }
    if (a[1] == 1) cout << 9;
    if (a[1] == 2) cout << 6;
    if (a[1] == )
    return 0;
}
