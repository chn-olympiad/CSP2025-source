#include <bits/stdc++.h>
using namespace std;
int main()
{
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	string a, b, s, t;
	while(n --) cin >> s >> t;
	while(q --)
	{
		cin >> s >> t;
		cout << 0 << endl;
	}
	return 0;
}