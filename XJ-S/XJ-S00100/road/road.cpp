#include <bits/stdc++.h>
using namespace std;
long long int m,n,k,ans,a1,b1,c1,ans1,f,flag,a2,b2,c2,a[1000006],b[1000006];
struct node
{
	long long int x,y,z;
}q[100005];
bool cmp(node a,node b)
{

		return a.z>b.z;
}
int main()
{
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	cin>>n>>m>>k;
	while(f=f)
	{
		cin>>a[f++];
		if(a[f++]==0)
		{
			brack;
		}
	}
	while(f=f)
	{
		cin>>b[f++];
		if(b[f++]==0)
		{
			brack;
		}
	}
	sort(q+1,q+1+n,cmp);
	if()
	cout<<
	return 0;
}

