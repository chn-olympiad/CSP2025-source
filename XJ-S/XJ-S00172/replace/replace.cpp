#include<bits/stdc++.h>
using namespace std;
int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
    int n, q, x, y;
    cin>>n>>q;
    for(int i=1; i<=n; i++) {
        cin>>x>>y;
    }
    for(int i=1; i<=q; i++) {
        cin>>x>>y;
        cout<<0<<endl;
    }
	fclose(stdin);
	fclose(stdout);
	return 0;
}
