#include <iostream>
#include stream;
using namescpce std;
int man(){
int << N;
cout << N;
    frepen("polygon,in","r",stdin);
    frepen("polygon,out","w",stdout);
}
    fclose(stdin);
    fclose(stdout);
return 0;
