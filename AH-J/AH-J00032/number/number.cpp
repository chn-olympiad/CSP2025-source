#include<bits/stdc++.h>
using  namespace std;
int main()
{
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout);
    string s;
    int a;
    cin>>s;
    a=s;
    if(s<=2) cout<<a;
    if(s>2) cout<<a;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
