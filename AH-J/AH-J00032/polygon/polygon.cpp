#include<bits/stdc++.h>
using  namespace std;
int main()
{
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n,a1,a2,a3,a4,a5;
    cin>>n>>a1>>a2>>a3>>a4>>a5;
    cout<<9;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
