#include <bits/stdc++.h>
using namespace std;
int a[10000005];
int main()
{
    freopen ("polygon.in", "r", stdin);
    freopen ("polygon.out", "w", stdout);
    int n;
    cin >> n;
    if (n >= 3 && n <= 10)
        cout << 12;
    else if (n >= 11 && 500)
        cout << 25;
    else
        cout << 1;
    return 0;
}
