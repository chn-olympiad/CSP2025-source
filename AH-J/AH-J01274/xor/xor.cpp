#include<bits/stdc++.h>
using namespace std;

int main() {
    freopen("xor.in", "r", stdin);
    freopen("xor.out", "w", stdout);

    cout << (1 ^ 0 ^ 3) << endl;

    fclose(stdin);
    fclose(stdout);
    return 0;
}
