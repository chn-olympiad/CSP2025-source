#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n,h,number_max,c;
    cin >> n;
    int polyon[5000] = {0};
    for(int i = 0;i <= n-1;i++){
        cin >> polyon[i];
    }
    for(int i = n;i >= 3;i++){

    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
