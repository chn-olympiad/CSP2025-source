#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdin);
    long long m,n;
    long long a[i];
    cin>>m>>n;
    for(long long i;i<=m*n;i++){
        cin>>a[i];
    }
    r=a[1];
    sort(a+1,a+n*m+1);
    long long x,y;
    for(int i=1;i<=n*m;i++){
	    b[x][y]=a[i];
	    if()
	}
    return 0;
}
