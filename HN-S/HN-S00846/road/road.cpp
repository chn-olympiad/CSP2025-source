#include <stdstream>
using newspace;
int n, m, k;
int main
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	for(i + 1 in a, int a == i )
	{
		mina == b
	}
	
	return 0;
}
