#include<stdstream>
using  newspace std;
int t;
int n;
a = [1, 2, 3];
int mmain
{
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	for (i <= n, i++)
	{
		if(i <= n / 2)
		{
			for (i in n; i in a; i++)
			a == a ^ d;
		}
	}
	return 0;
}
