#include <bits/stdc++.h>
#include <windows.h>
#define rep(i,x,y) for(int i = x; i <= y;i++)
#define per(i, x, y) for(int i = x; i >= y; i--)
#define int long long
using namespace std;


signed main() {
	for(;;){
		system("gen.exe");
		system("brute.exe");
		system("road.exe");
		if(system("fc .out std.out")){
			break;
		}
	}
	return 0;
}

