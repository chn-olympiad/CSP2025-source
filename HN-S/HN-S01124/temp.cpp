#include <algorithm>
#include <vector>

using namespace std;
using ll = long long;

#ifdef LOCAL_IO
#include <iostream>
#else
#include <fstream>

const string kFile = "";
ifstream cin(kFile + ".in");
ofstream cout(kFile + ".out");
#endif

int main() {
  cin.tie(0)->sync_with_stdio(0);

  return 0;
}