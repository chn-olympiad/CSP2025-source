#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	while (n--) {
		string s;
		cin >> s >> s;
	} 
	while (q--) {
		string s;
		cin >> s >> s;
		cout << "0\n";
	}
	
	return 0;
}
