#include<bits/stdc++.h>
using namespace std;
const int N = 1e5;
int c[N];
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	int n, m;
	cin >> n >> m;
	for(int i = 0; i < n; i++){
		string str;
		cin >> str[i];
	}
	for(int i = 0; i < n; i++){
		cin >> c[i];
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
