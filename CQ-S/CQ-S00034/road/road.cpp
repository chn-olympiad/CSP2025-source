#include<bits/stdc++.h>

using namespace std;

namespace hyh {
    const int N = 1e4, M = 1e6;
    int n, m, k;
    struct ovo {
        int x, w;
        bool operator < (const ovo d) const {
            return w > d.w;
        }
    };
    vector<ovo> e[M];
    signed main() {
        // freopen("road.in", "r", stdin);
        // freopen("road.out", "w", stdout);
        // 我故意的 我afo了 记得把我放上去哦
        cin >> n >> m >> k;
        for (int i = 1; i <= m; i++) {
            int u, v, w;
        }
        return 0;
    }
}
signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return hyh::main();
}
//决斗！
//byd我跟你报了
//started in 16:51 → /fad

//没这么冷静

//从十三年前的横空出世
//再到两年前的意外相遇
//最后是一分钟前的
//颠覆世界的一万三千亩玫瑰凋落了
//可你的长夏却永不会落下
//下一个瞬间
//也请你
//