#include <stdio.h>

int q;

int main()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);

    scanf("%d%d", &q, &q);
    while (q--)
        printf("0\n");

    return 0;
}
