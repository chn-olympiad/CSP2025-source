#include <bits/stdc++.h>
using namespace std;
fream(polygon.in);
fream(polygon.out);
int main(){
	int n, a[5000];
	int ans = 0;
	cin >> n;
	for(int i = 0;i <= n;i++) 
	{
		cin >> a[i];
		if(max(maxn, a[i]) < a[i])
		ans++;
	} 
	cout << ans;
	return 0;
}
