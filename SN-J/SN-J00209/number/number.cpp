#include<bits/stdc++.h>
using namespace;
	int main( )
{
	freopen(number.in,"r",stdin)
	freopen(number.out,"r",stdout)
   cher s;
	cin>>s;
	if(s[i]=1&&s[i]=9){
	
		for(int i=1;i<=s;i++){
		
			if(s[i]>s[j]){ 
			cout<<s[i]<<s[j];
			
		}
		else cout<<s[j]<<s[i];
		}
}
	return 0;
} 
