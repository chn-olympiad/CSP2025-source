#include <bits/stdc++.h>
using namespace std;
long long n, k, a[105], f[105];
int main()
{
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin >> n >> k;
	for (int i = 1; i <= n; i++)
	{
		cin >> a[i];
	}
//	for (int i = 1; i <= n; i++)
//	{
//		if ()
//		f[i] = max(f[i - 1], );
//	}
	cout << 2;
	return 0;
}
