#include <iostream>
#include <cstdio>
#include <string>
#include <cmath>
using namespace std;

string s;
string c;
int main()
{
	freopen("number.in","r",strin)
	freopen("number.out","w",strout)
	cin >> s;
	for(int i=0;i<=s.size();i++)
	{
		if(s[i]!='q' && s[i]!='w' && s[i]!='e' && s[i]!='r' && s[i]!='t' && s[i]!='y' && s[i]!='u' && s[i]!='i' && s[i]!='o' && s[i]!='p' && s[i]!='a' && s[i]!='s' && s[i]!='d' && s[i]!='f' && s[i]!='g' && s[i]!='h' && s[i]!='j' && s[i]!='k' && s[i]!='l' && s[i]!='z' && s[i]!='x' && s[i]!='c' && s[i]!='v' && s[i]!='b' && s[i]!='n' && s[i]!='m')
		{
			c[i]=s[i];
		}
	}
	for(int i=0;i<=s.size();i++)
	{
		if('9'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('8'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('7'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('6'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('5'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('4'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('3'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('2'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('1'==c[i])
		cout << c[i];
	}
	for(int i=0;i<=s.size();i++)
	{
		if('0'==c[i])
		cout << c[i];
	}
	return 0;
}
