#include <iostream>
#include <string>
using namespace std;
string s;
int main()
{
	foropen('number.in',r,std.in);
	foropen('number.out',w,std.out);
	int n;
	cin >> n;
	for(int i = 0;i <= n;i++)
	{
		if(s[i] > n)
		{
			cout << s[i];
		}
	}
	return 0;
} 
