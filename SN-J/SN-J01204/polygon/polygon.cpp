//SN-J01204 黄文博 汉滨区汉滨初级中学
#include <bits/stdc++.h>
using namespace std;
int n, a[100000], f[100000][100000], b[100000], dfs, ans;
int dfs(int x, int cnt)
{
	if (x==0&&cnt==0)
	{
		return 1;
	}
	if (x==0&&cut！=0)
	{
		return 0; 
	}
	if (f[x][cnt])
}
int main()
{
	freopen("polygon.in","r",stdin)
	freopen("polygon.ont","w",stdout)
	cin >> n;
	for (int i = 0;i < n;i++)
	{
		cin >> a[i];
		b[]
	} 
	for (int j = )
	return 0;
} 
