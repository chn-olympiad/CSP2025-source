#include<iostream>
using namespace;
int main(){
	feropen("xor.in","r",stdin);
	feropen("xor.out","w",stdout);
	int(n,k,l,r){
		cin >> n >> k >> r;
		for(1 <= i <= n,i++){
			a = 1;
		}
		for(1 <= i <= n,0 <= a <= 1,i++){
			cout << a << endl;
		}
		for(1 <= i <= n,0 <= a <= 255,i++){
			cout << a << endl;
		}
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
