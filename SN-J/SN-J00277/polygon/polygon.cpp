#include<iostream>
using namespace;
int main(){
	feropen("polygon.in","r",stdin);
	feropen("polygon.out","w",stdout);
	int(n,a,g){
		cin >> n >> a >> g;
	    n >= 3;
	    basic_string::length(n,a);
		count << g << endl;
		
	} 
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
