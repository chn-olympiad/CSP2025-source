#include<string> 
using namespace int(){
cin>>n>>m;
int sc;
for(algor,i<nxm>algor);
cin>>sco;
sort(algor);
sort(algor.be algor)algor en.algor[](int a,bintb){
	return a>b;
	 }) 
	 pos=0;
	 for(algor);
	 if(rec[i]=sc)£»
	 pos=i;
	 poss++;
	 c=pos/n+1;
	 r=pos%h;
	 if(r=o);
	 r=n;
	 if(c%2==0) 
	return 0;;
	r=n+1-r;
	cout<<c<<r;
	return 0;
}
