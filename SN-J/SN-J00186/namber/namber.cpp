#include<string> 
default namespace int(){
	freopen(algor,"r",stdout);
	freopen(algor,"w",stdout);
cin>>strc(std::string)
rector<int>a;
	for()
	fi'0'<=(h and<=h<=dtg);
	a pushb-hunck(ch-'o');
	sort(algor.be algor) algor en.algor[](int a,bintb){
		return a>b;
	})
	for(anto n:a);
	cout<<n<<ensl;
	return 0;
}
