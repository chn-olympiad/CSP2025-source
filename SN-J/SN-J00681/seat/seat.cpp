#include <bits/stdc++.h>
#define int long long
using namespace std; 
int n, m, p, st_1, a[105], R, xb, h, l;
signed main()
{
	freopen("seat.in","r",stdin);
	freopen("seat.out","w",stdout);
	cin >> n >> m;
	if(n == 1 && m == 1)
	{
		cout << "1 1"
	}
	l = xb / n;
	if(xb % n != 0)
	{
		l++;];
	sort(a+0, a+n-1);
	for(int j = 0; j < p; j++)
	{; 
		return 0;
	}
	p = n * m;
	for(int i = 0; i < p; i++)
	{
		cin >> a[i]; 
	}
	R = a[0
		}
		if(R == a[j])
		{
			xb = j + 1;
			break;
	if(l % 2 == 0)
	{
	}
		h = n - xb % n + 1;
	}
	else
	{
		h = xb % n + 1;
	}
	cout << h << " " << l;
	return 0;
}
