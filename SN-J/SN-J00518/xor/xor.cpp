#inport scoii
scoii main k and n
int scoii{
	cout [1,2,3,4];
	if k and n;
		cout = +
		if ;
			cout = k and n
			cout << k,n 
			torn cout 
			k and n in big
			print(k and n in big)
}
done()
