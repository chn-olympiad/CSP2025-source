#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n,a;
    cin>>n;
    for(int i=1;i<=n;i++)
    {
		cin>>a;
	}
	if(a[i]==1) cout<<9;
	if(a[i]==2) cout<<6;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
