#include<bits/stdc++.h>
using namespace std;
int a[l][r];

int main(){
            freopen(xor,"r","stdin");
            freopen(xor,"w","stdout");
            string b;
            cin>>a>>b>>endl;

            int a2[l-1][r];
            for(int j=0;j<=b;j++){
                        if(a[l][r]<a[l+1][[r+1]){
                             int sum=0;
                                    a[l+1][r];
                                    a2+=a;
                                    sum+=a2;
                        }

            }
            cout<<sum;
            sclose(stdin);
            sclose(stdout);
	return 0;
}
