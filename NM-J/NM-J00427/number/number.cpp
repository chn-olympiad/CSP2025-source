#include<bits/stdc++.h>
using namespace std;
string s;

int main(){
            freopen(number,"r","stdin");
            freopen(number,"w","stdout");
            cin>>s;
            sort(s,s+1);
            cout<<s;
            sclose(stdin);
            sclose(stdout);
	return 0;
}
