#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    long long n;
    long long a[10005];
    for(int i = 0;i<n;i++){
        cin >>a[i];
    }
    cout << n*2; 
    fclose(stdin);
    fclose(stdout);
    return 0;

}
