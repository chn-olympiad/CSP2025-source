#include <bits/stdc++.h>
using namespace std;

int main(){
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
    int n,m;
    cin >> n >> m;
    long long a[n+5];
    for(int i=0;i<n;i++){
    	cin >> a[i];
	}
	cout << m;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
