#include <iostream>

using namespace std;

long long a=100000000000;
int main()
{
    freopen("number.in","r",stdin);
    freopen("number.out","w",stdout);
    cin>>a;
    if(a!=0)
    {
        cout<<9876543210;
    }
    cout<<a;
    fclose(stdin);
    fclose(stdout);

    return 0;
}
