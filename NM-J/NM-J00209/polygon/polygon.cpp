#include <iostream>

using namespace std;

int main()
{
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int a;
    cin>>a;
    int b[a];
    for(int i=0;i<a;i++)
    {
        cin>>b[i];
    }
    cout<<8;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
