#include <bits/stdc++.h>
use namespace std;
int s[1001];
int main(){
    freopen("seat.in","r",stdin);
    freopen("seat.out","w",stdout);
    int n,m;
    cin>>n>>m;
    for(int i = 0 ;i<=n*m ;i++){
        cin>>s[i]
    }
    cout<<1<<' '<<1;
    fclose(stdin);
    fclose(stdin);
    return 0;
}
