#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int n;
    cin>>n;
    vector<int>arr(n+10);
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    cout<<"9";
    fclose(stdin);
    fclose(stdout);
    return 0;
}
