#include <bits/stdc++.h>
using namespace std;

int main() {


	int s;
	cin << s;
	freopen("number.in", "r", "stdin");
	freopen("number.out", "w", "stdout");
	s[number];
	for (int s = 1; s <= 9; s++);
	while (s.size())
		cout << s
	}
return 0;
