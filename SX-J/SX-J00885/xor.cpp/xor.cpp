#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor.in", "r", "stdin");
	freopen("xor.in", "w", "stdout");
	int k;
	[l, r] (1 <= l <= r <= n)
	[l1, r1], [l2, r2]
	1 <= i <= n(l1 <= i <= r1 || l2 <= i <= r2)
	if [2, 1, 0, 3]
		k == 2;
	choose[1, 1]&&[2, 4]
	cout << 2 && 1  0  3 = 2;
	if k == 3;
choose[1, 2]&&[4, 4]
	cout << 1 2 = 3 && 3;
	while (k.size());
	cout << k.pop
}
return 0;

