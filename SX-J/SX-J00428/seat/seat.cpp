#include <bits/stdc++.h>
using namespace std;
int a[10000];

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m, s;
	s = n * m;
	for (int i = 0; i <= s; i++) {
		cin >> a[i];
		int xiaoyang = a[i + 1];
	}
	return 0;
}