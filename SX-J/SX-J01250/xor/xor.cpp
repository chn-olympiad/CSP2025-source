#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n, m;
ll h = 1, l = 1;
ll a[110];

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> m;
	for (ll i = 1; i <= n; i++) cin >> a[i];
	cout << m;
	return 0;
}
