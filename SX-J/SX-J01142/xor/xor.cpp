#include <bits/stdc++.h>
using namespace std;
long long n, m, a[10005], pre[10005], mmax;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> m;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	cout << 2;
	return 0;
}

