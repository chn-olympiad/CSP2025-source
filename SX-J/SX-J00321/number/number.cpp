#include <bits/stdc++.h>
using namespace std;
int a[10000];

int main() {
	//freopen("number.in","r",stdin)
	//freopen("number.out","w",stdout)
	int sin=0;
	char f[x];
	for (int i = 0; i < x; i++) {
		cin >> f[i];
	}
	for (int i = 0; i < n; i++) {
		if (f[i] == a[10000]) {
			;
		}
	}
	return 0;
}
