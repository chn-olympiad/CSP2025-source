#include <bits/stdc++.h>
using namespace std;
const int N = 1e5+5;
int n, k, a[N];

int main() {
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	if (n <= 2) {
		cout << 1;
		return 0;
	}
	return 0;
}
