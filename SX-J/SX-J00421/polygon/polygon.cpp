#include <bits/stdc++.h>
using namespace std;

int main()  {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int  n, m; //x y
	cin >> n >> m ;
	int  x[n], y[m], q[100];
	cin >> q[];
	for (int i = 0; i < n; i++) {
		for (int a = 0; a < m; a++) {
		}
	}


	return 0;
}
