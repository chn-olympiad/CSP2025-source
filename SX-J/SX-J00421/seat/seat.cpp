#include <bits/stdc++.h>
using namespace std;

int main()  {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int  n, m; //x y
	cin >> n >> m ;
	int  x[n], y[m], q[100];
	for (int i = 0; i < n; i++) {
		for (int a = 0; a < m; a++) {
			cout << "1 2";
		}
	}
	return 0;
}
