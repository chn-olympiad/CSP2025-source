#include <bits/stdc++.h>
using namespace std;
int a[1000 + 5];

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int i = 1;
	string s;
	cin >> s;
	while (s.size() > 0) {
		if (s.size() == 1) {
			cout << s;
			break;
		}
		cout << 92100;
	}
	return 0;
}
