#include <bits/stdc++.h>;
using namespace std;
int b[5005];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int n, sum = 0;
	cin >> n;
	for (int i = 1; i <= n; ++i) {
		cin >> b[i];
	}

	printf("");
	fclose(stdin);
	fclose(stdin);
	return 0;
}