#include <bits/stdc++.h>;
using namespace std;
int b[100005];

bool f(int x, int y) {
	return x > y;
}

int main() {
	//freopen("xor.in","r",stdin);
	//freopen("xor.out","w",stdout);
	int n, k, sum = 0;
	cin >> n >> k;
	for (int i = 1; i <= n; ++i) {
		cin >> b[i];
	}

	printf("1");
	fclose(stdin);
	fclose(stdin);
	return 0;
}