#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	long long l, h, y, x, a, n;
	cin >> l;
	cin >> h;
	cin >> y;
	cin >> x;
	cin >> a;
	cin >> n;
	if (h = 2) {
		cout << "2";
	} else
		cout << "2";

	return 0;
}