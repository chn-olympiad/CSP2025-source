#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	long long l, h, y, x, a, n;
	cin >> l;
	cin >> h;
	cin >> y;
	cin >> x;
	cin >> a;
	cin >> n;
	if (h = 1) {
		cout << "9";
	} else
		cout << "6";

	return 0;
}