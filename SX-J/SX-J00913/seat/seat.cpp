#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	long long l, h, y, x, a, n;
	cin >> l;
	cin >> h;
	cin >> y;
	cin >> x;
	cin >> a;
	cin >> n;
	if (y = 99) {
		cout << "1 2";
	} else
		cout << "2 2";

	return 0;
}
