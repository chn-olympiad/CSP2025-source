#include <bits/stdc++.h>
using namespace std;
vector<string>c;
long long a = 3465;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);

	for (int i = 1; i <= a; i++) {
		if (a >= 0 && a < 10) {
			long long	c = a;
		}
	}
	long long s;
	cin >> s;
	cout << s;

	return 0;
}
