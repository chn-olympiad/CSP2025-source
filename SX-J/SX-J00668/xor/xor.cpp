#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	ll n, k;
	cin >> n >> k;
	if (n <= 2 && k == 0) {
		cout << 0;
		return 0;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
