#include <bits/stdc++.h>
using namespace std;

int main() {
	//freopen("number.in","r"stdin);
	//freopen("number.out","w"stdout);
	char s;
	cin >> s;
	int a = 0;
	int b = 0;
	for (int i = 0; i < s; i++) {
		for (int j = 0; j < s; j++) {
			if (s != 'a~z' || s == '0~9') {

			}
		}
	}
	for (int i = 0; i < s; i++) {
		for (int j = 0; j < s; j++) {
			cout << a << b;
		}
	}
	//fclose(stdin);
	//fclose(stdout);
	return 0;
}