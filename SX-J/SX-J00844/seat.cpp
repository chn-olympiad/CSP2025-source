#include <bits/stdc++.h>
using namespace std;

int main() {
	ifstream fin("seat.in"cpp);
	ofstream fout("seat.out"cpp);
	int n, m;
	cin >> n >> m;
	int a1, b[1001];
	for (int i = 0; i < b[1001]; i++) {
		fin >> b[i];
	}
	return 0;
}
