#include <bits/stdc++.h>
using namespace std;
int s[500005];

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	int n;
	long long k;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> s[i];
	}
	cout << 16;
	return 0;
}