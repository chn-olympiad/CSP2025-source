#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	char s[1000000];
	string n;
	cin >> s;
	for ( int  i = 0; i < s[10000000] ; i++) {
		if (s[10000000] ==  1 || 2 || 3 || 4 || 5 || 6 || 7 || 8 || 9 || 0)

			n = s;
	}
	cout << n << endl;
	return 0;
}