#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);

	freopen("seat.out", "w", stdout);
	int n, m, h[1000], c, r;
	cin >> n >> m;
	for (int i = 0; i <= n * m; i++) {
		cin >> h[i];
	}

	return 0;
}