#include <bits/stdc++.h>
using namespace std;
long long n, m, a[1005]

int main() {
	//freopen("seat.in","r",stdin)
	//freopen("seat.out","w",stdout)
	cin >> n >> m;
	for (int i = 1; i <= n * m; i++) {
		cin >> a[i];
	}
	return 0;
}
