#include <bits/stdc++.h>
using namespace std;
char s;

int main() {
	freopen("number.in", "r", stdin);
	fre("number.out", "w", stdout)
	cin >> s;
	int lens = strlen(s);
	int a[lens + 1];
	for (int i = 1; i <= lens; i++) {

	}
}