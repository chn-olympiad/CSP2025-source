#include <bits/stdc++.h>
using namespace std;
int a[1000000];
int b[1000000];
int m;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	cin >> m;
	cout << m;
	//string s;
	//cin >> s;
	//int n = strlen(s);
	/*	for (int i = 1; i <= 10000000; i++) {
			cin >> a[i];
		}
		for (int i = 1; i <= 10000000; i++) {
			if(a[i]>0 && a[i]<0)
			b[i] = a[i];
			}
	  	int m =sort( b[0], b[x]);
	  	cout << m;*/
	return ;
}
