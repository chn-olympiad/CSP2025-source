#include <bits/stdc++.h>
using namespace std;

int main() {
	string a[100];
	for (int i = 1; i <= size(a); i++) {
		cin >> a[i];
		if (a[i] >= "0" && a[i] <= "9") {
			
		}
	}

	return 0;
}
