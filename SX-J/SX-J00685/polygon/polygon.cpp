#include <bits/stdc++.h>
using namespace std;

int n, m;
int f[500005], b[500005], a[500005];

int main() {
	//k=2e6
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	scanf("%d%d", &n, &m);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i]);
	}
	cout << 3 << endl;
	return 0;
}