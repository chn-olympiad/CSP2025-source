#include <bits/stdc++.h>
#include <string>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);

	string s;
	int i, d;
	int a[] = {s};
	int b[] = {};
	int c;
	c = string lenth(s);
	if (c == 1)
		cout << s;
	else
		for (i = 0; i <= c; i++) {

		}
	return 0;
}
