#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int a[] = {};
	int n = 0, k = 0, l, r;
	cin >> n;
	cin >> k;
	for (int i = 0; i <= n; i++) {
		cin >> a[i];
	}
	return 0;
}
