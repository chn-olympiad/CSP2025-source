#include <bits/stdc++.h>
using namespace std;
int n, k, maxinum, xori[500005], m;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> k;
	for (int i = 0; i < n; ++i)
		cin >> xori[i];
	cout << 2;
	return 0;
}
