#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor.in", "r", "stdin");
	froepen("xor.out", "r", "stdout");
	int n, k;
	cin >> n >> k;
	int a[100005];
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	return 0;
}