#include cdits/stdc++.h>
using namespace std;
int main{
	int a;
	cin >> a;
	int m;
	int b;
	for (a) {
		cin >> b;
		m = m + b
	}
	int n = max(m);
	for (m) {
		if (m - n == len(m)) {
			m = n + len(m) - 1
		}

	}
	cout << m;
	return 0;
}

