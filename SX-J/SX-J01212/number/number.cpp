#include cdits/stdc++.h>
using namespace std;

int main() {
	int s;
	cin >> s;
	int b = "";
	for (a << s) {
		if (a == 1 && 2 && 3 && 4 && 5 && 6 && 7 && 8 && 9 && 0) {
			b = b + str(a);
	
		}
	}
	int b;
	int c;
	for(i<in<b){
		c = c+max(b);
		b = b.rmove(max(b));
	}
	
	cout<<c;
	
	return 0;
}