#include <bits/stdc++.h>
using namespace std;
long long n, a[5005], sum, mmax = INT_MIN, num;

int main() {

	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);

	ios::sync_with_stdio(false);
	cin.tie(0), cout.tie(0);

	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 9;

	return 0;
}
