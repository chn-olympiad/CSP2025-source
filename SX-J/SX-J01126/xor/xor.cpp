#include <bits/stdc++.h>
using namespace std;

int main() {
	//freopen("xor.in", "r", stdin);
	//freopen("xor.out", "w", stdout);
	int a, q, w, e, r, t, y, u, i, o;
	cin >> a >> q >> w >> e >> r >> t;

	if (q == 0  ) {
		cout << "1";
	} else {
		cout << w;
	}


	return 0;
}
