#include <bits/stdc++.h>
using namespace std;

int main() {
	int a[100], n = 0, k = 0;
	cin >> n, k;
	for (int i = 0; i < k; i++) {
		cin >> a[i];
		m = a[i];

	}
	cout << m;
	freopen("seat.in", "r", stdin);
	freopen("seatout", "w", stdout);
}