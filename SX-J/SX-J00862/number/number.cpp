#include <bits/stdc++.h>
using namespace std;

int main() {
	int a;
	cin >> a;
	if (a % 2 == 0 || a % 2 != 0) {
		cout << a;
	}
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);

}
