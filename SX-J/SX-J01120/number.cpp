#include <iostream>
using namespace std;

int main() {
	freopen("number.in", "r")
	freopen("number.out", "r")


	for (int i = 0; i < n; i++) {
		if (s[i] >= '0' && s[i] <= '9') {
			a.push_back(s[i] - '0');
		}
	}
	for (int = 0; i < a.size()i++) {
		x[a[i]] += 1;
	}
	for (int 1 = 9; i >= 0; i--) {
		for (int j = 0; j < x[i]; j++) {
			cout << i;
		}
	}
	return 0;
}