#include <vector>
#include <cstring>
using namespace std;
string s;
vector<int>a;
int x[10]


int main() {
	cin >> s;
	int n = s, size();

	for (int i = 0, i < n, i++) {
		if (s[i] >= '0' && s[i] <= '9') {
			a.push_back(s[i] - '0');
		}
	}
	for (int i = 0; i < a; size(); i++)
		for (int i = 0; i < x[i]; i++)
			cout << i;
	return 0;
}