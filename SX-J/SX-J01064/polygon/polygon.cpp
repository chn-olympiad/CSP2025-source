#include <bits/stdc++.h>
using namespace std;
int n, x;
int a[1005];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> x;
		a[i]++;
	}
	cout << 9;
	return 0;
}
