include < bits / stdc++.h >
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int n, sum = 0;
	bool a;
	srting s;
	for (int i = 1; i <= n; i++) {
		if (s[i] >= a[i]) {
			sum += s[i]
		} else {
			sum += a[i];
		}
	}
	cout << sum;
	return 0;
}