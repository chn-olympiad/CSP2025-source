#include<bits/stdc++.h>
using namespace std;
int main(){
	/*freopen(number.in,"r",stdin);
	freopen(number.out,"w",stdout);*/
	string s;
	int a,b;
	cin>>s;
	int n=s.size();
	for(int i=0;i<n;i++){
		if(int(s[i])>=0&&int(s[i])<=9){
			b==int(s[i]);
		}sort(b,b+n);
		cout<<b;
	}
	return 0;
}