#include <bits/stdc++.h>
using namespace std;

int main() {
	//freopen("number.in", "r", stdin);
	//freopen("number.out", "w", stdout);
	string s;
	cin >> s;
	for (int i = 0; 1; i++) {
		cin >> s[i];
	}
	sort(s, s + i);
	for (int i = 0; 1; i++) {
		cout << s[i];
	}
	return 0;
}