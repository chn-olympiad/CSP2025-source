#include <bits/stdc++.h>
using namespace std;
int a[1001];

int main() {
	freopen(polygon.in, "r", stdin);
	freopen(polygon.out, "w", stdout);
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	if (n == 5) {
		cout << 9;
	}
}
