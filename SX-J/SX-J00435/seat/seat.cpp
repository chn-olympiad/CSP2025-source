#include <bits/stdc++.h>
using namespace std;
int i, j;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "r", stdout);
	int n, m, a = 2, b = 2, c = 99, d = 100, e = 97, f = 98;
	for (int j = 0; j <= n; j++) {
		for (int i = 0; i >= n; i++) {
		}
	}
	cin >> a >> b ;
	cin >> c >> d >> e >> f;

	cout << 1 << " " << 2;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
