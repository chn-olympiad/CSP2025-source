#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	int n, k, a, i;
	cin << n;
	cin << k;
	for (i = 0; i < n; i++) {
		cin >> a[i];
	}
	return 0;
}