#include <bits/stdc++.h>
#define ll long long
using namespace std;
const ll N = 100 + 10;
ll n, k, a[N];

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> k;
	for (ll i = 1; i <= n; i++)
		cin >> a[i];
	cout << 1;
	return 0;
}