#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen(polygon.in, "r", stin);
	freopen(polygon.out, "w", stout);
	int a, b;
	cin >> a;
	for (int i = 1; i <= a; i++) {
		cin >> b;
	}
	cout << -1;

























	return 0;
}
