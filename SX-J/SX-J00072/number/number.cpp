#include <bits/stdc++.h>
using namespace std;

//int cmp(int a, int b) {
//	if (a > b)
//		return a;
//}

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	char a[100005];
	cin >> a;
//	int sum = 0, d = 1;
//	int len = strlen(a);
//	for (int i = 0; i < len; i++) {
//		if (a[i] >= '0' && a[i] <= '9') {
//			sum = sum * 10 + a[i];
//			d *= 10;
//		}
//	}
//	sort(sum / d, sum % d, cmp);
//	cout << sum;
	cout << a;


	return 0;
}
