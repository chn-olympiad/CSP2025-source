#include <bits/stdc++.h>
using namespace std;
int n;

struct edge {
	int fa;
	int sum;
};
int dp[5010][5010];
int a[5010];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	for (int i = 1; i <= n; i++) {

	}
	return 0;
}
