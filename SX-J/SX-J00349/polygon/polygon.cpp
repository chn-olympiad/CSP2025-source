#include <bits/stdc++.h>
using namespace std;
long long ans = 0;
int n;
int a[5050];
long long f[5050];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	cout << 366911923;
	return 0;
}
