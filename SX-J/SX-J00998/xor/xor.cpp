#include <bits/stdc++.h>
using namespace std;
int a, s, d, f, g, h;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> a >> s;
	cin >> d >> f >> g >> h;
	if (s != 0) {
		cout << "2";
	} else(s == 1) {
		cout << 1;
	}
	return 0;
}
