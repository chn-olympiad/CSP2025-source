#include <bits/stdc++.h>
using namespace std;
int n, a[5010];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polyong.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 9;
	return 0;
}