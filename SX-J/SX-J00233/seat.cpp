#include <bits/stdc++.h>
using namespace std;

int main() {
	//peropen("number.in","r",stdin)
	//peropen("numder.out","w",stdout)
	int n, m, y;
	int a[n * m - 1], c, r, a1, b, f;
	cin >> n >> m;
	cin >> a[n * m - 1];
	if (a1 = a[n])
		cout << 1 << n;
	if (a1 = a[b * n + f])
		cout << b + 1 << f;
}

