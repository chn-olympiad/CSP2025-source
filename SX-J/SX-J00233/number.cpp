#include <bits/stdc++.h>
using namespace std;

int main() {
	//peropen("number.in","r",stdin)
	//peropen("number.out","w",stdout)
	int s, c, m, N, t, Y, i = 0;
	int a[1000][1000], b[100];
	cin >> m;
	cin >> s;
	for (int i = 0;
	        if i < 	c * m; i++)
			cin >> b[i];
	if (i = 0)
		N = b[i];
}

for (int i = 0, i < c * m, i++) {
	for (int j = 1, j < i, j++)
		if (a[j > ]b[j])
			seap(b[i], b[j])
		}

for (int i, i < c * m, i++) {
	if (b[i] = N)
		t = i;
}
Y = t / m;

if (t % 2 == 0) {
	y++;
}
cout << y;
if (t % 2 == 0)
	cout << j;




