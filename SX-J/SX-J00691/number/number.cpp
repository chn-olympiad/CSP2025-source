#include <bits/stdc++.h>
using namespace std;
int ma = 0, ch, a[1000005];
string s;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	/*getline(cin, s);
	int l = strlen(s);
	for (int i = 1; i <= l; i++) {
		a[i] = s[i];
		if (a[i] < "0" || a[i] > "9") {
			char(a[i]) = q;
		}
		if (a[i] <= a[i + 1]) {
			swap(a[i], a[i + 1]);
		}
	}
	for (int i = 1; i <= l; i++) {
		cout << a[i];
	}*/
	cin >> s;
	cout << s;
	return 0;
}