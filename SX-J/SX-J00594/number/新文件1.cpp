#include <bits/stdc++.h>
using namespace std;
int a[100010], s;

int main() {
	//freopen("number.in","r",stdin)
	//freopen("number.out","w",stdout)
	cin >> s;
	string len = s.sze;
	for (int i = 1; i <= len; i++) {
		if (a[i] >= '0' && a[i] <= '9')
			cout << a;
			for (int i = 1; i <= len; i++) {
		if (a[i] >= '0' && a[i] <= '9')
			cout << a;
	}
	}
	return 0;
}