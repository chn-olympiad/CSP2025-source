#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int n[10005], m;
	for (int i = 1; i <= 50005; i++) {
		cout << n[i];
	}
	fcluse(stdin);
	fcluse(stdout);
	return 0;
}