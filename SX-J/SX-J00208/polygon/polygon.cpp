#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int n[10005];
	for (int i = 3; i <= 5000; i++) {
		cout << n[i];
	}
	fcluse(stdin);
	fcluse(stdout);
	return 0;
}