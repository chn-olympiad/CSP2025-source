#include <iostream>
using namespace std;
int n = 6666666;

/*

char w = 0;
int e, r, t, c, u, l, q, b, j, k;
int e1, r1, t1, c1, u1, l1, q1, b1, j1, k1;
int e2 = 1, r2 = 1, t2 = 1, c2 = 1, u2 = 1, l2 = 1, q2 = 1, b2 = 1, j2 = 1;
int e3, r3, t3, c3, u3, l3, q3, b3, j3;
int g;
int y;*/
int a = {444};
int s  [6];
int main() {
	cin >> s[1];
	cout << s;
	//freopen("number.in","r",stdin);
	//freopen("number.out","w",stdout);
	/*
		cin >> s;
		y = s;

		for (int i = 0; i < s; i++) {

			y %= 10;
			if (y == '1') {
				e *= 10;
				e += 1;
			}
			if (y == '2') {
				r *= 10;
				r += 2;
			}
			if (y == '3') {
				t *= 10;
				t += 3;
			}
			if (y == '4') {
				c *= 10;
				c += 4;
			}
			if (y == '5') {
				u *= 10;
				u += 5;
			}
			if (y == '6') {
				l *= 10;
				l += 6;
			}
			if (y == '7') {
				q *= 10;
				q += 7;
			}
			if (y == '8') {
				b *= 10;
				b += 8;
			}
			if (y == '9') {
				j *= 10;
				j += 9;
			}
			if (y == 0) {
				k++;
			}
		}
		while (e) {
			e /= 10;
			e1++;
		}
		while (r) {
			r /= 10;
			r1++;
		}
		while (t) {
			t /= 10;
			t1++;
		}
		while (c) {
			c /= 10;
			c1++;
		}
		while (u) {
			u /= 10;
			u1++;
		}
		while (l) {
			l /= 10;
			l1++;
		}
		while (q) {
			q /= 10;
			q1++;
		}

		while (b) {
			b /= 10;
			b1++;
		}
		while (j) {
			j /= 10;
			j1++;
		}
		for (int i; i < k; i++)
			e2 *= 10;
		e3 = e * e2;
		for (int i; i < k + e1; i++)
			r2 *= 10;
		r3 = r * r2;
		for (int i; i < k + e1 + r1; i++)
			t2 *= 10;
		t3 = t * t2;
		for (int i; i < k + e1 + r1 + t1; i++)
			c2 *= 10;
		c3 = c * c2;
		for (int i; i < k + e1 + r1 + t1 + c1; i++)
			u2 *= 10;
		u3 = u * u2;
		for (int i; i < k + e1 + r1 + t1 + c1 + u2; i++)
			l2 *= 10;
		l3 = l * l2;
		for (int i; i < k + e1 + r1 + t1 + c1 + u2 + l2; i++)
			q2 *= 10;
		q3 = q * q2;
		for (int i; i < k + e1 + r1 + t1 + c1 + u2 + l2 + q2; i++)
			b2 *= 10;
		b3 = b * b2;
		for (int i; i < k + e1 + r1 + t1 + c1 + u2 + l2 + q2 + b2; i++)
			j2 *= 10;
		j3 = j * j2;
		g = e3 + r3 + t3 + c3 + u3 + l3 + q3 + b3 + j3;
		cout << g;
		//fclose(stdin);
		//fclose(stdout);
	*/
	return 0;
}


