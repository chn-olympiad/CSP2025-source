#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m;
	int a[n][m];
	for (int i = 0; i < m; i++) {
		for (int j = i; j < m; j++) {
			cin >> a[i][j];
		}
	}
	cout << 1 << 2 << endl;
	return 0;
}
