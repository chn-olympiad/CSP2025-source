#include <bits/stdc++.h>
using namespace std;
int a[500005], sum1, sum2;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	long long n, k;
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 1;
	return 0;
}