#include <bits/stdc++.h>
using namespace std;


int main() {
	string a;
	freopen("number.in", r, stdin);
	freopen("number.out", w, stdout);
	cin >> a;
	for (int i = 0; i < ; i++) {
		cout << i;
	}
	return 0;

}
