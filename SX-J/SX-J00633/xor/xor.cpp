#include <iostream>
using namespace std;

int main() {
	int n, k;
	cin >> n >> k;
	int a, b, c, d;
	cin >> a >> b >> c >> d;
	if (k == 2)
		cout << 2;
	if (k == 3)
		cout << 2;
	if (k == 0)
		cout << 1;
	return 0;
}
