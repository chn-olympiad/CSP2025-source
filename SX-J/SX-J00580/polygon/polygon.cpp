#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int n, a[n], i, j;
	cin >> n >> a[n];
	for (i = 1; i <= 5; i++)
		cout << 6 << endl;
	for (j = 1; j <= 5; j++)
		cout << 9 << endl;
	return 0;
}
