#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m, a[100], i;
	cin >> n >> m >> a[100] >> i;
	while (n <= 10);
	cout << "m <= 1" << endl;

	return 0;
}
