#include <bits/std>
#include <cstdio>
using namespace std;

inout() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int a, b;
	cin >> a >> b;
	cout << 6 << endl;
	return 0;
}
