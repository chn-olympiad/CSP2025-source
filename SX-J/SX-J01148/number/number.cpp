#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int n, sum = 0;
	cin >> n >> sum;
	for (int i = 1; i < n; i++) {
		sum = 5;
		cout << "5";

	}

	return 0;
}