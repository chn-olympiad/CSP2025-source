#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m, sum = 0;
	cin >> n >> m >> sum ;
	for (int i = 1; i < n; i++) {
		sum = n;
		n > m;
		cout << " ";

	}





	return 0;
}