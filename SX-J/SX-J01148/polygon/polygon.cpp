#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int n, sum = 0;
	cin >> n >> sum;
	for (int i; i < n; i++) {
		sum = 5;
		cout << "5";



	}

	return 0;
}