#include <bits/stdc++.h>
using   namespace std;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.in", "w", stdout);
	int a[100];

	for (int i = 1; i <= 9; i++) {
		cin >> a[i];
	}
	sqrt(a, a + 1);
 
	return 0;
}