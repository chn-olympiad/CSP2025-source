#include <iostream>
#include <bits/stdc++.h>
using namespace std;
string s;
int a[11];

int main() {
	freopen(polygon.in, "r", stdin);
	freopen(polygon.out, "w", stdout);
	int b, c;
	cin >> b;
	for (int i = 1; i <= b; ++i) {
		cin >> c;
	}
	cout << 1;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
