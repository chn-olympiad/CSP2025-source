#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("numbre.in", "r", stdin);
	freopen("number.out", "w", stdout);
	string s, msxn = -1, mini = 10000, a[10000];
	cin >> s ;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}

	cout <<  << endl;
	return 0;
}