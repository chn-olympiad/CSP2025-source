#include <bits/stdc++.h>
using namespace;

int main() {
	long long s, n;
	freopen("number.in", "r", stdent);
	freopen("number.out", "s", stdent);
	for (int s, s != 1000, n <= s, n++) {
		if (n >= "n") {
			cout << "n" << endl;
		} else {
			cout << n << endl;
		}
	}
	retrun 0;
}
