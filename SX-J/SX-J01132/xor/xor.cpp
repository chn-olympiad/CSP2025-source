#include <bits/stdc++.h>
using namespace std;
int n, k;
int qujian[500005];

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> qujian[i];
	}
	cout << 2;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
