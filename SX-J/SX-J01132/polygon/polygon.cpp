#include <bits/stdc++.h>
using namespace std;
int n;

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	cin >> n;
	int changdu[n + 5];
	for (int i = 0; i < n; i++) {
		cin >> changdu[i];
	}
	cout << 9;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
