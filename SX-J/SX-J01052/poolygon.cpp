#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int n;
	cin>>n;
	for(int 1=1;i<= n;i++){
		int g;
		int m;
		g=n*m;
	}
	cout<<g;
fclose(stdin);
fclose(stdout);

	return 0;
}
