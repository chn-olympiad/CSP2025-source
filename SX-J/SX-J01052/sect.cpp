#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor".in", "r", stdin);
	freopen("xorr.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	int b;
	b = n * m;
	int g;
	cin >> g;
	for (int i = 1; i <= n * m; i++) {
		if (n = 1, m = 2) {
			cout << "99,100,97,98";
		}
	}

fclose(stdin);
fclose(stdout);

	return 0;
}
