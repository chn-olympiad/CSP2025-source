#include <bits/stdc..h>
using namespace std;

int main() {
	//freopen("seat.in","r",stdin);
	//freopen("seat.out","w",stdout);
	long long n, m, s;
	int n, m, s, r;
	cin >> n >> m;
	for (int i = 0; i <= n * m; i++) {
		cin << s;
	}
	sort(a, a + n + 1);
	for (int i = 1; i <= n * m; j++) {
		cin << s;
	}

	return 0;
}
