#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "W", stdout);
	long long int n, m, ai;
	int a, b;
	cin >> n, m;
	int [q + 10];
	long long int q = n * m;
	for (int i = 0; i < q; i++) {
		cin >> ai;
		ai = [q + 10];
		if (i == 0 && ai == [i]) {
			a = 1;
			b = i;
		}
	}
	cout << a << " " << b;
	return 0;
}
