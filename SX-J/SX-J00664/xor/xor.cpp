#include <bits/stdc++.h>
using namespace std;
const int N = 5e5 + 5;
int n, k;
int a[N];

int main() {
//	freopen("xor.in", "r", stdin);
//	freopen("xor.out", "w", stdout);
//	ios::sync_with_stdin(false);
//	cin.tie(nullptr);
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << a[1];
	return 0;
}