#include <bits/stdc++.h>
using namespace std;

struct o {
	int St = -1, Ed = -1;
};

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	int n, k, os_num = 1;
	cin >> n >> k;
	int d;
	cin >> d;
	cout << (d == k ? 1 : 0);



	return 0;
}

