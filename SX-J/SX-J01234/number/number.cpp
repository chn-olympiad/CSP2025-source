#include <bits/c++>.h

using namespace std;
const int N = 1e5;
int h[N];

freopen("number.in", "r", stdin);
freopen("number.out", "w", stdout);

int main() {
	string s;
	cin >> s;





















	return 0;
}