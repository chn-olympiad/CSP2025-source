#include <bits/c++.>


using namespace std;
const int N = 1e5;
int h[N];

freopen("xor.in", "r", stdin);
freopen("xor.out", "w", stdout);

int main() {
	int n, k;
	cin >> n >> k;
	for (int i = 0; i <= n; i ++) {
		cin >> h[i];
		if (k == 2) {

		}
	}

















	return 0;
}