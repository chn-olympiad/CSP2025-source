#include <bits/c++>.h

using namespace std;
const int N = 1e5;
int h[N];

freopen("seat.in", "r", stdin);
freopen("seat.out", "w", stdout);

int main() {
	int n, m;
	cin >> n >> m;
	int c, r;
	for (int i = 0; i <= n * m; i ++) {
		cin >> h[i];

	}
















	return 0;
}