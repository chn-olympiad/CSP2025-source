#include <bits/stdc++.h>
using namespace std;
int n, m;
int a[100];

int main() {
	freopen("seat.in", "w", stdin);
	freopen("seat.out", "s", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n * m; i++) {
		cin >> a[i];
	}
	cout << 1 << " " << 2;
	return 0;
}