#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int s, n, m;
	cin >> s;
	for (int i; i <= s; i++) {
		n <= 10000;
		m = 0;
	}
	cout << s;
	return 0;
}