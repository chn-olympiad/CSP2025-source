#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n, arr[5005];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	cin >> n;
	for (int i = 1; i <= n; i++)
		cin >> arr[i];
	cout << 9;
	return 0;
}
