#include <bits/stdc++.h>
using namespace std;
int s, a[1000], Max_maxx;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	string s, Max, a;
	cin >> s;
	for (int i = 1; i <= 1000; i++) {
		if (s > Max) {
			s == Max;
		} else {
			Max == s;
		}
	}
	cout << s;
	return 0;
}
