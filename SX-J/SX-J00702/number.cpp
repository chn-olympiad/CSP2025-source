#include <bits/stdc++.h>
using namespace std;
int tong[15];

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	string s;
	cin >> s;
	for (int i = 0 && i < 10; i++) {
		if ()
		}
	treurn 0;
}