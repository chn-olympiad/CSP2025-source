#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	int n, k, r;
	cin >> n >> k >> r;
	int n[l, r];
	for (int i = 1; i <= n; i++) {
		cin >> n[i];
	}
	if (n[l1, r1] = n[l2, r2]) {
		l <= i <= n;
	}
	sort(k + n, k + n + 1);
	cout << k;
	return 0;
}
