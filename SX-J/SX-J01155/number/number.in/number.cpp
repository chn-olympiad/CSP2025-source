#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	string s;
	int x, r;
	s= {1, 2, 3, 4, 5, 6, 7, 8, 9};
	sort(s + n, s + n + 1);
	if (s[1] == s[1]) {
		s[1] = 1;
	}
	cout << s[1];
	return 0;
}
