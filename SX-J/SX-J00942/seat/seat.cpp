#include <bits/stdc++.h>
using namespace std;
int a[105];

int main() {
	freopen("seat", "r", stdin);
	freopen("seat", "w", stdout);
	int n, m, sum = 0;
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		for (int j = 1; j <= n; j++) {
			cin >> a[i];
			sum = a[1];

		}

	}
	cout << n << m;
	return 0;
}