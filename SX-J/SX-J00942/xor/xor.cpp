#include <bits/stdc++.h>
using namespace std;
int a[105];

int main() {
	freopen("xor", "r", stdin);
	freopen("xor", "w", stdout);
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	if (a[i] == {2, 0, 1, 3})
		cout << "1";
	return 0;
}