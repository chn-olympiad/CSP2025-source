#include <bits/stdc++.h>
using namespace std;
int x[100][100];

int main() {
	//freopen("seat.in","r",stdin);
	//freopen("seat.out","w",stdout);
	int n, m;
	cin >> n >> m;
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < m; j++) {
			cin >> x[i][j];

		}
	}
	while (1) {
		sort(as)
	}

	return 0;
}