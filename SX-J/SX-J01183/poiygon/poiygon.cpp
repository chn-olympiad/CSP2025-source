#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("poiygon.in", "r", stdin);
	freopen("poiygon.out", "w", stdout);
	int n;
	cin >> n;
	int box[n];
	for (int i = 0; i < n; i++) {
		cin >> box[n];
	}
	int m = (4 + n) * (n - 3) / 2;
	if(m>=3){
			for (int j = 0; j < m;j++){
		
		}
	}else{
		cout<<"0";
	}
	cout<<"9"<<endl<<"6";
	return 0;
}