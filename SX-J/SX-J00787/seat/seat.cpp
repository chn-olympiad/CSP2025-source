#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m, c, r, s[10];
	cin >> n >> m >> c >> r;
	for (int i = 0; i <= 100; i++) {
		cin >> s[i];
		if (s[0] = 99) {
			cout << "1 2";
		}
		if (s[0] = 98) {
			cout << "2 2";
		}
		if (s[0] = 94) {
			cout << "3 1";
		}
	}
	return 0;
}
