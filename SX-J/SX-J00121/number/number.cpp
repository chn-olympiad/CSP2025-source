#include <bits/stdc++.h>
using namespace std;

string s;

int main() {
	freopen("number.in", "r", stin);
	freopen("number.out", "w", stdout);
	cin >> s;
	if (s.length() == 1)
		cout << s;
	return 0;
}
