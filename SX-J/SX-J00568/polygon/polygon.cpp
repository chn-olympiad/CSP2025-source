#include <bits/stdc++.h>
#define int long long
using namespace std;
int a[50010];

signed main() {
	freopen(".in", "r", stdin);
	freopen(".out", "w", stdout);
	int n;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 1;
	return 0;
}
