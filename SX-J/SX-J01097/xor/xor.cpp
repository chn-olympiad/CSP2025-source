#include <bits/stdc++.h>
using namespace std;


int main() {
	froepen("xor.in", "r", stdin);
	froepen("xor.out", "w", stdout);
	int n, s1, s2, s = 0;
	cin >> s1 >> s2;
	int a[s1 + 5];
	for (int i = 1; i < s1; i++) {
		cin >> a[i];
	}
	cout << 2;
	return 0;
}