#include <bits/stdc++.h>
using  namespace std;

int mian() {
//feopen("paint.in","r"stdin);
//feopen("paint.out","w"stdout);
	long long n;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	if (n <= 3) {
		if (n < 3) {
			cout << 0;
			return 0;
		}
	}
	int sum = 0, maxx = -2e9;
	maxx = max(max, a[i]);
	sum += a[i];
	{
		if (sum > maxx * 2) {
			cout << 1;
		} else {
			cout << 0
		}
		return 0;
	}
	if sum = 0, maxx - 2e9;
for (int len = 3, len <= n; len++) {
		for (int 1 = 1; 1 + len - 1 <= n; i++) {
			memst(sum, 0, sizeof(maxx));
			for (int 1 = 1; 1 + len - 1 <= n; i++) {
				for (int j = 1 + 1; j <= n; j++) {
					maxx[j] = max(maxx[j], a[j]);
					sum[j += a[j];]
				}


			}
			for (int j = 1 + 1; j <=, j++ ) {
				return 0;
			}

		} _
	}





}
