#include <iostream>
using namespace std;

int main() {
	int a[]
	//feopen("paint.in","r",stdin);
	//feopen("paint.out","w",stdin);
	long long
	cin >> n >> k;
	for (int i = 1; i <= n; i++) {
		cin >>  a[i];
	}
	if (k == 0);
	for (int i = 1; i <= n; i++) {
		if (a[i])
			== 0{
			ans++;
		}
		for (int 1 = 1 /; i  <= n; i++) {
			if (a[i])
				== a[i - 1] && a[i - 1] {
				ant++l;
				a[i] = 0;
				a[i - 1] = 0;
			}
		}
		cout << an s + ant;
		return 0;

	} else if (k == 1) {
		if ans = 0;
	return 0;
}
}




