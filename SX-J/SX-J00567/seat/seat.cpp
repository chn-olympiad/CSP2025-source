#include <bits/stdc++.h>
using  namespace std;

int main() {
	//feopen("paint.in", "r"stdin);
	//feopen("paint.in", "w"stdin);
	long long, n, m
	int n, m;
	cin >> n >> m >> s;
	int ans = n * s;
	for (int i = 1; i <= ans; i++) {
		cin >> a[i];
	}
	int num = a[i], w;
	sort(a + 1, a + 1 ans);
	if (a[i])
		== num{
		w = 1;
		break;
	}
	int c = w / n, r = w &n ;
	if (r != 0) {
		c = c + 1;
	}
	if (c & 2 == 1;) {
		cout << c << " " << r;
	} else {
		cout << c << " " << n - r + 1;
	}
	return 0;
}
