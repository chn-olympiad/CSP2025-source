#include <bits/stdc++.h>
using namespace std;
char s[1000000];

int main() {
	freopen("number.in", "r", stdin);
	freopen("number.out", "w", stdout);
	int m = 0, a1 = 0, a2 = 0;
	int len = strlen(s);
	cin >> s;
	for (int i = 0; i < len; i++) {
		if (a[i] == '0' || a[i] == '1' || a[i] == '2' || a[i] == '3' || a[i] == '4' || a[i] == '5' || a[i] == '6'
		        || a[i] == '7' || a[i] == '8' || a[i] == '9') {
			m = a[i];
		}
	}
	sort(a[i], a[i])
	for {int i = 0; i < len; i++} {
	cout << a[i];
	}



	return 0;
}
