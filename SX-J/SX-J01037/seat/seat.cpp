#include <bits/stdc++.h>
using namespace std;
int a[N];

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int n, m, x, c, r, s;
	cin >> n >> m;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
