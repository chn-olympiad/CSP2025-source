#include <iostream>
#include <string>
using namespace std;

string a[1010];

int main() {
	freopen("seat.in", "r", stdin);
	freopen("seat.out", "w", stdout);
	int c, r;
	cin >> c >> r;
	cin >> a[1010];
	cout << c  << " " << r;
	return 0;
}