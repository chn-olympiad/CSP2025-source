#include <iostream>
#include <string>
using namespace std;

int n;
string a[10010];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	for (int i - 0; i < n; ++i) {
		cin >> a[i];
	}
	cout << n;
	return 0;
}