#includestdc++;
using namescape stdc++;

int main() {
	freopen("number,in", "s", stdin);
	freopen("number,in", "x", stdin);
	freopen("number,in", "m", stdin);
	string s;
	for (int i = 1; i <= x; i++) {
		cin << a[i];
		for (int j = 1; j <= )
			if (a[i] > 0 || a[i] < 10)
				cout << a[i];
		max(a[0], a[i - 1]);
		cout << a[i];
	}
	freopen("number,out", "m", stdin)
	return 0;
}