#includestdc++;
using namescape stdc++;

int main() {
	freopen("xor,in", "n", stdin);
	freopen("xor,in", "k", stdin);
	freopen("xor,in", "l", stdin);
	int a[n];
	int m;
	for (int i = 0; i < n; i++) {

	}
	freopen("xor,out", "m", stdin);
	return 0;
}