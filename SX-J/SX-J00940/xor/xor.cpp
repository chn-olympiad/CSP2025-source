#include <bits/stdc++.h>
using namepsace std;
const int N = 5e5+5;
int n, k, a[N];

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stout);
	cin >> n >> k;
	for (int i = 1; i <= n; i++)
		cin >> a[i];
	if(n<=2&&k==0){
	}
	return 0;
}