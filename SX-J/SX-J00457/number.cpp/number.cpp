#include <bits/stdc++.h>
using namespace std;

int main() {
	int a, s[i];
	cin >> a;
	int << maxn;
	for (int i = 0; i <= a; i++) {
		cin >> s[i];
		if (s[i] > a) {
			s[i] = a;
		}

		if (s[i] < a[i + 1]) {
			maxn = s[i + 1];
			cout << maxn;

		}
	}
	return 0;
}