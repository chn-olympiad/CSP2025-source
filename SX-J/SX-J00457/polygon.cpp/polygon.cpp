#include <bits/stdc++.h>
using namespace std;

int main() {
	int n, a[10001];
	cin >> n;
	for (int i = 0; i <= n - 1; i++) {
		int s = i;
		cin >> a[s];

	}
	if (a[i] <= 3) {

	}
	return 0;
}