#include <bits/stdc++.h>
using namespace std;
int n,arr[500005];
int main(){
	freopen("polygon.in","r",stdin);
	freopen("polygon.out","w",stdout);
	cin>>n;
	for(int i=1;i<=n;++i){
		cin>>arr[i];
	}
	sort(arr+1,arr+1+n);
	if(n==3){
		if(arr[]=)
	}
	cout<<"1";
	fclose(stdin);
	fclose(stdout);
	
	return 0;
} 
