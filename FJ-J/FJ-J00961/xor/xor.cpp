#include <bits/stdc++.h>
using namespace std;
const long long NMAX = 500005;
long long n, k;
long long a[NMAX];
long long ans = -1;
int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	scanf("%d %d", &n, &k);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &a[i][0]);
	}
	
	return 0;
}
