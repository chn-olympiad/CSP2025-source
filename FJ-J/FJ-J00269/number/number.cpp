#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
	int i;
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	string s;
	cin >> s;
	vector <int>a;
	for (int i = 0;i<s.size>,i++){
		if(isdight(s[i])a.push_back(static_cas<int>(s[i]));
		sort(a.begin,a.back)
		
	
	cout << s << endl;
	return 0;
}
/*
