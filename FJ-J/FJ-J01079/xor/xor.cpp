#include<bits/stdc++.h>
using namespace std;
int arr[i];
int main(){
	freopen("xor.in","r",stdin);
	freopen("xor.out","w",stdout);
	int n,k;
	cin >> n >> k;
	for(int i = 0;i<n;i++){
		cin >> a[i];
	}
	int I,r1,r2,r3;
	for(I = 1;i < n; i++){
		for(i = 1; r <= n;r++){
			for(int i = 1; i <= r;i++){
				xplus = (a[i]* a[i-1]);
			}
		}
		if(xplus == k){
			zi[zil][0] = 1;
			zi[zil][1] = r;
			z[i]++;
		}
	}
	return 0;
}
