#include <bits/stdc++.h>
using namespace std;

int main()
{
//	freopen("polygon.in", "r", stdin);
//freopen("polygon.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	for(int i = 1; i <= n * m; i++)
	{
		cin >> num[1];
	}
	for(int i = 1; i <= n * m; i++)
	{
		cout << num[i] << ' ';
	}
	return 0;
}
