#include <bits/stdc++.h>
using namespace std;

int n, k, a[520229];

int search (int pos) {
	int A=pos, B;
	for (int )
	search ();
	
	return ;
}

int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	
	scanf ("%d%d", &n, &k);
	for (int i=1; i<=n; i++) {
		scanf ("%d%d", &a[i]);
	}
	
	cout << search(1);
	
	return 0;
}
