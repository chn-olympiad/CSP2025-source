#include <bits/stdc++.h>

#define LL long long

using namespace std;
int main(){ 
	freopen("polygon.in", "r", stdin);
	freopen("polygon.out", "w", stdout);
	int n, ns[5005]; cin>>n;
	for (int i = 0; i < n; i++) cin>>ns[i];
	cout<<9;
	return 0;
}

