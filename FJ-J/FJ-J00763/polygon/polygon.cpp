#include<bits/stdc++.h>//最后记得要删去注释 
using namespace std;///////////Attention!!!Don't end!!!|
int a,c[5005];//                                       V
long long s,/;
int main(){
	//freopen("polygon.in","r",stdin);
	//freopen("polygon.out","w",stdout);
	scanf("%d",&a);
	cout<</;
	for(int i=0;i<a;i++){
		scanf("%d",&c[i]);
	}
	sort(c,c+a);
	if(a==3){
		if(c[0]+c[1]>c[2]){
			s++;
		}
	}else{
		for(int h=3;h<=a;h++){
			for(int i=0;i<h;i++){
				for(int j=0;j<;j++)
			}
		}
	}
	printf("%lld",s);
	//fclose(stdin);
	//fclose(stdout);
	return 0;
}
