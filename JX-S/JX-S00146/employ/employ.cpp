#include <bits/stdc++.h>

using namespace std;

const int MAX_N = 2e5 + 5;

int n, m;
string s;
int c[MAX_N];

int main () {
    freopen ("employ.in", "r", stdin);
    freopen ("employ.out", "w", stdout);

    cin >> n >> m >> s;
    for (int i = 1; i <= n; i++) {
        cin >> c[i];
    }
    cout << 0 << endl;
    //AC
    //rp++
    return 0;
}
