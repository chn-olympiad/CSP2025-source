#include <bits/stdc++.h>

using namespace std;

int a[510];
map<string, int> mp;


int main() {
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m;
    cin >> n >> m;
    string s;
    getline(cin, s);
    for (int i = 1;i <= n;i++) {
        cin >> a[i];
    }

    cout << 2;
    return 0;
}