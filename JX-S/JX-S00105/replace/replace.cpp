#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int n, q;
    string x, y, z, a;
    cin >> n >> q;
    char c[10000];
    char b[1000000];
    for(int i = 1; i <= q; i++)
    {
        cin >> a[i];
    }
    for(int i = 1; i <= n; i++)
    {
        cin >> b[i];
    }
    for(int i = 1; i <= q; i++)
    {
        cout << "0";
    }
}
