#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("club.in","r",stdin);
    freopen("club.out","w",stdout);
    int n,q;cin>>n>>q;
    char ch;
    while(cin>>ch);
    while(q--)cout<<0<<'\n';
    fclose(stdin);
    fclose(stdout);
    return 0;
}
