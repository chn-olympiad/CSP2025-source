#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m, c, p;
    cin >> n >> m >> c;
    while(){
        bool s;
        if(s = 1){

        }
        else if(s = 0){

        }
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
