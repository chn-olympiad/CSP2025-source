#include<iostream>
#include<cstdio>
using namespace std;
int main(){
    freopen("club.in", "r", stdin);
    freopen("club.out", "w", stdout);
    int n, t;
    cin >> n >> t;
    for(int i = 1;i <= n / 2;i++){

        cout << max(a + n + t) << endl;

    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
