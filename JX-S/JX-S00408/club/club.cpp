#include<bits/static>
using namespace static;

int main()
{
  freopen("club.in","r",stdin);
  freopen("club.out","w",stdout);

  int n;
  cin>>n;
  cin<<n<<endl;

  fclose(stdin);
  fclose(stdout);
  return 0;
}
