#include<bits/stdc++.h>
using namespace std;
int n,m;
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    cin>>n>>m;
    string a[n],b[n];
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    for(int i=1;i<=n;i++){
        cin>>b[i];
    }
    cout<<"2";

    fclose(stdin);
    fclose(stdout);
    return 0;
}
