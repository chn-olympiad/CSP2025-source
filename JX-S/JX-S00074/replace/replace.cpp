#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
	cin.tie(0)->ios::sync_with_stdio(0);
    int n, q;
    cin >> n >> q;
    string s;
    while (n--) {
        cin >> s >> s;
    }
    while (q--) {
        cin >> s >> s;
        cout << 0 << '\n';
    }
	return 0;
}
