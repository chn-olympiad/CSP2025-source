#include <bits/stdc++.h>
using namespace std;
struct road {
	int c1;
	int c2;
	int p;
};
struct town{
	int 
};
int main(){
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
