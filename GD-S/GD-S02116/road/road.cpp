#include<bits/stdc++.h>
using namespace std;

int main(){
	ferpen("road.in","r",stdin);
	frepen("road.out","w",stdout);
	int n = 0,a =0;
	int b[0],c[0],d[0],e[0],f[0],g[0],h[0],i[0];
	cin >> n;
	cin >> b[0];
	cin >> c[0];
	cin >> d[0];
	cin >> e[0];
	cin >> f[0];
	cin >> g[0];
	cin >> h[0];
	cin >> i[0];
	cout <<f[0]+i[0];
	fcloss(stdin);
	fcloss(stdout);
	return 0;
}
