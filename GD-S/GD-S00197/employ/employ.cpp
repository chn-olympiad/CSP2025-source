#include<bits/stdc++.h>
using nameapce std;
int c[510];
int main(){
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	int n,m;
	string s;
	cin>>n>>m;
	cin>>s;
	for(int i=1;i<=n;i++){
		cin>>c[i];
	} 
	sort(c+1.c+n+1);
	cout<<m;
	fclose(stdin);
	fclose(stdout);	
	return 0;
}
