#include <bits/stdc++.h>
using namespace std;
const int N = 2e5 + 10;
int n, m;
char w[N], v[N], u[N], z[N];
int main(){
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	scanf("%d%d", &n, &m);
	for(int i = 1; i <= n; i++) scanf("%s%s", w + 1, v + 1);
	for(int i = 1; i <= m; i++) scanf("%s%s", u + 1, z + 1), printf("0\n");
	return 0;
}

