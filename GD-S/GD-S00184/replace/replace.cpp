#include<map>
#include<iostream>
using namespace std;
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	map<string,string> mp;
	string s,t;
	int n,q;
	cin>>n>>q;
	for(int i=1;i<=n;i++){
		cin>>s>>t;
		mp[s]=t;
	}
	while(q--){
		cin>>s>>t;
		int cnt=0;
		string a,b;
		for([a,b]:mp){
			string re=s;
			
		} 
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
