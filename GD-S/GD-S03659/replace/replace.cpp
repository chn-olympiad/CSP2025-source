#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen('replace.in','r',stdin);
	freopen('replace.out','w',stdout);
	string a,b;
	int q;
	cin > w >> q;
	for(int i=0;i<w;i++){
		cin >> a >> b;
	}
	for(int i=0;i<q;i++){
		
		cin >> a >> b;
		cout << 0;
	}
	return 0;
}
