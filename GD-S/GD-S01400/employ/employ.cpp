#include<bits/stdc++.h>
using namespace std;

const int csp_N = 505;
int csp_a[csp_N];
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	
	int csp_n,csp_m;
	cin >> csp_n >> csp_m;
	int csp_s;
	cin >> csp_s;
	for(int i = 1;i <= csp_n;i++) cin >> csp_a[i];
	cout << 0;
	
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
