#include<bits/stdc++.h>
using namespace std;

int n, m, s[505], c[505];

int main() {
    (void)scanf("%d %d", &n, &m);
    for(int i = 1; i <= n; i++) {
        (void)scanf("%d", &s[i]);
    }
    for(int i = 1; i <= n; i++) {
        (void)scanf("%d", &c[i]);
    }
    (void)printf("0");
    return 0;
}

