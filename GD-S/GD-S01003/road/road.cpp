#include <iostream>
#include <stdio.h>
using namespace std;

int main(){
	freopen("road.in", "r", stdin);
freopen("road.out", "w", stdout);
	int n, m, k;
	cin>> n>> m>> k;
	fclose(stdin);
	cout<< 0;
fclose(stdout);
	return 0;
}
