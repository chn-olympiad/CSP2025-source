#include <bits/stdc++.h>
using namespace std;
int a,b;
char c[1000000][3];
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	cin >> a >> b;
	for (int i = 0;i < a+b;i++){
		for (int j = 0;j <= 1;j++){
			cin >> [i][j];
		}
	}
	for (int x = 0;x < b;x++){
		cout << 0 << endl;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}