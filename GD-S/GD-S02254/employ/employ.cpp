#include <iostream>
#include <algorithm>
#include <vector>
#include <queue>
#include <string>
#define MAXN (200010)

using namespace std;
int T, n, q;

string s[MAXN][3];
string ask[MAXN][3];

void solve() {
	cout << rand() % 998244353 << "\n";
}

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	T = 1;
	while (T --) solve();
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
