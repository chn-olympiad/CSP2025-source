#include<iostream>
#include<algorithm>
#define rf(a, b, c) for(int b = (a); b <= (c); b++)
using namespace std;

int main(){
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	return 0;
}

