#include<iostream>
#include<algorithm>
#define rf(a, b, c) for(int b = (a); b <= (c); b++)
using namespace std;

int main(){
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	return 0;
}

