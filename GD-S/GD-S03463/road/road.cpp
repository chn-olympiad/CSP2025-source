#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
struct eage{
	int v, w;
};
vector<eage> a[N];
int n, m, k;
int d[N];
int main(){
//	freopen(road.in, "r", stdin);
//	freopen(road.out, "w", stdout);
	cin >> n >> m >> k;
	for (int i = 1; i <= n; i ++) {
		int u, v, w;
		cin >> a[i].v >> v >> w;
		
	}
	return 0;
} 
