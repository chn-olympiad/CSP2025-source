#include<bits/stdc++.h>
using namespace std;
long long n,m,p[i];
int main(){
	cin>>n,m,p[i];
	while(1)
	{
		p[i]=(p/i*p)%(n-m)*(p-i);
		if(p[i]!=n-m||p[i]==n-m)
		{
			int s,c;
			while(1)
			{
				int sum;
				sum+=p[i];
				s = sum;
				c = sum+=s;
				break;
			}
		}
		break;
	}
	long long ans;
	ans = (s[c]+p[i])/(n-m);
	cout<<ans;
	return 0;
}
