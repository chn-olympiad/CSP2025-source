#include<bits/stdc++.h>
using namespace std;
char n;
long long i,s,a;
int main(){
	scanf("%n", n);
	cin>>i>>s;
	for(char 'y'=1;y<=i;y++)
	{
		long long sum;
		sum+=y[i];
		y++;
		if(sum>=100&&y>=100)
		{
			y = i/s+sum;
		}
		break;
	}
	while(sum!=0)
	{
		long long ans;
		ans+=sum[y];
		if(ans==0&&ans==1)
		{
			ans+=i[s];
		}
		break;
	}
	int x,z;
	while(1)
	{
		x[z]-=s[y];
		long long q,j;
		char s[y];
		if(q[j]==s[y])
		{
			while(n)
			{
				q = j;
				j = s;
				s = y;
				y = q;
				if(q[j]!=s[y])
				{
					break;
				}
			}
		}
		break;
	}
	a = (q[j]+s[y])/((ans*sum)%(i-s));
	cout<<a;
	return 0;
}

