#include <bits/stdc++.h>
using namespace std;

const int N = 1000;
int n, m, s, c[N];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	
	cin >> n >> m >> s;
	for(int i = 1; i <= n; i++) {
		cin >> c[i];
	}
	
	
	
	return 0;
}
