#include <bits/stdc++.h>
using namespace std;

long long n, q;
string s;

int main()
{
	#ifdef LOCAL
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	#endif
	cin >> n >> q;
	for(int i = 1; i <= n; i ++)
	{
		cin >> s;	
		cin >> s;
	}
	for(int i = 1; i <= q; i ++)
	{
		cin >> s;
		cin >> s;
	}
	for(int i = 1; i <= q; i ++)
	{
		cout << 0 << endl;
	}
}
