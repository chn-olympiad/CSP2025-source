#include <bits/stdc++.h>
using namespace std;
const int maxn=100010; 

int main() {
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int n,q;
	cin >> n >> q;
	for(int i=1;i<=(n+q);i++) {
		string a,b;
		cin >> a >> b;
	}
	while(q--) {
		cout << 0 << "\n";
	}
	return 0;
} 
