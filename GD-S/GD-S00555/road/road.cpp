#include <bits/stdc++.h>
using namespace std;
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	
	int n, m, k;
	int u;
	cin >> n >> m >> k;
	vector<int> vec[n];
	for (int i = 0; i < (m*3+k*(n+1); i++) {
		cin >> n;
	}
	cout << 505898585
	return 0;
}
