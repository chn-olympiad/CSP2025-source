#include <bits/stdc++.h>
using namespace std;
int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	ios :: sync_with_stdio(0), cin.tie(0), cout.tie(0);
	cin >> n >> q;
	while (q--) puts("0");
	return 0;
}
