// 不可以，总司令！！！ 
#include<bits/stdc++.h>
using namespace std;
const int M = 2e5 + 7;
string s1[M], s2[M];
int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q; cin >> n >> q;
	for (int i = 1; i <= n; ++i) {
		cin >> s1[i] >> s2[i];
	}
	while(q--) {
		string t1, t2; cin >> t1 >> t2;
		cout << 0 << "\n";
	}
	return 0;
}
