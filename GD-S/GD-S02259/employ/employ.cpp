#include <bits/stdc++.h>

using namespace std;

const int N = 1e5 + 10;
int n, m;
string str;
int c[N];

int main(){
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	cin >> str;
	for(int i = 1; i <= n; i++){
		cin >> c[i];
	}
	cout << 1;
	return 0;
}
