#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int n,q;
	cin>>n>>q;
	string a,b;
	while(n--){
		cin>>a>>b;
	}
	while(q--){
		cin>>a>>b;
		cout<<"0\n";
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
