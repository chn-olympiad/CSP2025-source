#include<bits/stdc++.h>
using namespace std;
int main()
{
	/*
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);*/
	int n,p;
	cin>>n>>p;
	string a[n],b[n];int k=0;
	string s1,s2;
	cin>>s1>>s2;k = s1.size();
	while(p>0)
	{
		for(int i=0;i<n+p;i++)
		{
			string s;
			cin>>s;
	   		if(s.size == k)
	  	    {
	    		
			}
		}
		
	}
	/*
	fclose(stdout);
	fclose(stdin);*/
	return 0;
}
