#include <bits/stdc++.h>
using namespace std;

struct students {
    int x, y, z, to;
};

struct choose {
    int val, idx;
};

int main () {
    freopen ("replace.in", "r", stdin);
    freopen ("replace.out", "w", stdout);
    int T;
    scanf ("%d%d", &T, &T);
    while (T--) {
		printf("0");
	}
    return 0;
}
