#include<bits/stdc++.h>
#define mod 998244353
using namespace std;
int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	for (int i = 1; i <= n + q; i++) {
		string s;
		cin >> s >> s;
	}
	for (int i = 1; i <= q; i++) cout << "0\n";
}
