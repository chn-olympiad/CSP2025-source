#include <bits/stdc++.h>
using namespace std;
const int N = 1e2 + 10;
int n, q;
string s;
signed main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i=1; i<=n; i++)
		cin >> s >> s;
	while (q--) {
		cin >> s >> s;
		cout << "0\n";
	}
	return 0;
}
