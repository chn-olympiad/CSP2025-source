#include <bits/stdc++.h>

using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m;
	string s;
	cin >> n >> m;
	for (int i = 0; i < n; i++)
		cin >> s >> s;
	for (int i = 0; i < m; i++)
		cout << 0 << endl;
	return 0;
}
