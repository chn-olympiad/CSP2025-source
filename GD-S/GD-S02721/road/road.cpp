#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	for (int i = 1; i <= n; i++){
		int x;
		cin >> x >> x >> x;
	}
	for (int i = 1; i <= m+1; i++){
		for (int j = 1; j <= n+1; j++){
			int x;
			cin >> x;
		}	
	}
	cout << 14;
	return 0;
}
