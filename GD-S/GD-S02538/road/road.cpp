#include <iostream>

#define Nahida return 0

/*
int k;

namespace LinkedForwardStar {
	struct Edge {
		int to;
		int w;
		int next;
	} edge[1000005];
	
	int cnt = 0, head[10005] = {};
	
	void addEdge(int u, int v, int w) {
		edge[++cnt].to = v;
		edge[cnt].w = w;
		edge[cnt].next = head[u];
		head[u] = cnt;
		
		return;
	}
	
	void addNoDirEdge(int u, int v, int w) {
		addEdge(u, v, w);
		addEdge(v, u, w);
	}
}

namespace MinimumSpanningTree {
	using namespace LinkedForwardStar;
	
	const int INF = 0x7fffffff;
	
	int n, m;
	int dis[10005];
	bool vis[10005];
	int ans = 0;
	
	void minimumSpanningTree(void) {
		
	}
}
*/

// 写到一半才发现忘了 MST 怎么写。

int main(void) {
	//freopen("road1.in", "r", stdin);
	
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	
	std::cout << 0 << '\n';
	
	fclose(stdin);
	fclose(stdout);
	Nahida;
}

/* 炸了。

估分？算了。总分肯定上不了 100 分。
今年没有蓝钩了。 

洛谷 uid = 1263338, 1531009, 1265062。

*/
