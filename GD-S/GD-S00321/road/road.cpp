#include <bits/stdc++.h>
using namespace std;
const int N = 1e6+5;
//最小生成树 
int main(){
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k, ans=0;
	cin >> n >> m >> k;
	for(int i=1;i<=m;++i){
		int u, v, w;
		cin >> u >> v >> w;
	}
	
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
