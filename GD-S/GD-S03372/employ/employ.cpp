#include<bits/stdc++.h>
using namespace std;
#define int long long

const int N = 700;
int n,m,a[N];
char s[N];

signed main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	cin >> n >> m >> s;
	for(int i = 1;i <= n;i ++){
		cin >> a[i];
	}
	cout << 0;
	return 0;
}
