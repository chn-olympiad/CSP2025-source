#include<iostream>
using std::cin;
using std::cout;
const int N=1;
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    std::ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int n,q;
    int i;
    for(i=1;i<=n;++i)
        cout<<0;
    return 0;
}