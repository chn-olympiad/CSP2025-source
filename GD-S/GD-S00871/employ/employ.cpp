#include <bits/stdc++.h>
#define int long long
using namespace std;
int n, m, a[501];
char s[501];
signed main(){
	freopen("employ.in", "r", "std.in");
	freopen("employ.out", "w", "std.out");
	cin >> n >> m;
	scanf("%s", s + 1);
	for (int i = 1; i <= n; i++)
	{
		cin >> a[i];
	}
	cout << 2;
	return 0;
}
