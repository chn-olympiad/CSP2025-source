#include <bits/stdc++.h>
using namespace std;
int main(){
	freopen("polygon.in","r",stdin);
	freopen("polygon.out","w",stdout);
	int n,a;
    cin>> n >> a; 
	for(int i = 1;i<=n;i++){
    if(n == 5);
    else cout << n + a <<endl;
	}
	cout << n + a <<endl; 
	fclose(stdin);
	fclose(stdout); 
	return 0;
}
