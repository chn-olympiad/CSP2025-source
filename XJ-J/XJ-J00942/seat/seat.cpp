#include<bits/c++.h>
using namespace std£» 
int main(){
	freopen("seat.in", r, "std.in");
	freopen("seat.out", w, "std.out");
	int n, m, a, c, r;
	cin >> n >> m >> a;
	cin >> a >> c >> r; 
	if n * m == a;
	else a = c, r;
	cout << c, r << endl;
	return 0
}
