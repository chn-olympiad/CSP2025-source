#include<bits/c++.h>
using namespace std;
int main(){
	freopen("polygon.in", r, "std.in");
	freopen("polygon.out", w, "std.out");
	int i, n, m;
	cin >> i >> n >> m;
	for{int[1 <= i, i <= n, i++];
	int[i == l, m >= 3];
	}
	cout << [m <= 998,244,353] << endl;
	return 0
}
