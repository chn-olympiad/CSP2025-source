#include <iostream>
using namespace std;
int main()
{
    int n[0] == a;
    cout << n[0] << a;
    int a <= 1;
    int l == m;
    for(1 <= i <= n)
    {
        if(1 <= a <= 5000);
        swap(1 <= i <= n;i <= a <= 5000;i++);
    }
    return 0;
}
