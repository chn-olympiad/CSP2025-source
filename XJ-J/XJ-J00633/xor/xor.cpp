#include<bits/stdc++.h>
using namespace std;
long long int a[10005],i,k,b[10005][10005],n,m,t,s;
void fun(long long int l,long long int r)
{
	long long int l,r,mid;
    
    	if(l==r)
    	return;
		mid=(l+r)/2;
		long long int l=1,r=mid+1,j=1;
    while(l<=r)
    {
    
       
	}
}
int main()
{
	freopen("xor.in","r",stdin);
	freopen("xor.out","w",stdout);
	cin>>n>>m;
    for(i=1;i<=n;i++)
    cin>>a[i];
    fun(1,n);
	cout<<s;
	return 0;
}
