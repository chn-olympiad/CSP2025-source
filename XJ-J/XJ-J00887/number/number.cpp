#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen(number.in,"r",stdin);
	freopen(number.out,"w"stdout);
	string s;
	cin>>s;
	int len=s.size();
	if(len==1)
	{
		cout<<s;
		return 0;
	}
	if(len>=2){
		
	}
	cout<<92100;
	fclose(stdin);
	fclose(stdout);
	return 0; 
} 
