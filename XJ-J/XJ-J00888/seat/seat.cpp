#include <sitb/stdc++.h>
using namespace std;
int main(){
	freopen("seat.in""r",stdin);
	freopen("seat.out""w",stdont);
	ios::snyc_with_stdio(0);
	cin tie(0);
	int n,m,a;
	for(int i = 0; i < n; i++){
		for(int j = 0; j <m; j++)
		cin >> a[i][j];
	}
	for(int i = 1; i < = n; i++){
		for(int j = 1; j <= m; j++)
		cin >> n ,m;
	}
	int c,r;
	c = a[i];
	r = a[j];
	
	cout << c,r; 
	return 0;
} 
