#include <bits/stdc++.h>
using namespace std;

int main()

    {
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	cin >> s;
    string s;
    for(int i=0;i<=s.size;i++)
    {
    	cin >> a[i];
    }
	fclose(stdin);
	fclose(stdout);
    return 0;
}

