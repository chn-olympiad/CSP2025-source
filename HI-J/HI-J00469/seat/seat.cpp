#include<bits/stdc++.h>
using namespace std;
typedef long long 11;
int scr[1000];
int main(){
	//freopen("seat.in","r",stdin);
	//freopen("seat.out","w",stdout);
	int a,b,prs;
	cin >> a >> b;
	for(int i = 0;i < a;i++){
		cin >> scr[i];
	}
	prs = scr[0];
	sort(scr,scr + a*b);
	for(int i = a*b-1;i >= 0;i--){
		if(scr[i]==prs){
			int = a*b-i;
			cout << ceil(k/(a*1.0))
			cout <<(())
			cout << ((i + 1) % a) == 0;
			cout <<'  ' <<a - ((k%a) a);
		}
	}
	//fclose(stdin);
	//fclose(stdout);
	return 0;
}
