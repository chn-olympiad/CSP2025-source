#include <bits/stdc++.h>
using namespace std;
int main()
{
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	string s;
	cin>>s;
	cout<<s;
	fclose(stdin);
	fclose(stdout);
	return 0;
} 
/*
	int a[1000001];
	int sum=0;
	string s;
	cin>>s;
	for(int i=1;i<=s.size();i++)
	{
		if(s[i]>=48&&s[i]<=57)
		{
			s[i]=a[i];
		}
	}
	for(int i=1;i<=a[i];i++)
	{
		
	}
*/
