#include<bits/stdc++.h>
using namespace std;
int main()
{
	//freopen("number.in","r",stdin);
	//freopen("number.out","w",stdout);
	string s;
	int a[1000000];
	cin>>s;
	for(int i=0;i<=a[i];i++)
	{
		cin>>s;
		if(a[i]==a[i+1])
		{
			return a[i]>a[i+1];
			cout<<a[i]<<a[i+1];
		}
	}
	
	//fclose(stdin);
	//fclose(stdout);
return 0;
}
 
