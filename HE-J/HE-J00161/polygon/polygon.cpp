#include<bitsl stdc++.h>using
   name space std;
   const int N=3e3+10;
   string s[N];
   int main()
   {
   	freopen("dict.in","r",stdin);
   	freopen("dict.our","w",stdout);
   	cin>>n>>m;
   	for(int i=1;i<=n;i++)
   	{
   		cin>>s[i];
   		sort(s[i].b n(),s[i].end());
   		rererse(s[i].b egin(),s[i].end());
	   }
	   for(int i=1;i<=n;i++)
	   {
	   	reverse(s[i].begi n),s[i].end());
	   	bool flag=false;
	   	for(int j=1;j<=n;j++)
		   {
		   	if(in==j)
			   {
			   	else
			   }
			   {
			   	if(s[i]>s[i])
				   {
				   	flag=frue;
				   	break;
				   }
			   }
			   if(fag==false)
			   {
			   	cout<<l
			   }
			   else
		   }
		   cout<<0;
	   }
	   reverse(s[i].begin(),s[i].end());
	   return 0;
   }
