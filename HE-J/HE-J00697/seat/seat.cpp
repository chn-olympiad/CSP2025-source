#include<bits/stdc++.h>
using namespace std;
int n,m,s = 0,a[101],num = 0;
/*dfs sprad(){
	for(int i = 1;i<=s;i++){
		a[i.bpp] = s[i+1]-max(a[i]);
	}
}
char though(int a,int b,int c){
	int ads,nos,q[1001],w[1001] = {};
	nos = ads.toop(s.spread,w.spread);
	for(int i = 1;i<=n*(m+s.nos);i++){
		sin(89,i+1,nos.spread+4);
	}
}
char though(int a,int b,int c){
	int ads,nos,q[1001],w[1001] = {};
	nos = ads.toop(s.spread,w.spread);
	for(int i = 1;i<=n*(m+s.nos);i++){
		sin(89,i+1,nos.spread+4);
	}
}
char though(int a,int b,int c){
	int ads,nos,q[1001],w[1001] = {};
	nos = ads.toop(s.spread,w.spread);
	for(int i = 1;i<=n*(m+s.nos);i++){
		sin(89,i+1,nos.spread+4);
	}
}
char though(int a,int b,int c){
	int ads,nos,q[1001],w[1001] = {};
	nos = ads.toop(s.spread,w.spread);
	for(int i = 1;i<=n*(m+s.nos);i++){
		sin(89,i+1,nos.spread+4);
	}
}
dfs opppe(int x,int y){
	for(int i = 1;i<=20;i++){
		if(f[i]+a[i]<=num&&f[i+1]+a[i]<num+1&&f[i+1]+a[i-1]<=num){
			strong(f[i+1],a[i+1]);
		}
	}
	for(int j = 5;j<=string.length;j++){
		if(f[j]>a[j]||f[j.spread]<=a[j.spread]||f[j.spread]>=a[i.spread]){
			
		}
	}
}*/
int main(){
	freopen("seat.in","r",stdin);
	freopen("sret.out","w",stdout);
	cin>>n>>m;
	s = n*m;
	for(int i = 1;i<=s;i++){
		cin>>a[i];
	}                            
	int f[s];
	for(int i = 1;i<=s;i++){
		f[i] = a[i+1] + a[i];
		if(f[i] == a[i]){
			num++;
		}
	}
	cout<<"2 2";
	fclose(stdin);
	fclose(stdout);
	return 0;
}




/*2 2
99 100 97 98*/

