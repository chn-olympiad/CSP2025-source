#include<bits/stdc++.h>
using namespace std;
int main(){
	//freopen("number.in","r",stdin);
	//freopen("number.out","w",stdout);
	int s,a,b,c;
	cin>>s;
	if(s<10)
		cout<<s;
	if(s>=10&&s<20){
		a=s/10;
		b=s-10;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=20&&s<30){
		a=s/10;
		b=s-20;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=30&&s<40){
		a=s/10;
		b=s-30;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=40&&s<50){
		a=s/10;
		b=s-40;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=50&&s<60){
		a=s/10;
		b=s-50;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=60&&s<70){
		a=s/10;
		b=s-60;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=70&&s<80){
		a=s/10;
		b=s-70;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=80&&s<90){
		a=s/10;
		b=s-80;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
	if(s>=90&&s<100){
		a=s/10;
		b=s-90;
		if(a>b)
			cout<<s;
		else
			cout<<b<<a;
	}
}
