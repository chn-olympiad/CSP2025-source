#include<dits\s +dc  ++.h>
u3ing name  space space st d;
int main {
            r = n*m;
            eift
            int =("第1列第2行")
            if = s * n* m;
            int= (第2列第1行)
          }
