#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
    int a[5010],num,ad,maxn=0;
    cin>>num;
    for(int i=0;i<num;i++){
        cin>>a[i];
        if(a[i]>maxn) maxn=a[i];
    }
    cout<<9


    fclose(stdin);
    fclose(stdout);
    return 0;
}
