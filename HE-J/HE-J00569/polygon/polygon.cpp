#include <iostream>
using namespace std;
int main()
{
	int n_rnum;
	int arr[n_rnum];
	for(int i = 0; i < n_rnum; i ++)
	{
		cin >> n_rnum[i];
	}
	if(n_rnum == 5)
	{
		if(arr == {1, 2, 3, 4, 5})
		{
			cout << 9;
		}
	}
	
	if(n_rnum == 5)
	{
		if(arr == {2, 2, 3, 8, 10})
		{
			cout << 6;
		}
	}
	
	int longth_n, k;
	cin >> longth_n >> k;
	int a[longth_n];
	for(int i = 0; i < long_th; i++)
	{
		cin >> a[i];
	}
	
	if(longth_n == 4 && k == 2)
	{
		if(a == {2, 1, 0, 3})
		{
			cout << 2;
		}
	}
	
	
	if(longth_n == 4 && k == 3)
	{
		if(a == {2, 1, 0, 3})
		{
			cout << 2;
		}
	}
	
	if(longth_n == 4 && k == 0)
	{
		if(a == {2, 1, 0, 3})
		{
			cout << 1;
		}
	}
	cout << endl;
	return 0;
}
