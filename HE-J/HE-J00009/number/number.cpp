#include <bits/stdc++.h>
using namespace std;
int main()
{
    int s,a[i],c,sum[s],sum[c];
    freopen("number in","r",stdin)
    freopen("number out","w",stdout)
    cin >> s;
    double a[i];
    for (int i=1;i<=sum[s];i++)
    {
	if(a[i]%1=0)
	{
	    int a[i]=c;
	}
    }
    for(int i=1;i<=sum[c];i++)
    {
	if(a[i]>a[0])
	{
	    int a[0]=a[i];
	}
    }
    cout << c;
    return 0;
}