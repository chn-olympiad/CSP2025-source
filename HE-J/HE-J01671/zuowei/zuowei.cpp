#include <bits/stdc++.h>
using namespace std;
int main()
{
	//freopen("zuowei.in","r",stdin);
	//freopen("zuowei.out","w",stdout);
	int m,n,c,r,h=0,a[100][100];
	cin>>n>>m;
	int w=0;
	for(int i=1;i<=n;i++)
	{
		for(int j=1;j<=m;j++)
		{
			cin>>a[i][j];
			if()
			if(a[1][1]<a[i][j])
			{
				cout<<n<<" "<<m;
				break;
			}
		}
		break;
	}
	//fclose(stdin);
	//fclose(stdout);
	return 0;
}
