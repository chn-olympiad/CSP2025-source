#include<bits/stdc++.h>
using namespace std;
int main{
	freopen("pot in"," r "stdin);
	freopen("pot out"," w"stdout);
	int n,m,a,b;
	cin >> n >> m ;
	if(n % 2 = 0){
		a = n * m;
		cont << a;
	}else{
		b = (n + 1)
		cont << b;
		}
	return 0;
}
