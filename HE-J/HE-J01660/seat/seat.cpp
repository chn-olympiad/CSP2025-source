#include<bits/stdc++.h>
using namespace std;
int main()
{
	freopen("seat.in","r",stdin);
	freopen("seat,out","w",stdout);
	long long n,m,a;
	cin>>n>>m;
	cin>>a;
	n,m>0;
	m,n=2;
	a=n*m;
	if(a=100);
		{
			m-1,n-1;
			
		}
		
		if(a<=100,a>=98);
		{
			m-1;
			
		}
		
		if(a<=99,a>=97);
		{
			n-1;
				
		}
		
		if(a<=98,a>=96);
		{
			m-0,n-0;
			
		}
		cout<<n<<" "<<m;	
	fclose(stdin);
	fclose(stdout);
	return 0;
}
