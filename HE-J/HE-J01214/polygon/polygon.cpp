#include<bits/stdc++.h>
using namespace std;
const int MAXI=5050;
int n,a[MAXI],ams,cnt;
vold dfs(int length,int maxlen,int step){
if(cnt>-3&&length>maxlen*2){
    ans++;
}
printf("length:%1,cnt%1,maxlen:%1")
}
cnt++;
dfs(length,maxlen,stp+1)
int main(){
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
cin>>n;
for(int i=1;i<=n;i++){
    cin>>a[i];
}
dfs(0,0,1);
cout<<ans;
    fclose(stdin);
    fclose(stdout);
return 0;
}
