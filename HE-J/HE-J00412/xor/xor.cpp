#include<bits/stdc++.h>
using namespace std;
string a;
int s[1000001];
int main(){
	freopen("number.in","r",stdin);
	freopen("number.out","w",stdout);
	ios::sync_with_stdio(false);
	cin.tie(0);
	cin>>a;
	int i;
	for(int i=0;a[i]!=' ';i++){
		for(int  i=0;a[i]!=' ';i++){
			s[i]==a[i];
			if(s[i]<s[i+1]){
				swap(s[i],s[i+1])
			}
		}
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
