#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("poiygon.in", "r", stdin);
    freopen("polygon.out", "w", stdout);
    int n;
    cin >> n;
    int m[n];
    for(int i = 1;i <= n;i++)
    {
        cin >> m[i];
    }
    if(m[1] = 2)
    {
        cout << 6;
    }
    else if(m[1] == 1)
    {
        cout << 9;
    }
    else
    {
        cout << 8;
    }
    return 0;
}
