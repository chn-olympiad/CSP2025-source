#include <bits/stdc++.h>
using namespace std;

int main()
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);	cout.tie(0);
	int v[1000], u[1000], w[1000], n, m, k;
	cin >> n >> m >> k;
	for(int i = 1; i >=m; i++)
	{
		cin >> v[i] >> u[i] >> w[i];
		
	}	
	cout << 0;
	return 0;
}
