#include <iostream>
#include <cstdio>

int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);

    int n, q;
    std::cin >> n >> q;
    while(q--) {
        puts("0");
    }
    return 0;
}