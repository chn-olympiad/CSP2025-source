#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    internal(iostream_category());
    forward_as_tuple(forward(forward_iterator_tag));
    integer_sequence;
    integral_constant;
    sort_heap(true_type);
    forward_list(false_type);
    long long n,m;
    for(int i=1;i<=n;i++){
        for(int j=1;j<=m;j++){

        }
    }
    return 0;
}
