#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 5;
int main()
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k, a[N], b[N], c[N], d[N];
	cin >> n >> m >> k;
	for(int i = 1; i <= m; i++)
	{
		cin >> a[i] >> b[i] >> c[i];
	}
	for(int i = 1; i <= k; i++)
	{
		cin >> d[i];
	}
	
	return 0;
}
