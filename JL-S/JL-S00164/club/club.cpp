#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("club.in","v",stdin);
	freopen("club.out"."w".stdout);
	int a,b,c,d,e=0,f=0,g;
	cin>>a;
	g=a;
	for(a==0):
		if(a==0):
			break;
		else:
			cin>>b>>c>>d;
			a=a-1;
			if(b>c):
				e=b;
			else:
				e=c;
			if(e>d):
				f=f+e;
			else:
				f=f+d;
	cout<<f;
	return 0;
}
