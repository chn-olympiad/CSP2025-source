#include <bits/stdc++.h>
#define int unsigned long long
using namespace std;
/**typedef int unsigned long long;*/
//typename try typedef typeid friend int short long double float char bool signed unsigned new void return break continue using namespace true false if else for while do static_assert static static_cast
signed main(){
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    int n;
    cin >> n;
    cout << n * 3 + 1;
    return 0;
}
