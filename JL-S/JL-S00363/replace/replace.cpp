#include<bits/stdc++.h>
using namespace std;

const int N=1e4+100;

long long n,m,k;
long long u[N],v[N],w[N];
int squ[N][N]

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++)
    {
        cin>>u[i]>>v[i]>>w[i];
    }
    return 0;
}
