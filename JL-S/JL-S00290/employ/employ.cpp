#include <bits/stdc++.h>
using namespace std;
int n, m, c[i];
string s;
int main()
{
    freopen("clud.in", "r",stdin);
    freopen("clud.out", "w", stdout);
    cin >> n >> m;
    cin >> s;
    for (int i = 1;i <= n;i++)
    {
        cin >> c[i];
    }

    return 0;
}
