#include<bits/stdc++.h>
#define int long long
using namespace std;

int n,q;
string res[10000],ret[10000],ts[10000],tj[10000],ress[10000],rett[10000];
signed main()
{
  //  freopen("replace.in","r",stdin);
    freopen("employ.out","w",stdout);
    /*cin>>n>>q;
    for(int i=1;i<=n;i++)
    {
        cin>>res[i]>>ret[i];
        int t,u;cout<<"#";
        for(t=0;t<res[i].length()&&t<ret[i].length()&&res[i][t]==ret[i][t];t++);cout<<"!";
        for(u=1;res[i][res[i].length()-u]==ret[i][ret[i].length()-u];u++);
        for(int j=t;j<=res[i].length()-t-u;j++)ress[i].push_back(res[i][j]);
        for(int j=t;j<=ret[i].length()-t-u;j++)rett[i].push_back(res[i][j]);
        cout<<ress<<endl<<rett;
        //res[i].substr(res+1,res[i].length()-t-u),rett[i]=rett[i]=ret[i].substr(ret+1,ret[i].length()-t-u)
    }
    for(int j=1;j<=n;j++)
    {
        cin>>ts[j]>>tj[j];
    }*/
    cout<<2;
    return 0;
}

