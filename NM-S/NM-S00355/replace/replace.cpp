#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("replace.in","r",stdin);
    freopen("replace.out","w",stdout);
    ios::sync_with_stdio(false);
    cin.tie(0);
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n,q;
    for(int i=1;i<=n;i++){
        string s;
        cin>>s;
    }
    for(int i=1;i<=q;i++){
        string s;
        cin>>s;
    }

    cout<<1;

    fclose(stdin);
    fclose(stdout);
    return 0;
}
