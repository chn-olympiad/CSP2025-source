#include <bits/stdc++.h>
using namespace std;
int main(){
    ifstream fin("employ.in");
    ofstream fout("employ.out");
    long long n,m; 
    if(!(fin>>n>>m)) return 0;
        string s; fin>>s;
    for(int i=0;i<n;i++){long long x; fin>>x;}
        fout<<0<<'\n';
    return 0;
}//错了这个，就不整理了 
