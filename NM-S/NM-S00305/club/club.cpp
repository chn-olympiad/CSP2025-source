#include<bits/stdc++.h> 
using namespace std;
int t,a,b,maxn,node;
int a[4],b[4];
bool cmp(node.a,node.b)
int main()
{
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	ios:sync with stdio(0),cin.tie(0),cout.tie(0);
	cin>>t;
	while(t--)
	{
		cin>>n;
		int m=n/2;
		for(int i=1;i<=n;i++)
		{
			sort(a+1,a+4,cmp)
			for(int j=1;j<=3;j++)
			{
				cin>>a[i].date;
				a[i].num='1';
				if([a[j]]+1)<=m;
				  {
				  	maxn=a[j];
				  	b[a[j].num]++;
				  	cout<<b[1]<<" "<<b[2]<<""<<b[3]<<endl;
				  	cout<<maxn;
				  	break;
				  }
			}
		}
		
		cout<<maxn1<<"/n";
		maxn=0;
		
	}
	 fclose(stdin);
	 fclose(stdout);
	return 0;
}
