#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main() {
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m;
    int s[n];
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> s[n];
    }
    for (int i = 0; i < n; i++) {
        cin >> i;
    }
    return 0;
}
