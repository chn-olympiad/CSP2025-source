#include <bits/stdc++.h>
#include <iostream>
using namespace std;
int main() {
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    int s, n, q;
    int x, y, z;
    s = x + y + z;
    cin >> n >> q;
    for (int i = 0; i < q; i++) {
        cin >> s;
    }
    return 0;
}
