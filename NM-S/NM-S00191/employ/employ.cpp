#include <iostream>

int main()
{
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int
        n, //Counts of members
        m; //counts want
    std::cin >> n;
    bool s[n];
    int c[n];
    std::string input; //Get the abilities of each day without space
    std::cin >> input;
    for (int i = 0; i < input.length(); i++)
    {
        std::string input; //Get the abilities of each day without space
        int err[n];
        for (int i = 0; i < n; i++)
        {
            
        }
    }
    fclose(stdout);
    fclose(stdin);
    return 0;
}