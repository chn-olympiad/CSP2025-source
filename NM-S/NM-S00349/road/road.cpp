#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int n, m, k;
//vector<pair<int, int>> m[10005];
int main(){
    /*cin  >>  n  >>  m  >>  k;
    for(int i = 0;i < m;++i){
        cin  >>  u  >>  v  >>  w;
        m[u].push_buck(v, w);
        m[v].push_buck(u, w);
    }
    */
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    cout  <<  8;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
