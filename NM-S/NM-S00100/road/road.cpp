#include <bits/stdc++.h>
using namespace std;
int n,m,k,a[1000011],b[11];
int main()
{
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    cin >> n >> m >> k;
    for (int i = 1;i <= m;i++)
        cin >> a[i];
    for (int i = 1;i <= k;i++)
        cin >> b[i];
    cout << 1;
    return 0;
}
