#include<bits/stdc++.h>
using namespace std;
int m,n,p,q;
int c[505];
bool s[505];
long long c;
long long mt(int k){
    long long an=1,ls=1;
    for(int i=1;i<=k;i++){
        ls*=k;
    }
}
int main()
{
    freopen("employ.in","r",stdin);
    freopen("employ.out","w",stdout);
    cin>>n>>m;
    for(int i=0;i<n;i++){
        scanf("%1d",&s);
    }
    for(int i=0;i<n;i++){
        scanf("%d",&c);
    }
    sort(c,c+n);
    for(int i=0;i<n;i++){
        if(c[i]<=p||s[i]==0)p++;
        else q++;
    }
    for(int i=0;i<=mt(n))
    return 0;
}
