#include<bits/stdc++.h>
using namespace std;
struct node(int y x,y,z;)
int maxx[100005];
struct[a][maxx];
int first,second,third;
int main()
{ freopen("club.in","r",stdin);
  freopen("club.out","w",stdout);
  int t,maxx=2e9;
  cin>>t;
  while(t--)
    {int n;
      cin>>n;
      for(int i=1;i<=n;i++)
             {cin>>a[i].x>>a[i].y>>a[i].z;
             maxx[i]=max(max(a[i].x,a[i].y,a[i].z);}
    }
   int ans=0;
      
      if(maxx[i]=a[i].x&&first<=n/2) ans=max[i];
     else ans=a[i].x;
     if(maxx[i]=a[i].x&&second<=n/2) ans=max[i];
     else ans=a[i].x;
     if(maxx[i]=a[i].x&&third<=n/2) ans=max[i];
     else ans=a[i].x;
     if(maxx[i]=a[i].y&&first<=n/2) ans=max[i];
     else ans=a[i].y;
     if(maxx[i]=a[i].y&&second<=n/2) ans=max[i];
     else ans=a[i].y;
     if(maxx[i]=a[i].y&&third<=n/2) ans=max[i];
     else ans=a[i].y;
     if(maxx[i]=a[i].z&&first<=n/2) ans=max[i];
     else ans=a[i].z;
     if(maxx[i]=a[i].z&&second<=n/2) ans=max[i];
     else ans=a[i].z;
     if(maxx[i]=a[i].z&&third<=n/2) ans=max[i];
     elae ans=a[i].z;
     
     
  fcliose(stdin);
  fclose(stdout);
  return 0;
}
