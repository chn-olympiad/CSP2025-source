#include<bits/stdc++.h>
using namespace std;
struct city
{
    int u,v,w;
}ci[1000000];
struct vil
{
    int mon;

}vi[10];
int mian()
{
    freopen("road.in","r",stdin);
    freopen("road.out"."w",stdout);
	int n,m,k;
	vector<int> cityroad(vector<int>(vector<int>));
    city head;
    int vilmoney[k];
	for(int i=0;i<m;i++)
	{
        cin>>ci[i].u;
        cin>>ci[i].v;
        cin>>ci[i].w;
	}
    for(int i=0;i<k;i++)
    {
        cin>>vilmoney[i];
        for(int j=0;j<n,j++)
        {
            int x;
            cin>>x;
        }
    }
    fclose(stdin);
    fclose(stdout);
	return 0;
}
