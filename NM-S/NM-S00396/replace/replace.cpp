#include <iostream>
#include <cstdio>
#include <algorithm>
#include <string>
#include <utility>
#include <vector>
#define notGiveUp false // (0.0) --False--> X(
using namespace std;
int n,q;string a;
vector<pair<string,string>> s;
vector<pair<string,string>> question;
int llength = 0;int rlength = 0;
void solve(){
    /* a very super effenciently and pretty good code to AK CSPS */
}
int main(){
    freopen("./replace.in","r+",stdin);
    freopen("./replace.out","w+",stdout);
    cin>>n>>q;string tmp1,tmp2;
    if(notGiveUp){
    for(int i=n;i;i--){
        cin>>tmp1>>tmp2;
        s.push_back(make_pair(tmp1,tmp2));
    }
    for(int i=q;i;i--){
        cin>>tmp1>>tmp2;
        question.push_back(make_pair(tmp1,tmp2));
    }
    // a.find();
    a.substr();a.substr();a.substr();}
    else{for(;q;q--){cout<<0<<endl;}}// :( I have no way to pass.
    fclose(stdin);
    fclose(stdout);return 0;
}
