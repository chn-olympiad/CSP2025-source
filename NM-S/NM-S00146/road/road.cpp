#include<bits/stdc++.h>
using namespace std;

int n,m,k,p,u,v,w,c,a;
int main()
{
    freopen("road.in","r",stdin);
    freopen("road.out","w",stdout);
    for(int i = i+1; i <= m; ++i)
    {
        cin >> u >> v >> w;
        for(int j = i + m + 1; j <= k; ++j)
            cin >> c >> a;

        }
    cout << p;

    return 0;
}
