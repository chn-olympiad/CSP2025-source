#include<stdio.h>
#include<vector>
#include<queue>
#include<list>
using namespace std;

int main (void) {
    FILE *
    freopen(club.in,"r",stdin);
    freopen(club.out,"w",stdout);
    unsigned int n,m,k;    
    cin >> n >> m >> k;
    std::vector<int [3]> edges
    for (int i = 0;i < m;i++) {
        cin >> edges[i,1] >> edges[i,2] >> edges[i,3];
    } 

    fclose();
    return 0;
}
