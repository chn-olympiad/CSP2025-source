#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    int n, m;
    cin >> n >> m;
    string d;
    cin >> d;
    vector <int> c(n);
    for (int i = 0; i < n; i++) {
        cin >> c[i];
    }
    return 0;
}