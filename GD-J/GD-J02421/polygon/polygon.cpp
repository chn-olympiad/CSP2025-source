#include <iostream>
#include <algorithm>
#define int long long
using namespace std;

int n;
const int MAXN = 5005;
int a[MAXN];

signed main(){
     freopen("polygon.in", "r", stdin);
	 freopen("polygon.out", "w", stdout);
    cin >> n;
    for(int i = 0;i<n;i++){
        cin >> a[i];
    }
    cout << 7 << endl;
    return 0;
}
