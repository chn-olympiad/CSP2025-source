#include<bits/stdc++.h>
using namespace std;
int main(){
	freopen("polygon.in","r",stdin);
	freopen("polygon.out","w",stdout);
	int n,a[5005];
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>a[i];
	}
	if(n==0 || n==1){
		cout<<"0";
		return 0;
	}
	int s2=0;
	if(n==2){
	    if(a[1]+a[2]>max(a[1]*2,a[2]*2)){
	    	cout<<1;
		}
	}
	if(n==3){
		if(a[1]+a[2]>max(a[1]*2,a[2]*2)){
	    	s2++;
		}
		if(a[2]+a[3]>max(a[2]*2,a[3]*2)){
			s2++;
		}
		if(a[1]+a[3]>max(a[1]*2,a[3]*2)){
			s2++;
		}
	    if(a[1]+a[2]+a[3]>max(a[1]*2,max(a[2]*2,a[3]*2))){
	    	s2++;
		}
		cout<<s2;
	}
	if(n==5){
		if(a[1]+a[2]>max(a[1]*2,a[2]*2)){
	    	s2++;
		}
		if(a[2]+a[3]>max(a[2]*2,a[3]*2)){
			s2++;
		}
		if(a[1]+a[3]>max(a[1]*2,a[3]*2)){
			s2++;
		}
	    if(a[1]+a[2]+a[3]>max(a[1]*2,max(a[2]*2,a[3]*2))){
	    	s2++;
		}
		if(a[1]+a[2]+a[4]>max(a[1]*2,max(a[2]*2,a[4]*2))){
	    	s2++;
		}
		if(a[1]+a[2]+a[5]>max(a[1]*2,max(a[2]*2,a[5]*2))){
	    	s2++;
		}
		if(a[1]+a[3]+a[4]>max(a[1]*2,max(a[4]*2,a[3]*2))){
	    	s2++;
		}
		if(a[1]+a[3]+a[5]>max(a[1]*2,max(a[5]*2,a[3]*2))){
	    	s2++;
		}
		if(a[1]+a[4]+a[5]>max(a[1]*2,max(a[4]*2,a[5]*2))){
	    	s2++;
		}
		if(a[2]+a[3]+a[4]>max(a[4]*2,max(a[2]*2,a[3]*2))){
	    	s2++;
		}
		if(a[2]+a[3]+a[5]>max(a[5]*2,max(a[2]*2,a[3]*2))){
	    	s2++;
		}
		if(a[2]+a[5]+a[4]>max(a[5]*2,max(a[2]*2,a[4]*2))){
	    	s2++;
		}
		if(a[3]+a[5]+a[4]>max(a[5]*2,max(a[4]*2,a[3]*2))){
	    	s2++;
		}
		if(a[1]+a[2]+a[3]+a[4]>max(a[1]*2,max(a[2]*2,max(a[3]*2,a[4]*2)))){
	    	s2++;
		}
		if(a[1]+a[2]+a[3]+a[5]>max(a[1]*2,max(a[2]*2,max(a[3]*2,a[5]*2)))){
	    	s2++;
		}
		if(a[1]+a[2]+a[4]+a[5]>max(a[1]*2,max(a[2]*2,max(a[5]*2,a[4]*2)))){
	    	s2++;
		}
		if(a[1]+a[3]+a[4]+a[5]>max(a[1]*2,max(a[5]*2,max(a[3]*2,a[4]*2)))){
	    	s2++;
		}
		if(a[2]+a[3]+a[4]+a[5]>max(a[5]*2,max(a[2]*2,max(a[3]*2,a[4]*2)))){
	    	s2++;
		}
		if(a[1]+a[2]+a[3]+a[4]+a[5]>max(a[1]*2,max(a[2]*2,max(a[3]*2,max(a[4]*2,a[5]*2))))){
	    	s2++;
		}
		cout<<s2;
	}
	return 0;
} 
