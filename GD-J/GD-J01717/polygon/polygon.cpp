#include<bits/stdc++.h>
using namespace std;
const int N=5005,P=998244353;
int n,a[N],f[N],as;
int main(){
#ifndef ASTRALFLARE_CE
    freopen("polygon.in","r",stdin);
    freopen("polygon.out","w",stdout);
#endif
    cin>>n;
    for(int i=1;i<=n;++i)cin>>a[i];
    sort(a+1,a+n+1);
    // AFK I'm gonna sleep
    // w Not easy to think
    // Maybe 300 / 400 pts

    // A good way if you dont want to forget using freopen while disabling freopen
    // when you are debugging:
    // #ifndef SOME_CUSTOM_VAR
    // freopen...
    // #endif
    // and compile with -DSOME_CUSTOM_VAR
}