#include <bits/stdc++.h>

using namespace std;

const int N = 5009;

int n;
int a[N];

int main() {
	freopen("polygon.in", "r", stdin);
	freopen("polygen.out", "w", stdout);
	scanf("%d", &n);
	for (int i = 1; i <= n; ++i)
		scanf("%d", a[i]);
	printf("6\n");	
	return 0;
}
