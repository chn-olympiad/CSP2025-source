#include <bits/stdc++.h>
using namespace std;
int a,b,c,d;
struct s;
int cmp(a,b,c,d)
{
	return(a[x].s)a[y].s)
}
int main()
{
	//freopen("seat.in","r",stdin)
	//freopen("seat.out,"w",stdout)
	int m,n;
	cin >> m,n
	scanf("%d","%d",&m,&n);
	for(int i=1;i<=n*m;i++)
	{
		a[i].m=i;
		scanf("d",&a[i].s);
	}
	sort(a+1,a+1=m*n,cmp);
	return 0;
	//fclose("seat.in")
	//fclose("seat.out")
}
