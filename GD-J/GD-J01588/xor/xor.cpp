#include <bits/stdc++.h>

using namespace std;
const int N = 5e3 + 5;

int n;
int a[N];

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> n;
	for (int i = 1; i <= n; i++)
		cin >> a[i];
	for (int i = 3; i <= n; i++)
	{
		for (int j = 1; j <= n; j++)
		 
	}
	return 0;
} 
