#include<bits/stdc++.h>
using namespace std;
/*int f(vector<int> a,int n,int b){
	vector<int> x;
	vector<int> y;
	x.copy
	if(n<3){
		return 0;
	}
	if(sum(a,b,n-1)>a[n-1]*2){
		return f(a,n-1,b+1)
	}
}
int sum(int a[],int b,int e){
	int s=0;
	for(int i=b;i<=e;i++){
		s+=a[i];
	}
	return s;
}*/
int main(){
	freopen("polygon1.in","r",stdin);
	freopen("polygon.out","w",stdout);
	int n,su = 0,sui =0;
	cin>>n;
	int i[n];
	for(int x=0;x<n;x++){
		cin>>i[x];
	}
	sort(i,i+n);
	/*for(int x=0;x<n;x++){
		for(int )
	}*/
	cout<<9;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
