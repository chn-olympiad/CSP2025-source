#include<bits/stdc++.h>
using namespace std;
int n, k, cnt;
int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> k;
	vector<int>a(n), sum;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	for (int i = 0; i < n; i++) {
		for (int j = i + 1; j < n; j++) {
			
		}
	}
	fclose(stdin);
	fclose(stdout); 
	cin >> n >> k;
	return 0;
}
