#include <bits/stdc++.h>
using namespace std;
#define int unsigned long long

int a[500005], yh[200000];
signed main() {
	freopen ("xor.in", "r", stdin);
	freopen ("xor.out", "w", stdout);
	int n, k;
	cin >> n >> k;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	for (int i = 0; i < n; i++) {
		
	}
	fclose (stdin);
	fclose (stdout); 
	return 0;
}
