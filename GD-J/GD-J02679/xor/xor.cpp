#include<bits/stdc++.h>
using namespace std;
#define MaxN 500001
int n, k, a[MaxN];
int main(){
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n >> k;
	for(int i = 1; i <= n; i++) cin >> a[i];
	cout << "None." << endl;
	return 0;
}
/* None. */
