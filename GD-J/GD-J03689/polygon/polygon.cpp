#include<bits/stdc++.h>
using namespace std ;
int main()
{
	char in ;
	freopen( "polygon.in" , "r" , stdin) ;
	freopen( "polygon.out" , "w" , stdout) ;
	for(int i = 0 ; i < 5 ; i ++)
	{
		std::cin >> in ; 
	}
	std::cout << "9" ;
	fclose(stdin) ;
	fclose(stdout) ;
	return 0 ;
}
