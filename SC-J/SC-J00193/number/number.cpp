#include<bits/stdc++.h>
using namespace std;
int main(){
	char s;
	cin>>s;
	int c=0,b=1,a=0,e=0;
	for(int i=0;i<=b;i++){
		b++;
		if(int(s[i])=49||int(s[i])=50||int(s[i])=51||int(s[i])=52||int(s[i])=53||int(s[i])=54||int(s[i])=55||int(s[i])=56||int(s[i])=57){
			c+=1;
				
		}
	}
	char d[c];
	for(int n=0;n<=c;n++){
        for(int i=0;i<=b;i++){
		    b++;
		    if(int(s[i])=49||int(s[i])=50||int(s[i])=51||int(s[i])=52||int(s[i])=53||int(s[i])=54||int(s[i])=55||int(s[i])=56||int(s[i])=57){
			    d[n]=s[i];
			    continue;
		}
	}
}
	for(int m=0;m<=c;m++){
		if(d[m]){
			
		}
}
	
	
    cout<<a;
    return 0;
}