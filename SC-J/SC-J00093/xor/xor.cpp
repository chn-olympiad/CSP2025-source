#include<bits/stdc++.h>
#include<bits/stdc++.h>
using namespace std;
const int N=1e6;
string sq[N];
int a[N];
int n,m;
char ch[N];
int main(){
	cin>>n>>m;
	for(int i=1;i<=n;++i){
		cin>>a[i];
	}
	/*while(cin>>ch){
		n++;
		for(int i=1;i<=n;++i){
			if(ch[i]<='0'||ch[i]>='9'){
			ch[i]=' ';
			break;
			//cout<<ch;
			//continue;
		}
		if(ch[i]>='0'||ch[i]<='9'){
			if(ch[i]>ch[i-1]); 
			swap(ch[i],ch[i-1]);
			ch[i]=ch[i];
			//continue;
		}
		cout<<ch[n];
	}
}
	return 0;
}
	for(int i=1;i<=n*m;++i){
		cin>>a[i];
		//a[1]=b[1];
		
		//sort(a+1,a+n+1);
		//i=i;
		//a[i]=a[i];
		//a[i]=b[1];
		//if(i<=m) m=m-i+1;
		//if(i>m&&i<=m+n) n=n+i+1;
		//if(i-m-n>n) m=m+1+i;
	
	}
	sd=a[1];
		if(sd=100)
		  cout<<1<<" "<<1;
		if(sd<100&&sd>98)
		cout<<n<<m-1;
		if(sd<=95){
	     	n=n-2+1;
		    cout<<n<<" "<<m;*/
		    cout<<2;
		    return 0;
	}