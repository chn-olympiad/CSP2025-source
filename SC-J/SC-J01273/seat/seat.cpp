#include <bits/stdc++.h>
using namespace std;
int n,m,c,r;
int main(){
	freopen("seat.in","r",stdin)
	freopen("seat.out","w",stdout)
	cin>>n>>m>>c>>r;
	for(int i<=20,i=n*m,i++)
		n=i-1/20;
		m=i+1/20;
		if(n>m&&n>x&&n>c)
		cout<<n;
		else if(m>n&&m>x&&m>c)
		cout<<m;
		else if(x>m&&x>n&&x>c)
		cout<<x;
		else if(c>m&&c>x&&c>n)
		cout<<c;
		if(n>m&&m<c&&c<x&&x>n)
		cout<<x<<c<<m<<n;
		if(n>m&&m>c&&c<x&&x>n)
		cout<<n<<m<<x<<c;
		if(n<m&&m<c&&c<x)
		cout<<x<<c<<n<<m;
		if(n<m&&m>c&&c>x&&x>n)
		cout<<m<<c<<n<<x;
	return 0;
		
}