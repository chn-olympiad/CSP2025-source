#include<bits/stdc++.h>
using namespace std;
int a,n,k;
for(a=2,a>=n,a++){
	for(n=1,n>=k,n++)
	for(k=0,k>=a,k++)
for(a=0,a<=n,a--){
	for(n=1,n<=k,n--)
	for(k=2,k<=a,k--)
}
if([a][n]=[a+1][n+3]=[k]){
	if([a][k]=[a+3][k+1]=[n])
	if([n][k]=[n+1][k+3]=[a])
	if([n][a]=[n+1][a+3]=[k])
	if([k][a]=[k+3][a+1]=[n])
	if([k][n]=[k+1][n+3]=[a]) 
}
else(a,n,k<=[a,n,k]){
	else(a<=[a,n,k])
	else(n<=[a,n,k])
	else(k<=[a,n,k])
	else(a<=[a])
	else(n<=[n])
	else(k<=[k])
	else+(a+n+k<=[a+n+k])
	else(a,n<=[a,n])
	else(a,k<=[a,k])
	else(n,k<=[n,k])
}
    cout<<a<<n<<k;
    cin>>a>>n>>k;
    return 0;
} 