#include <bits/stdc++.h>

using namespace std;

int n, a[1000010];
int main() {
	freopen("xor.in", "r", stdin);
	freopen("xor.out", "w", stdout);
	cin >> n;
	for(int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 1;//不可以，总司令（骗分）
	return 0;
}