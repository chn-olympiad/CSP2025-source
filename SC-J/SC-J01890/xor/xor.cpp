#include <bits/stdc++.h>

using namespace std;

const unsigned long long N = 2 * 1e8;
int n, k;
int a[N];

int main(){
	freopen("xor.in","r",stdin);
	freopen("xor.out","w",stdout);
	ios::sync_with_stdio(0);
	cin.tie(NULL), cout.tie(NULL);
	cin >> n >> k;
	for (int i = 1;i <= n;++i){
		cin >> a[i];
	}
	cout << k;
	return 0;
}

/*

I think I can have 100 score!
a, the score of all
好了不装外国人了~

*/