#include <iostream>
#include <fstream>
#include <stdio.h>
using namespace std;
int n, m;
int p;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cin >> n >> m;
	string s;
	cin >> s;
	for (int i = 1; i <= n; i++) {
		cin >> p;
	}
	cout << 2;
	return 0;
}