#include <bits/stdc++.h>
using namespace std;
const int N = 5e6 + 10;
int n, q;
string s1, s2;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	while (n--) {
		cin >> s1;
	}
	while (q--) {
		cin >> s2;
		cout << 0 << endl;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}