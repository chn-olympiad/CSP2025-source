#include <bits/stdc++.h>
using namespace std;
int n, m;
string s1;
int c[114514];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n >> m >> s1;
	for (int i = 1; i <= n; i++) {
		cin >> c[i];
	}
	cout << 0;
	return 0;
}