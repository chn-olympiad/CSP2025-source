#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const ll mod = 988244353;

ll n, m, ans, c[505], t[505], fac[505], f;
string s;

int main () {
	freopen("employ.in", "r", stdin);
	freopen("empoly.out", "w", stdout);
	cin >> n >> m;
	cin >> s;
	s = " " + s;
	for (int i = 1; i <= n; i++) {
		cin >> c[i];
	}
	return 0;
}