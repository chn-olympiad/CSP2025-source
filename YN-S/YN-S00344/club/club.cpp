#include <bits/stdc++.h>
using namepace std;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int l=a,n=b,j;
	cin<<a<<b;
	for (int"a/2,a>0,a++");
	{
		else("a<=i<=b"){
			j={i+l+i+n}={1,2,3}*i
		};
	}
	cout>>j;
}

retarn 0;