#include <bits/stdc++.h>
using namepace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n = a, m = x, k = b;
	int i + 1;
	{
		int strcmp(char *s1, char *s2) {
			ui"//"vi"//"wi
		}
	}
	int  j + m + 1;
	{
		int strcmp(char *s1, char *s2) {
			n + 1(n > (-1))"//"j
		}
	}
	cin << j
	    cout>>n;

}
retarn 0;