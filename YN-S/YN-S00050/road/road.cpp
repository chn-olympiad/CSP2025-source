#include <bits/stdc++.h>
using namespace std;
int b;

int main() {
//	freopen("road.in","r",stdin);
//	freopen("road.out","w",stdout);
	int m, n, k;
	cin >> m >> n >> k;
	int a[b];
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
		for (int i + 1; i <= n; i++) {
			for (int j = 1; j <= n + 1; j++) {

			}
		}
	}

	cout << 13;

	return 0;
}