#include <bits/stdc++.h>
using namespace std;
int b;
int u;


int main() {
//	freopen("employ.in","r",stdin);
//	freopen("employ.out","w",stdout);
	int p[b];
	int t[u];
	int n, m;
	int sum = 0;
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		cin >> p[i];
		if (p[i] == t[u]) {
			sum++;
		}



	}
	return 0;
}