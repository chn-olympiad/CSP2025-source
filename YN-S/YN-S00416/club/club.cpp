#include <bits/stdc++.h>
using namespace std;
int num;
freopen("club.in", "r", stdin);
freopen("club.out", "w", stdout);

{
	if (cin >> main)
		int n;
	cin >> n;
	if (cin >> main >> n)
		n + 1;
	n + 1 + 1;
	^
	cout << n + 1 << n + 1 + 1 << n << main << endl;
	else
		break;
	cout << main << n << endl;
	return 0;
}
