#include <bits/stdc++.h>
using namespace std;
#define int long long
int n, m;
string s;

signed main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m >> s;
	for (int i = 1, x; i <= n; i++) {
		cin >> x;
	}
	cout << 1;
	return 0;
}