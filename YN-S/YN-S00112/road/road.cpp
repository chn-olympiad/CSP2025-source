#include <bits/stdc++.h>
using namespace std;
int n, m;

int main() {
	scanf("%d%d", &n, &m);
	char s[n + 5];
	scanf("%s", &s);
	int c[n + 5];
	for (int i = 1; i <= n; i++)
		scanf("%d", &c[i]);

	return 0;
}