#include <bits/stdc++.h>
using namespace std;
int n, q;

int main() {
	scanf("%d%d", &n, &q);
	char s1[101], s2[101], t1[101], t2[101];
	for (int i = 1; i <= n; i++)
		scanf("%s%s", &s1, &s2);
	for (int j = 1; j <= q; j++)
		scanf("%s%s", &t1, &t2);
	return 0;
}