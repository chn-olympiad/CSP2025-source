#include <bits/stdc++.h>
using namespace std;
int n, m, k;
int c[n + 3];

int main() {
	scanf("%d%d%d", &n, &m, &k);
	int v1, v2, v3;
	for (int i = 1; i <= m; i++)
		scanf("%d%d%d", &v1, &v2, &v3);
	for (int j = 1; j <= k; j++)
		for (int i = 1; i <= n + 1; i++)
			scanf("%d", &c[i];)
			return 0;
}