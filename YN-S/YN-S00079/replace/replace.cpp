#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int q, n;
	cin >> q >> n;
	for (int i = 0; i < n; i++) {
		cout << 0 << endl;
	}
	return 0;
	fclose(stdin);
	fclose(stdout);
}
