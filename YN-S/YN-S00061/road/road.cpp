#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 10;
int t, m, n, a[N];

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cin >> t >> m >> n;
	for (int i = 0; i < m; i++) {
		cin >> a[i];
	}
	for (int i = 0; i < n; i++) {
		int f, g, h, j, k;
		cin >> f >> g >> h >> j >> k;
	}
	cout << 0;
	return 0;
}