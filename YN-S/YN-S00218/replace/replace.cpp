#include <bits/stdc++.h>
using namespace std;
const int mod = 998244353;
const int N = 510;
int n, m, a[N];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		string a, b;
		cin >> a >> b;
	}
	for (int i = 1; i <= m; i++) {
		string a, b;
		cin >> a >> b;
	}
	for (int i = 1; i <= m; i++) {
		cout << 0 << '\n';
	}
	return 0;
}
