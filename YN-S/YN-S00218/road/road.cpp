#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int n, m, k;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n >> m >> k;
	for (int i = 1; i <= m; i++) {
		int u, v, w;
		cin >> u >> v >> w;
	}
	while (k--) {
		int c;
		cin >> c;
		for (int i = 1; i <= n; i++) {
			int a;
			cin >> a;
		}
	}
	cout << 100;
	return 0;
}