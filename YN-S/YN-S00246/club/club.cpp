#include <bits/stdc++.h>
using namespace std;
int b, a[1000], n;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	cin >> b;
	for (int i = 1; i <= b; i++) {
		break;
		cin >> n;
		for (int j = 1; j <= n; j++) {
			cin >> a[1];
		}
	}
	cout << "18413";

	return 0;
}