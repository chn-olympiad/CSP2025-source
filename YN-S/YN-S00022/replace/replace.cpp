#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef string str;
ll n, q;
str temp;
signed main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	cin >> n >> q;
	for (int i = 0; i <= n + q; i++)	cin >> temp;
	for (int i = 0; i < q; i++) cout << 0 << endl;

	return 0;
}

