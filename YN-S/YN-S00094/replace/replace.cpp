#include <bits/stdc++.h>
using namespace std;
char a;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m;
	cin >> n >> m;

	for (int i = 1; i <= (n + m) * 2; i++) {

		cin >> a;
	}

	for (int i = 1; i <= m; i++) {

		cout << "0" << endl;
	}
}