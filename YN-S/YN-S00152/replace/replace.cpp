#include <bits/stdc++.h>
using namespace std;

int main () {
	freopen("replace.out", "w", stdout);
	freopen("replace.in", "r", stdin);
	int n, q;
	cin >> n >> q;
	while (q--) {
		cout << 0 << endl;
	}
	return 0;
}