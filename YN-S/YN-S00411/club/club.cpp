#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int t, n, a[100005];
	cin >> t >> n;
	while (t) {
		for (int i = 1; i <= n; i++) {
			for (int j = 1; j <= 3; j++) {
				cin >> a[i];
			}
		}
	}
	cout << 18 << endl << 4 << endl << 13 << endl;

	return 0;
}