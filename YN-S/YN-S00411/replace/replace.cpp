#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	string a;
	cin >> n >> q;
	cout << 0 << endl << 0 < endl << 0 << endl << 0 << endl;

	return 0;
}