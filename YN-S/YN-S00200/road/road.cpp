#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	int a[m], b[k];
	for (int i = 0; i <= m; i++)
		cin >> a[i];
	for (int j = 0; j <= k; j++) {
		cin >> b[j];
	}
	cout << 13;
	return 0;
}