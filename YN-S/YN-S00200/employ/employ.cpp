#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen(".in", "r", stdin);
	freopen(".out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string s;
	int c[n];
	cin >> s;
	for (int i = 0; i <= n - 1; i++) {
		cin >> c[i];
	}
	return 0;
}