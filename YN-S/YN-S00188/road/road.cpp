#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	int n, m, k;
	cin >> n >> m >> k;
	while (m--) {
		int x, y, z;
		cin >> x >> y >> z;
	}
	while (k--) {
		int a, b, c, d;
		cin >> a >> b >> c >> d;
	}
	cout << 100;
	return 0;
}