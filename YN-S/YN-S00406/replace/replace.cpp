#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	char a, b;
	for (int i = 0; i <= 7; i++) {
		for (int j = 1; j < 3; j++) {
			cin >> a >> b;
		}
	}
	cout << "2" << endl << "0";
	return 0;
}