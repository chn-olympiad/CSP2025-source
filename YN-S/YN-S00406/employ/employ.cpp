#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	int a;
	cin >> a;
	int s, d, f, g, h, j, k, l, y, t;
	if (n == 3) {
		cin >> s >> d >> f;
		cout << "2";
	} else {
		cin >> s >> d >> f >> g >> h >> j >> k >> l >> y >> t;
		cout << "2204128";
	}
	return 0;
}