#include <bits/stdc++.h>
#define For(a,b,i) for(int i=a;i<=b;i++)
using namespace std;
int n, m;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);

	cin >> n >> m;
	For(1, m, i) {
		cout << 0 << endl;
	}

	return 0;
}