#include <bits/stdc++.h>
using namespace std;
int m [100000], a[100], b[100], c[100];
struct sat;

int main() {
	freopen("club.in","r",stdin);
	freopen("club.out","w",stdout);
	int t;
	cin >> t;
	for (int j = 1; j <= t; j++) {
		cin >> m[j];
		for (int i = 1; i <= m[j]; i++) {
			cin >> a[i] >> b[i] >> c[i];

		}
	}

	cout << 18 << endl << 4 << endl << 13 << endl;
	/* bool cmp(int x,int y)
	if(x>y)
	sort*/



	return 0;
}