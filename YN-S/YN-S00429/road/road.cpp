#include <bits/stdc++.h>
using namespace std;
int n, m, k, ans = 0;
int v[1000005], aj[15];

int main() {
//    freopen("road.in","r",stdin);
//    freopen("road.out","w",stdout);
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	//ÌØA
	cin >> n >> m >> k;
	for (int i = 1; i <= m; i++) {
		cin >> v[i];
	}
	for (int i = 1; i <= k; i++) {
		cin >> aj[i];
	}

	return 0;
}