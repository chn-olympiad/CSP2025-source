#include <bits/stdc++.h>
using namespace std;
long long n, m;
string s;

signed main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	cin >> s;
	for (int i = 1; i <= n; i++) {
		int a;
		cin >> a;
	}
	cout << "0";
	return 0;
}