#include <bits/stdc++.h>
using namespace std;
const int N = 505;
int a[N];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	string s;
	cin >> n >> m >> s;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << (long long)pow(2, rand()) % 998224353;
	return 0;
}