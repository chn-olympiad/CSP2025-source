#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("club.in", "r", stdin);
	freopen("club.out", "w", stdout);
	int t, n, m, l, o, i, u;
	cin >> t;
	cin >> n;
	for (int i = 1; i <= t * n; i++) {
		cin >> i;
	}
	cout << 4;
	return 0;
}