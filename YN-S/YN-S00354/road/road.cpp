#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;

	for (int i = 1; i <= m; i++) {

		int x;
		cin >> x;
	}

	for (int i = 1; i <= k; i++) {

		for (int j = 1; j <= n + 1; j++) {

			int z;
			cin >> z;
		}
	}

	cout << n + m + k * 2;
	return 0;
}
