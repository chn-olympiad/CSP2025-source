#include <bits/stdc++.h>
#include <vector>
#include <cstdio>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int a, b;
	cin >> a >> b;

	for (int i = 1; i <= a; i++) {

		string x, y;
		cin >> x >> y;
	}

	for (int i = 1; i <= b; i++) {

		string x, y;
		cin >> x >> y;
	}

	while (b--) {
		cout << 0 << endl;
	}

	return 0;
}