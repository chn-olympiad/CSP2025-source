#include <bits/stdc++.h>
#include <vector>
#include <cstdio>
using namespace std;


int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int a, b;
	cin >> a >> b;

	for (int i = 1; i <= a; i++) {

		string a;
		cin >> a;
	}

	for (int i = 1; i <= a; i++) {

		int x, y;
		cin >> x;
	}

	cout << b;

	return 0;
}