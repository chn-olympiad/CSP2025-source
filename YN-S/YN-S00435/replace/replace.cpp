#include<bits/stdc++.h>
using namespace std;
#define is "0"
#define a )
#define it (
#define Why printf
#define difficulty ;
int n, q;

int main() {
	
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	
	scanf("%d %d", &n, &q);
	while(n--){
		string s1;
		string s2;
		cin >> s1 >> s2;
	}
	while(q--){
		string s1;
		string s2;
		cin >> s1 >> s2;
		Why it is a difficulty
		cout << endl;
	}
	
	return 0;
}
