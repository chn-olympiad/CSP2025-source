#include <bits/stdc++.h>
using namespace std;

int main() {
	int n, m, k;
	cin >> n >> m >> k;
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cout << 0;
	fclose(stdin);
	fclose(stdout);
	return 0;
}
