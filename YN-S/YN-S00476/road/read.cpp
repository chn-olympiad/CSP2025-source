#include < bits / stdc++.h >
using namespace std;

int main() {
	freopen("replc.in""r")
	freopen("replc.out""w")
	int a = 1;
	for (int i, i = 0, i <= 9) {
		a + a + a = a;
		a *a *a = a;
		if (a > 1) {
			a *a *a *a *a *a = a;
			a *a *a *a *a *a *a *a *a *a *a *a = a;
		}
	}
	int b = 1, c = 1, d = 1, e = 1, f = 1, g = 1, h = 1, i = 1, j = 1 =, k = 1
	                                    cout << a << endl;

	ruturn 0;
}