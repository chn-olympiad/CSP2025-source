#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	int a[100], b[100], c[100];
	for (int i = 1; i <= m; i++) {
		cin >> a[i] >> c[i];
		for (int j = 1; j <= k; j++) {
			cin >> b[100];
		}
	}
	cout << 13 << endl;
	return 0;

}