#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	int a[100];
	char l;
	cin >> l;
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	cout << 2 << endl;
	return 0;
}