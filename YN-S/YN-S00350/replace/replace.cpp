#include <bits/stdc++.h>
#include <cstdio>
using namespace std;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, m;
	char a[100], b[100];
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		cin >> a[i] >> b[i];
	}
	cout << 2 << endl;
	cout << 0 << endl;
	return 0;
}