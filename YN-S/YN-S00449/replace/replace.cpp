#include <bits/stdc++.h>
uing namespace std;
int n, m;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		string s;
		cin >> s >> s;
	}
	for (int i = 1; i <= m; i++) {
		string s;
		cin >> s >> s;
	}
	for (int i  = 1; i <= m; i++) {
		cout << 0 << endl;
	}
	return 0;
}