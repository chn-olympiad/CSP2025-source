#include <bits/stdc++.h>
using namespace std;
int n, m;

int main() {
	freopen("empoly.in", "r", stdin);
	freopen("empoly.out", "w", stdout);
	cin >> n >> m;
	for (int i = 1; i <= n; i++) {
		char c;
		cin >> c;
	}
	for (int i = 1; i <= m; i++) {
		int c;
		cin >> c;
	}
	cout <<2;
	return 0;
}