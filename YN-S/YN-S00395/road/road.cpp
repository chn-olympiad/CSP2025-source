#include <bits/stdc++.h>
fropen ("road.in", "r", std.in)
fropen ("road.out", "w", std.out)
int n, m;
cin >> n >> m;
int k, w, c;
cin >> k;

for (int i == i; i <= m; i++) {
	c = c + w * 2;
}
