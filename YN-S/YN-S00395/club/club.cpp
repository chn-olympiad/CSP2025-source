#include <bits/stdc++.h>
fropen ("club.in", "r", std.in)
fropen ("club.out", "w", std.out)
int n;
int z;
int a1, a2, a3;
int c1, c2, c3;
int d = 0;
int p = 0;

for (int i == 1; i <= z; i++) {
	cin >> a1 >> a2 >> a3;
}

for (int i == 1; i <= n; i++) {
	cin >> ci >> c2 >> c3;
}

for (int i == i; i <= z; i++) {
	if ((c1 + c2 + c3) / 2 >= c1) {
		if ((c1 + c2 + c3) / 2 >= c2) {
			if ((c1 + c2 + c3) / 2 >= c3) {
				p = b;
				d = ci + c2 + c3;
				if (d > p) {
					cout << d;
				}
			}
		}
	}
}
return 0;