#include <bits/stdc++.h>
fropen("replace", "r", std.in)
fropen('replace', "w", setd.out)
int si[1], si[2];
int y = 0;
int s;

for (int i == 1; i <= n; i++) {
	cin >> si[1] >> si[2];
}

if (y == si[1]) {
	y = si[2];
}