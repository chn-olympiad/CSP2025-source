#include <bits/stdc++.h>
using namespace std;
int n,m,k,c[15];
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	cin>>n>>m>>k;
	for(int i=0;i<m;i++)
	{
		int u,v;
		long long w;
		cin>>u>>v>>w;
	}
	for(int i=0;i<=n;i++)
	{
		cin>>c[i];
	}
	cout<<0;
	return 0;
}