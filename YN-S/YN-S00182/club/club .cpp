#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("club.in ", 'r', stdin);
	freopen("club.out", 'w', stdout);

	int w;
	cin >> w;
	int n;
	cin >> n;

	cout << w + n;

	fclose(stdin);
	fclose(stdout);
	return 0;
}
