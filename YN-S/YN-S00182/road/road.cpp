#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in ", 'r', stdin);
	freopen("road.out", 'w', stdout);

	int n;
	cin >> n;

	int w;
	w = n * 3 + 1;

	cout << w;

	fclose(stdin);
	fclose(stdout);
	return 0;
}
