#include <bits/stdc++.h>
using namespace std;
string a;
int b[10000];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	cin >> a;
	for (int i = 1; 1 <= n; i++) {
		cin >> b[i];
	}
	cout << 2;
	return 0;
}