#include <bits/stdc++.h>
using namespace std;
int a, b, c, d, e, f, g;

int main() {
	freopen('employ.in', 'r', stdin);
	freopen('employ.out', 'w', stdout);
	cin >> a >> b >> c >> d >> e >> f >> g;
	cout << "2";
	return 0;
}