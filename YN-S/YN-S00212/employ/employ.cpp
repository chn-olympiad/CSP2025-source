#include <bits/stdc++.h>
using namespace std;
int n, m;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "r", stdout);
	cin >> n >> m ;
	string s;
	cin >> s;
	for (int i = 1; i <= n; ++i) {
		int x;
		cin >> x;
	}
	cout << 0 << endl;
	return 0;
}