#include<bits/stdc++.h>
using namespace std;
const int maxn = 5e6+10;
char s[maxn];
string s1, s2, s3, s4, s5, s6;
int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	int n, q;
	cin >> n >> q;
	while (n--) {
		cin >> s1 >> s2;
	}
	while (q--) {
		cin >> s3 >> s4;
	}
	if (q == 1) {
		cout << "0";
	}
	return 0;
}

