#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
const ll N = 2e5 + 5;
const ll Q = 2e5 + 5;
int n, q;
int dp[N];

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	scanf("%d%d", &n, &q);
	for (int i = 1; i <= n; i++) {
	}
	for (int i = 1; i <= q; i++) {
	}
	return 0;
}