#include <iostream>
#include <cstdio>
#include <cstring>
const int MAXN = 2e5;
int n, q;

int main() {
	std::freopen("replace.in", "r", stdin);
	std::freopen("replace.out", "w", stdout);
	std::cin >> n >> q;

	while (q--)
		std::cout << 0 << "\n";
	return 0;
}