#include <bits/stdc++.h>
using namespace std;
int a, b[100010];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m >> a;
	for (int i = 1; i <= n; i++) {
		cin >> b[i];
	}
	cout << 2;
	return 0;
}