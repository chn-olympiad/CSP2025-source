#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string s;
	cin >> s;
	for (int i = 0; i < n; i++) {
		int a;
		cin >> a;
	}
	if (m == 3 && n == 2) {
		cout << 2;
	} else {
		cout << 1;
	}


	fclose(stdin);
	fclose(stdout);
}