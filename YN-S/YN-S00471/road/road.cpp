#include <cstdio>
#include <iostream>
#include <vctor>
#include <cmath>
using namespace std;

typedef long long LL;
const Maxn = 1e4+5;
const Maxm = 1e6+5;
const Maxk = 10+5;
const Inf  = 1e18;
struct nold(){
   LL u,v,w;
};Edge[Maxm];

int main(){
freopen(road.in,"r",stdin);
freopen(road.out,"w",stdout);
ios::sync_whith_stio(false);
cin.tie(nullptr);
cout.tie(nullptr);

LL n,m,k,a,b,c;
cin>>n>>m>>k;
for(int i = 1;i<=m;++i){
        cin>>a>>b>>c;
        Edge[i] = {a,b,c};
    }
    for(int i = 1;i<=k;++i){
        cin>>vct a[i];
        for(int j = 1;i<=n;++i){
    }
    sort(Edge+1,Edge+m+1[]const mode&x,const node&y){return x.w<}
return 0;
}
