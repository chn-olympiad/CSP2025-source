#include <iostream>
#include <cstdio>
using namespace std;

int main() {
	FILE *fin, *fout;
	fin = fopen("road.in", "rb");
	fout = fopen("road.out", "wb");
	int n, m, k, u, v, w;
	int z = 13;

	fscanf(fin, "%d%d%d", u, v, w);
	if (n = 4, m = 4, k = 2) {
		fprintf(fout, "d", z);
	}
	return 0;
}