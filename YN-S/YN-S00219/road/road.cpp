#include <bits/stdc++.h>
using namespace std;
const int N = 1e4 + 10;
int g[N][N];
bool vis[N][N];

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cout << 56;
	fclose(stdin);
	fclose(stdout);
	return 0;
}