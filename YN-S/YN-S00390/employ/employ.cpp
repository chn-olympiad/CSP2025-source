#include <cstdio>
#include <iostream>
#include <algorithm>
#include <string.h>
#include <map>
#include <queue>
#include <vector>
#include <stack>

using namespace std;

int n;

int main(void) {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);

	scanf("%d", &n);
	printf("%d", n);

	fclose(stdin);
	fclose(stdout);
	return 0;
}