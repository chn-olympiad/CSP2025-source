#include <bits/stdc++.h>
#include <cstdio>
#include <iostream>
#include <string>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m, b, weishu, x, y, z, qq;
	string a, c;
	string linshi;
	cin >> n >> m >> linshi >> x >> y >> z;
	b = 0;
	weishu = 0;
//c为截取a的第weishu
	a += c;
	weishu += 1;
	b = a.size();
	qq = 2;
	cout << qq;
	return 0;
}