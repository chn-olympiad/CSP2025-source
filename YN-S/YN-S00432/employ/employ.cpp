#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	int n, m;
	cin >> n >> m;
	string str;
	cin >> str;
	for (int i = 0; i < n; i++) {
		int a;
		cin >> a;
	}
	cout << 0 << endl;
	return 0;
	fclose(stdin);
	fclose(stdout);
}

