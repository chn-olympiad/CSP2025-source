#include <bits/stdc++.h>
using namespace std;

int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m, k;
	cin >> n >> m >> k;
	for (int i = 0; i < m; i++) {
		int a, b, c;
		cin >> a >> b >> c;

	}
	for (int i = 0; i < k; i++) {
		int a;
		cin >> a;
		for (int j = 0; j < n; j++) {
			int b;
			cin >> b;
		}
	}
	cout << 13 << endl;
	fclose(stdin);
	fclose(stdout);
	return 0;
//	cout << LONG_LONG_MAX << endl;
}

