#include <bits/stdc++.h>
using namespace std;
const int maxn = 5100, M = 998244353;
int n, m, c[maxn];
char s[maxn];

int main() {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	scanf("%d %d", &n, &m);

	for (int i = 1; i <= n; i++) {

		scanf("%c", &s[i]);
	}

	for (int i = 1; i <= n; i++) {

		scanf("%d", &c[i]);
	}


	return 0;
}