#include <bits/stdc++.h>
using namespace std;
string a;
int n, m, b;

int main(void) {
	freopen("employ.in", "r", stdin);
	freopen("employ.out", "w", stdout);
	cin >> n >> m;
	cin >> a;
	for (int i = 0; i < n; i++) {
		cin >> n;
	}
	cout << 123456789;
	return 0;
}