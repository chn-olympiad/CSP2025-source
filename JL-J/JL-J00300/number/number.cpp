#include<bits/stdc++.h>
using namespace std;
int main(){
    //freopen("number.in","r"stdin);
    //freopen("number.out","w"stdout);
    int a,b=0,c[1001];
    cin>>n;
    for(int i=0;i<n;i++){
        if (n[i]>=0){
            a[n]+=i;
        }
    }
    int d=0,num=1;
    while(k<0){
        for(int i=0;i<d;i++){
            if(a[i]>num){
                num=a[i];
            }
        }
        cout<<num;
        for(int i=0;i<n;i++){
            if(num=a[i]){
                a[i]=1;
                num[0];
                break;
            }
        }
    }
    k--;
}
