#include<bits/stdc++.h>
using namespace std;
int n,k;
int a[50006];
int main(){
    freopen("xor.in","r",stdin);
    freopen("xor.out","w",stdout);
for(int i=0;i<n;i++)
        cin>>a[i];
    cout<<1;//what the hell is this?
    fclose(stdin);
    fclose(stdout);
return 0;
}
