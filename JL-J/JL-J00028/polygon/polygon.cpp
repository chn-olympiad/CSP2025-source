#include<bits/stdc++.h>
using namespace std;
int a[1000];
void c(int big){
    while(m>=3){
        totol+=m;
        big=max(m);
        if(total>=2*big){
           cnt++;
        }
        m--;
    }
}
int main(){
    int n,m,cnt=0;
    cin>>n;
    freopen(polygon.in,'r',stdin);
    freopen(polygon.out,'w',stdout);
    for(int i=1;i<=n;i++){
        cin>>a[i];
        m=a[i]
    }

    cout<<cnt;
    return 0;
}
