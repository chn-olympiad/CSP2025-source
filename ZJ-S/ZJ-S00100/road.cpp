#include <bits/stdc++.h>
using namespace std;

int main()
{
    freopen("road.in", "r", stdin);
    freopen("road.out", "a", stdout);
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long n, m, k, tmp;
    cin >> n >> m >> k;
    for (long long i = 1; i <= 3 * m + (1 + n) * k; i++)
    {
        cin >> tmp;
    }
    cout << "\n";
    return 0;
}