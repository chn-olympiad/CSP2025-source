#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "a", stdout);
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, q;
    cin >> n >> q;
    string tmp;
    for (int i = 1; i <= (n + q) * 2; i++)
    {
        cin >> tmp;
    }
    cout << "\n";
    return 0;
}