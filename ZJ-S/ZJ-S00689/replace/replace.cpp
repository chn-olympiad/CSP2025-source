#include <bits/stdc++.h>
const int N = 2e5 + 5;
using namespace std;

int n, q;
std::string s[N][2], t[2];

signed main()
{
	std::freopen("replace.in", "r", stdin;
	std::freopen("replace.out", "w", stdout);
	
	std::cin >> n >> q;
	for (int i = 1; i <= n; ++i)
	{
		std::cin >> s[i][1] >> s[i][2];	
	}	
	while (q--)
	{
		std::cin >> t[1] >> t[2];
		std::cout << 0 << '\n';
	}
	return 0;
}