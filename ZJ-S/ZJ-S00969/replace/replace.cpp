#include <bits/stdc++.h>

using namespace std;

const int N = 2e5 + 10, M = 5e6 + 10, P = 1331;

int n, q;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 1; i <= n; ++i) {
		string x, y;
		cin >> x >> y;		
	}
	for (int i = 1; i <= q; ++i) {
		string x, y;
		cin >> x >> y;
		cout << 0 << '\n';
	}
	return 0;
}
