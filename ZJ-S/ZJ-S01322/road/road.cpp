#include<bits/stdc++.h>
using namespace std;
//struct road{
//	int from, to, pay;
//}a[2000000];
//int c[2000000];
int main() {
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
//	int n, m, k;
//	cin >> n >> m >> k;
//	for (int i = 1; i <= m; i++) {
//		cin >> a[i].from >> a[i].to >> a[i].pay;
//	}
//	for (int i = 1; i <= k; i++) {
//		cin >> c[i];
//		for (int j = 1; j <= n; j++) {
//			cin >> a[j + m].pay;
//			a[j + m].from = j + m;
//			a[j + m].to = j; 
//		}
//	}
	cout << 2025;   // Happy 2025!!!
	
	
	
	
	return 0;
}
