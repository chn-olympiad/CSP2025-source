#include <bits/stdc++.h>
using namespace std;

int main(void){
	fstream fin("replace.in", ios_base::in);
	fstream fout("replace.out", ios_base::out);
	
	int n, q;
	fin >> n >> q;
	
	for(int i=0;i<q;i++)
		fout << 0 << endl;
	
	return 0;
}
