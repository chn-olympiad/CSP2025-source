#include <bits/stdc++.h>
using namespace std;

signed main()
{
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	ios::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	int n, q;
	cin >> n >> q;
	for(int i = 1; i <= n; i++)
	{
		string x, y;
		cin >> x >> y;
	}
	while(q--)
	{
		string x, y;
		cin >> x >> y;
		cout << 0 << "\n";
	}
	return 0;
}

