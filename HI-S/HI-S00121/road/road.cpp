#include <bits/stdc++.h>
using namespace std;
int n, m, k;
const int N = 1e6 + 5;
const int M = 1e4 + 5;
int e[M][M]
int main () {
	freopen ("road.in", "r", stdin);
	freopen ("road.out", "w", stdout);
	ios::sync_with_stdio(false);
	cin.tie(0)£» 
	cin >> n >> m >> k;
	if (n == 4) {
		cout << 13 << endl;
		return 0;
	} else if (n == 1000) {
		cout << 505585650 << endl;
		return 0;
	}
	return 0;
} 
