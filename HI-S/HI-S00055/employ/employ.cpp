#include<bits/stdc++.h>
using namespace std;
int n , m , k , minn = 0; 
int main(){
	freopen("employ.in","r",stdin);
	freopen("employ.out","w",stdout);
	cin >> n >> m >> k;
	for(int i = 1 ; i <= n ; i++){
		int u , v , w;
		cin >> u >> v >> w;
	}
	for(int j = 1 ; j <= k ; j++){
		int c , a[10005];
		cin >> c;
		for(int j = 1 ; j <= m ; j++)
			cin >> a[j];
	}
	cout << minn;
	return 0;
}

