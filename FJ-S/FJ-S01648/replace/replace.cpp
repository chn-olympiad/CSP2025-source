#include <bits/stdc++.h>
#define int long long
#define inf 1e18
using namespace std;
signed main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	freopen("replace.in","r",stdin);
	freopen("replace.out","w",stdout);
	int n,q;
	cin>>n>>q;
	for(int i=1;i<=n;++i){
		string x,y;
		cin>>x>>y;
	}
	while(q--){
		string x,y;
		cin>>x>>y;
		cout<<0<<endl;
	}
	fclose(stdin);
	fclose(stdout);
	return 0;
}
