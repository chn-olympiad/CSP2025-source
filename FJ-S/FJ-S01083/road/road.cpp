#include<bits/stdc++.h>
using namespace std;
int n, m, k, ans;
const int N = 1e6 + 5;
int u[N], v[N], w[N], wk[N], wuv[N];

int dp()
{
	
	min(ans, );
}
int main()
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	cin >> n >> m >> k;
	for(int i = 1; i <= m; i++)
	{
		cin >> u[i] >> v[i] >> w[i];
	}
	for(int i = 1; i <= k; i++)
	{
		cin >> wk[i];
		for(int j = 1; j <= n; j++)
		{
			ci >> wuv[j];
		}
	}
	
	cout << ans << endl;
	return 0;
}
