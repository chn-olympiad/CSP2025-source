#include<bits/stdc++.h>
using namespace std;
int t,n,f[10005][4],jn[4];f1=0;f[10005][4];ans;
int main(){
scanf("d%";&t);
while(i--)
	scanf(d%;&n);
		for (int i=1;i<=n;i++){
		scanf("%d%d%d";a[j][1]&a[i][2]&a[j][3]);
		if(a[j][1]>a[j][2],fl[i]=1)
		f[j][1]=[j][2]=[j][3]=0;
		if(a[j][i]=a[j][2]=a[j][3])
		for int(i=1;i<=n;i++)
		for
		
			else
	}
}
return 0;
