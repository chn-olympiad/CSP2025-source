#include <bits/stdc++.h>
using namespace std;
int n, q;
string s[20007][2], t[20007][2];
int main()
{
	freopen ("replace.in", "r", stdin);
	freopen ("replace.out", "w", stdout);
	cin >> n >> q;
	for (int i = 1; i <= n; i++)
	{
		cin >> s[i][1];
		cin >> s[i][2]
	}
	for (int i = 1; i <= q, i++)
	{
		cin >> t[i][1];
		cin >> t[i][2];
	}
	return 0;
}
