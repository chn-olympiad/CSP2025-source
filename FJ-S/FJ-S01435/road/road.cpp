#include <bits/stdc++.h>
using namespace std;
const int MOD = 998244353;
int main ()
{
	freopen("road.in", "r", stdin);
	freopen("road.out", "w", stdout);
	int n, m;
	scanf("%d %d", &n, &m);
	for (int i = 1 ; i <= n ; i ++)
	{
		char ch;
		scanf("%c", &ch);
	}
	for (int i = 1 ; i <= n ; i ++)
	{
		int c;
		scanf("%d", &c);
	}
	printf("%d\n", 12 % MOD);
	fclose(stdin);
	fclose(stdout);
	return 0;
}
