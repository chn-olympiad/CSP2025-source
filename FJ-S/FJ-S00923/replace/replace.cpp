#include <stdio.h>
#include <algorithm>
#include <math.h>

using namespace std;

int n, q;

int main() {
	freopen("replace.in", "r", stdin);
	freopen("replace.out", "w", stdout);
	// Main Code
	scanf("%d%d", &n, &q);
	for (int i = 1; i <= q; i++) {
		printf("0\n");
	}
	return 0;
} 
