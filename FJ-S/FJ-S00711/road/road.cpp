#include<bits/stdc++.h>
using namespace std;
int n,m,k,i,a[2],f,c,t,a[j],v,w,x;
int main(){
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	cin>>n>>m>>k;
	for(int i,i<=m,i++){
		cin>>a[2];	}
	for(int j,j<=n,j++){
		cin>>c>>" ">>a[j];
	}
	for(int b,v,b<=1,v<=1){
		if(a[2][b]=a[2][v]){
		w=a[2][2]+a[2][2];
		sort(w){
			t=first(w);	}
		}
	}
	sort(c){
		c=first(c);
	}
	sort(a[j]){
		x=first(a[j])+second(a[j]);
	}
	cout<<x+t+c;
	return 0;
}
