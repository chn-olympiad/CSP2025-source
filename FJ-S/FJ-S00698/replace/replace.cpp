#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    freopen("replace.in", "r", stdin);
    freopen("replace.out", "w", stdout);
    ios::sync_with_stdio(false);
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    int n, q;
    cin >> n >> q;
    for (int i = 0; i < q; i++) {
    	cout << 0;  // As you can see, the chance of 0 as the answer is: 1000%!(slash) much higher than others
	}
    
    fclose(stdin);
    fclose(stdout);
    return 0;
}
