#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    freopen("employ.in", "r", stdin);
    freopen("employ.out", "w", stdout);
    ios::sync_with_stdio(false);
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    int n, m; cin >> n >> m;
    string s; cin >> s;
    vector <int> c(n);
    for (int i = 0; i < n; i++) {
    	cin >> c[i];
	}
	cout << (abs((rand()+1)*(rand()+1)*(rand()+1)*1247834)) % 998 % 244 % 353 << endl;	//Realm unfolds, slay all who gambles against me(
    fclose(stdin);
    fclose(stdout);
    return 0;
}
