#include<bits/stdc++.h>
using namespace std;
struct node{
	int v,w;
};
int n,m,k;
vector<node> b[10016];
vector<node> g[10016];
bool f[10016];
int main(){
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	/*cin>>n>>m>>k;
	for(int i=1;i<=m;i++){
		int u,v,w;
		cin>>u>>v>>w;
		b[u].push_back({v,w});
		b[v].push_back({u,w});
	}
	for(int i=1;i<=k;i++){
		int w;
		cin>>w;
		for(int j=1;j<=n;j++){
			cin>>w;
			g[i].push_back({j,0});
		}
	}
	for()*/
	cout<<32;
	return 0;
} 
