#include<bits/stdc++.h>
using namespace std;
int n, m, k;
int main()
{
	freopen("road.in","r",stdin);
	freopen("road.out","w",stdout);
	cin >> n >> m >> k;
	int u[m + 1], v[m + 1], w[m + 1];
	int a[k + 1];
	for (int i = 1;i <= m;i++)
		cin >> u[i] >> v[i] >> w[i];
	for (int j = 1;j <= k;j++)
		cin >> a[j];
	
	return 0;//Ã»Ñ§¹ýqwq 
}
